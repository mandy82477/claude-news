# Workaround 登記表

每上一個 workaround（繞路而非真解）就在此登記一列，避免繞路悄悄變永久（見根目錄 `CLAUDE.md`「完工定義」第 3 條）。
`scripts/open_loops.py` 每週讀此表，**逾「複查日」仍在「進行中」的列**會被列出提醒。

## 使用規則

- **上 workaround 時當場新增一列**到「進行中」（繞路內容 / 真解 / owner / 複查日 / 狀態）
- **複查日** = 預期能上真解的日期；到期未解就順延並在狀態欄記一次「（已順延 N 次）」
- **拿到真解後**：把該列移到「已收斂」區並標 ✅ 與收斂日；git history 留存原繞路
- 狀態符號：🟡 繞路中 ／ 🔴 繞路中且已逾期 ／ ✅ 已上真解

## 進行中

| 繞路內容 | 真解 | owner | 複查日 | 狀態 |
| **懸置佇列額度 Lane A 10／Lane B 5 是拍的，無實測依據**（2026-08-29 分流改版）：額度的物理意義是「主編一輪願意花的時間／token」，但這兩個數字沒有任何量測支撐。實測顯示 Lane A 只有 8 筆（額度 10 不具約束力），Lane B 有 43 筆、以 5 筆/週需 8.6 週排空，且近 7 天新增 24 筆 > 每週產能 15 筆——**結構性淨增，額度目前擋不住積壓** | 首次跑完 5c 後把實測值填回 `scripts/check_pending_markers.py` 的常數註解（Lane B 每筆約 N 次 WebFetch＋一次回掃、實測 X 分鐘；每輪預算 Y 分鐘 → 幾筆），四處同步由 review-registry 的「Lane A/B 額度四處同步」把關。若實測後仍淨增，則屬**方向性取捨**（要花多少人力在這件事上）→ 需使用者裁示，不由 Claude 自行調高 | Claude（首次 5c 後）＋使用者（若需調整投入） | 2026-09-05 | 🟡 暫定值待校準 |
| ~~**source funnel 的 `filtered`/`emitted` 低估低流量來源**~~（2026-08-16 診斷，**同日修復**：`FeedItem.contributors` 記錄 dedup 併掉的來源、`_count_by_prefix` 改按所有貢獻來源計數、日報來源欄以 `＋` 列出多來源、記者據此每來源各報一筆歸因。歷史 funnel 資料仍為舊語意，記分卡的長期平均要再累積數日才反映真值）（原始描述）：兩欄是按**存活條目的 source 標籤**回算，而 dedup 只留勝者標籤——低流量的官方來源（Anthropic Blog、Official Docs）幾乎必定輸給 HN／Google News，於是長期顯示「gathered N → filtered 0」，讀起來像該來源零產出。2026-08-15 實例：Anthropic Blog 抓到〈How Claude's text watermarking works〉，五來源合併後掛 HN 名下，自己那欄歸零——當日最大條新聞的功勞完全不計。同一假象也讓 `data/source_attribution.jsonl` 的 `anthropic-blog` 長期偏低（記者照日報的來源標籤歸因）。**尚未影響任何告警誤判**（`ok=true` 且 count=0 已於 2026-08-15 改為不發連續零告警），但會誤導來源記分卡與來源×類別 graph 分析 | dedup 合併時在勝者記下 `contributing_sources` 集合，funnel 兩欄改按該集合分攤（或並列「主掛名／有貢獻」兩組數字）；需同步確認 `check_gather_health.py` 不假設 `filtered ≤ gathered` | Claude | 2026-08-30 | 🟡 已診斷未修（量測失真，非資料遺失） |
| **`--confirm-digest` 舊 `gathered_items.json` 的相容窗**（2026-08-16 dedup_key 上線附帶）：confirm 路徑改讀 `{url, dedup_key}`，對缺 `dedup_key` 欄位的舊檔以空字串 fallback 回裸 URL。若在同一天內「用新版 gather、舊版 confirm」或反之，變更偵測類條目會確認到錯的鍵、被重複提供一輪 | 下一次 `/news-pipeline` 完整跑完（gather 與 confirm 同版）即自然收斂，屆時刪除本列 | Claude（下次 pipeline 後的 session） | 2026-08-18 | 🟡 過渡期 |
|---|---|---|---|---|
| **`entities/claude-code.md` 版本更新／歷史記錄表格逾百筆儲存格 > 120 字元**（2026-08-29 lint 提出，2026-08-30 使用者裁示另立專項）：`.claude/rules/wiki-ingest-format.md`「表格放結論，細節下沉」全站通用，但該頁 550–767 行區間的存量規模過大，隨手修會排擠當日 ingest；每日呈現品質審查因此連續標 📋 待辦而非 ⚠️ 已修復 | 分批短語化：每次 lint 處理一個月份區間的版本表列，細節下沉至表下；`managed-agents.md`（5 格）與 `claude-skills.md`（3 格）同型但規模小，可隨附處理 | Claude | 2026-09-27 | 🟡 |
| 2026-07-25 週六 01:03 UTC 的 weekly-wiki-lint-cloud 觸發後**無聲失敗**（無 commit、無 log 紀錄、無 abort 證據，三個週更頁停在 07-18）——舊 prompt 的最後一次執行，死因不可考；本機補跑 `/wiki-lint` 恢復本週週更 | 薄殼＋runbook 架構（2026-07-25 上線）＋心跳紀錄（2026-07-27 補，`wiki-lint.md`「10. 收尾閉迴路」步驟 4）＋STARTED 開跑標記（2026-07-27 補，`_shared.md`「無人值守原則」）：08-01 首跑判定改為**四態**——`STARTED + OK 心跳`＝收斂；`STARTED + FAILED`＝失敗可追查；`STARTED 無後續`＝中途死（07-25 的死法，首次可診斷）；`完全無 STARTED`＝trigger 未觸發或環境起不來。另 08-01 為 8 月首次 lint，月度「外部死鏈」已在 runbook 明令雲端跳過（egress 封鎖會產生假死鏈），驗證時確認**沒有**大量「原文已失效」標註出現即為防護生效 | Claude（08-01 後首個 session） | 2026-08-01 | 🟡 待下週六驗證 |
| **GitHub Search 成長偵測器的歷史存量盲區**（2026-08-04 改版附帶）：**>3000 星段已於 2026-08-28 由 C 存量盤點窗自動收斂**（判準取自 `news/*.md`，每日至多 2 則，掛所有 scope；比原訂的一次性人工掃描更好——它可重複、且日後新增 scope 自動補存量）。**剩餘：500..3000 中段仍卡住**——2026-08-28 實測確認 B 窗升冪每日回傳同一批最低 8 筆，首日吐完即停，之後每天 0 新；該帶尚有 345 個 repo（claude-code 319／mcp-server 26）進不來 | 對 B 窗套用與 C 窗相同的技法：擴大 per_page、先以 `news/*.md` 濾掉已報導者再截斷，窗才會逐日往上走。**未做，因為它會讓日報每日新增最多 16 則社群工具條目（345 筆積壓約 3 週清完），量體變動需使用者裁示** | 使用者裁示後由 Claude 實作 | 2026-09-04 | 🟡 待裁示（>3000 段已收斂）|
| **pipeline-change-check compare 待跑**（2026-08-04 GitHub Search 改版）：baseline 已記（`src/logs/pipeline_baseline.json`，對照 2026-08-01 digest）；改版後首次 pipeline (雲端 08-04 13:00 UTC 或本機) 跑完須執行 `/pipeline-change-check compare` 對照，特別留意 GitHub 來源 count 是否脫離連續 4 日 0 的狀態、以及首日湧入量 | 跑 compare 確認無 ⚠️ 漂移即移入已收斂 | Claude（下次 pipeline 後的 session） | 2026-08-06 | 🟡 待對照 |
| Reddit RSS 走 `sort=top&t=week` 加「· 週熱門」標記，wiki 視標記為達低門檻（RSS 天生無分數 → score 恆 0） | 設 Reddit OAuth 憑證（環境變數 `REDDIT_CLIENT_ID` / `REDDIT_CLIENT_SECRET`）→ `reddit.py` OAuth 路徑取真實 `ups`；之後退役「週熱門」特例 | 使用者（reddit.com/prefs/apps 建 script app） | 2026-07-24 | 🟡 繞路中 |
| GitHub API 走匿名（60 req/hr、搜尋 10 req/min），GitHub Search 常撞限流回 0。**2026-08-29 update：C 存量盤點窗上線後，每次 gather 的 search 呼叫由 6 次增為 10 次，正好等於匿名上限**——GH Actions 有內建 token（30/min）不受影響，本機手動跑則已無餘裕，再加任何 scope 必須先有 PAT | 設 GitHub PAT（環境變數 `GITHUB_TOKEN`，classic token 免勾 scope）→ 60/hr 升 5000/hr，`github_releases.py` 已備 Authorization 路徑 | 使用者（github.com Settings→Developer settings→Tokens classic） | 2026-08-10 | 🟡 繞路中（選配，非資料殘缺） |
| Blogroll RSS 名單已於 2026-07-11 確認上線（simonwillison / jessevincent / arminronacher / antirez，皆 probation）；煙霧測試 0 命中已診斷為 (a) 26h 窗內無 Claude/Anthropic 相關新文，非技術問題（4 個 feed HTTP 200、bozo=False、時間解析正常，Simon Willison 窗內僅 1 篇且與主題無關已正確被關鍵字擋下）。剩餘待辦：probation 首月觀察（汰換節奏已定：來源記分卡 2026-07-16 上線，每週隨 `/wiki-lint` 6e 執行，見 `docs/source-scoring-optimization.md`） | 30 天 probation 期滿（2026-08-11）後以記分卡數據檢視命中率決定去留 | 使用者 | 2026-08-11 | 🟡 繞路中 |
| news-pipeline-steps.md 新增 Step 3c 摘要忠實度自檢（2026-07-17），`/pipeline-change-check baseline` 已記錄（對照 07-16 digest）；依規定改後首次 pipeline 跑完須執行 `compare` 對照，尚未執行 | 下次 `/news-pipeline` 跑完後執行 `/pipeline-change-check compare`，確認指標無 ⚠️ 漂移即可移入已收斂 | Claude（下次 pipeline session） | 2026-07-19 | 🟡 待對照 |
| `data/external/domain_pc1.csv`（Lin et al. 2023 domain 信譽表）底層評級為 2022–2023 快照，新站查無資料落「未知」桶 | 每季複查：記分卡「未知」桶占比持續 > 30% 時重新下載上游（hauselin/domain-quality-ratings）或評估補充 idiap / Iffy Index 資料集 | Claude（隨 `/wiki-lint` 順檢） | 2026-10-16 | 🟡 繞路中（低風險，查無＝中性不懲罰） |
| **官方頁監看 state 的持續成本**（2026-08-29 段落級 diff 上線附帶）：`official_watch_state.json` 由數 KB 增為約 310 KB（存官網原文段落供比對），每次抓料 commit 都會重寫。存原文是「說得出移除了哪一段」的必要條件。曾以跨頁樣板過濾壓到 205 KB，但該機制連續三輪 review 各衍生一個正確性缺陷，且其宣稱的第二個好處（抑制噪音）經實測不成立——diff 是同一頁今昨的集合差，穩定的導覽段自動抵銷——故已整套移除，體積回升是刻意接受的代價 | 觀測三個月的 repo 體積成長；若成為負擔，可只對「移除偵測」保留原文、其餘改存段落雜湊（代價是移除時只說得出「少了 N 段」說不出內容）。 只擋單頁上限，不擋檔案總量 | Claude | 2026-11-29 | 🟢 已知取捨，觀測中 |
| **volatile chrome 的判準是措辭白名單**（2026-08-29 第 9 輪 review）：`_VOLATILE_RE` 要求字面 `updated` 前綴（必填是為了不吃掉正文「less than 14 days ago」這類退款期限）。代價是措辭脆弱——`Updated: 2 weeks ago`（帶冒號）、`Updated a few seconds ago`（moment.js 型）都會漏。Intercom 現行輸出無冒號故現在無事，但 Help Center 換版型就回到「每週 3–4 則假條目」 | 不修：擴充 regex 會再度逼近「吃掉正文相對日期」那條線，而必填正是為了離開它。改為在 Help Center 版型變動時處理——屆時 golden digest 與時間戳絕對斷言會先紅 | Claude | 2026-11-29 | 🟢 已知取捨 |
| **state 的 hash/length 未隨演算法變更同步遷移**（2026-08-29 第 8 輪 review 指出）：第 7、8 輪兩度改動 （加入、再調整 volatile 剝除），state 只就地正規化了 ，hash: hash table empty/ 留舊值。下次跑時這幾頁 hash 必然不同，會落到 40 字元長度閘——被剝字串長度為 17～24，單次出現時 <40 靜音，但若同一頁在頁首頁尾各渲染一次則 ≥40，當天會發一則退化條目 | 不修：離線無法驗證真實頁面的渲染次數（state 存的是集合，看不出重複）。最壞是 1–4 則一次性的退化條目，非靜默錯誤。下次 pipeline 跑完即自然收斂 | Claude | 2026-09-05 | 🟢 已知取捨 |
| **不含阿拉伯數字的事實變動可能被靜音**（2026-08-29 第 7 輪 review）：段落 diff 之後的判準是「差異裡有 `[0-9$%]` 變動 → 一定報，否則退回 40 字元長度閘」。於是 `per million → per thousand`（差 1 字元）、`free → paid`（差 0）、`two dollars → three dollars`（差 2）這類語意變動會被長度閘擋掉。官方定價頁實際用的是 `$3 / MTok` 這種阿拉伯數字寫法，故現況未觀測到此類漏報 | 不修：擴充判準到拼字數字會把「輪替計數器 vs 價格」那條分不開的線再拉長一次，而這正是六輪 review 反覆栽的地方。此限制在舊的純長度閘下同樣存在（非本輪引入），改為登記並在官方改用拼字寫法時再處理 | Claude | 2026-11-29 | 🟢 已知取捨 |
| **僅出現在 2 頁的產品導覽列不被過濾**（2026-08-29 第 6 輪 review）：樣板門檻取 3 而非 2，因為  與 platform docs 定價頁是彼此鏡像的兩頁，門檻 2 會把真正的價格字串當樣板而**雙頁靜音**——那是唯一不可接受的偽陰性。代價是 9 段產品導覽（Claude Cowork、Claude Design…）留在比對範圍，官方上架新產品時該 2 頁會各報一則 | 不修：兩害相權，寧可多兩則可見的雜訊，不可讓價格變動靜音。若導覽列變動頻繁到造成困擾，再考慮把導覽列以獨立規則處理 | Claude | 2026-11-29 | 🟢 已知取捨 |
| **樣板判定在抓不齊時可能歸零**（2026-08-29 第 5 輪 review 指認）：樣板集合只從本輪抓成功的頁計算，需 ≥3 頁才可能產出元素。若共享索引那一族（6 個 support 頁）只剩 1–2 頁抓成功，樣板集合近乎空集，該輪這些頁的共享索引變動會被錯誤歸因到個別頁 | 不修：修法（把樣板集合存進 state 跨輪沿用）等於再造一個「儲存時綁定基準」的機制，而那正是前三輪三個缺陷的共同根源。此情境需同時滿足「多頁抓失敗」與「當輪索引剛好變動」，最壞是少數幾則錯誤歸因條目，非靜默錯誤（日報看得到）。續觀測 | Claude | 2026-11-29 | 🟢 已知取捨 |
| **GH Actions 排程延遲超過 11.6 小時時，該日仍無日報**（2026-08-29 收斂後的殘餘）：雲端 routine 改為一天六班重試（12/14/16/18/20/22 UTC），資料到了就做、沒到靜默留給下一班，最後一班涵蓋 +11.6 小時（實測最大 +11.2 小時，2026-08-28）。超過的話該日無日報——但**內容不遺失**：`main.py` 的 `check_gap_lookback()` 在昨日日報缺件時把回看窗拉到 50 小時，那些條目會併進隔日日報。**但這只在單日破洞成立**——該函式只看昨天一天，連續兩天沒出報時第二天仍只拉 50h，中間約 22 小時的內容真的會漏 | 不修：最後一班再往後就跨 UTC 午夜（22:00 + 執行 2h ＝ 24:00，已是邊界）；加抓料班次是在增加生產者（已試過並撤回）；掃 archive 補所有缺日會造成 wiki 逆序prepend（已試過並撤回）。續觀測延遲趨勢，若常態回到 +30 分鐘可考慮把班次往前挪 | Claude | 2026-09-12 | 🟢 已知取捨 |

## 已收斂

- ✅ **記者間「轉知」標記無接手驗收機制**（登記於 2026-08-01，收斂於 2026-08-15）：照懸置標記掃描的同構做法建帳本閉迴路——`scripts/pending_handoffs.py`（open／list／close／void，append-only、開立冪等、逾 14 天標 ⚠️）＋ `data/pending-handoffs.jsonl`；`wiki-ingest.md`／`wiki-lint.md` 派工訊息附「轉知待接手」清單、記者回報契約新增第八欄「轉知處置」（八份規則檔＋registry 同步）、主編彙整表明定 open／close／void 規則、pipeline Step 3 與 wiki 同批 commit。原繞路：主編口頭轉達、無人驗收。原列內容：**記者間「轉知」標記無接手驗收機制**：2026-08-01 第二次月度聚焦校準發現，session/cache 跨帳號洩漏疑慮（#74066，06-29~07-05 週）在日誌中兩度被標「轉知安全政策記者」「下次併入 ai-agent-safety」，但 30 天內 `wiki/topics/ai-agent-safety.md` 查無任何記錄——**轉知＝發出即忘，無人驗收**。現行繞路：靠…

- ✅ **雲端 routine 無法載入自訂 `subagent_type`（`wiki-reporter-*` 六份）**（登記於 2026-07-24，收斂於 2026-08-15，期間復現至少 8 次：07-18／07-19／07-22／07-24／08-08～08-14）：真解不是修環境，是**取消雙軌**。原本本機走原生 `wiki-reporter-*` subagent_type、雲端走 general-purpose＋內嵌規則，同一角色兩種構成方式，規則檔改動只有一條路徑自動吃到。2026-08-15 裁決把內嵌路徑轉正為唯一正典：`.claude/rules/wiki-ingest.md`「派工方式」定義 `subagent_type: "general-purpose"`＋prompt 首段固定角色前導（導向 `.claude/agents/wiki-reporter-[category].md`），`wiki-ingest.md`／`wiki-lint.md`／`wiki-weekly-review.md`／兩份雲端 runbook 同步，runbook 移除「降級執行」措辭與摘要欄。角色檔仍是規則單一來源（前導只導向、不重抄），自訂 agent 註冊照留供本機手動呼叫但派工流程不依賴。

- ✅ **雲端 sandbox 缺 `feedparser`／`sgmllib3k` 依賴鏈**（登記於 2026-07-14，收斂於 2026-08-01，期間復現至少 9 次、順延 4 次）：**根因已由上游移除，不是我們繞過去的。** 2026-08-01 查證 PyPI，`feedparser` 6.0.14 的 `requires_dist` 已從壞掉的 `sgmllib3k`（用 `setup.py install`、Python 3.11 撞 distutils `install_layout` AttributeError）換成 `feedparser-sgmllib>=2,<3`——後者是純 python wheel、無依賴、`requires_python >=3.10`，`pip install` 一次到位。即登記表原列的真解方向 (c)「上游移除 sgmllib3k 依賴」已由 feedparser 自己完成。

  **為什麼拖到現在才發現：** `cloud_bootstrap.py` 當初為了躲開壞掉的 sgmllib3k 而用 `pip install --no-deps`，這個旗標同時也讓 pip **永遠看不到上游換掉依賴**——workaround 親手擋掉了自己的退場路徑。四次復現（07-28 套件內路徑、07-31 獨立套件路徑、08-01 週度 lint、08-01 日更）每次都在補新症狀，沒有人回頭問「上游是不是已經修好了」。教訓：**長期繞路必須定期回頭查上游狀態，而不是只在它又炸的時候補洞**；凡是為了繞開壞依賴而加的 `--no-deps`／`--ignore-installed` 類旗標，都應視為「會遮蔽上游修復」的高風險項，登記時一併註明複查方式。

  **修法：** `cloud_bootstrap.py` 移除 `--no-deps`、鎖 `feedparser>=6.0.14` 讓 pip 正常解析依賴；`verify_feedparser_import()` 直接驗 `import feedparser.sgml` 成功才回報就緒（前三次都是 bootstrap 說就緒、實際仍炸）；手動 vendoring 修法降級為離線退路（退路二），另補退路一處理「環境預裝舊版 feedparser 而不觸發安裝」。`src/requirements_news.txt` 同步鎖 `feedparser>=6.0.14`。

  **驗證（乾淨 venv 三情境）：** ① 全新空環境 → 正常裝完、import 驗證通過，完全不需要 vendoring；② 預裝 `--no-deps feedparser==6.0.13`（07-31 現場重現）→ 精準重現 `ModuleNotFoundError: No module named 'feedparser.sgmllib'`，退路一升版後自動修復；③ 同環境重跑 → 全部跳過，冪等。另 `run_tests.py` 144 案例全過。

- ✅ **每日新聞摘要「待驗證」項目正式收斂**（收斂於 2026-07-28）：2026-07-25 改版後首次雲端實跑（`news/2026-07-28.md`）驗證規格確實落地——📌 今日聚焦 5 條全數使用 `[N]` 編號（非行內 `（ref: ...)`）；`grep -cE '^\*\*\[.+\]\(https?://' news/2026-07-28.md` = 27（3a 自檢 ≥5）；來源狀態表 10 列（3b 自檢 ≥8，UTF-8 locale 下驗證）；「選材門檻」與「今日聚焦參考連結」皆位於 📡 來源狀態表格之後的檔尾附錄，順序與格式與規格逐字相符。判定 LLM 手動生成路徑穩定產出符合規格的輸出，不需回頭修 Step 1b 措辭。

- ✅ **雲端排程薄殼架構首跑驗證**（收斂於 2026-07-26）：07-25 13:02 UTC 首次真實執行四項驗收全過——(a) 日報產出且符合新 Step 1b 格式（6 個 [N] 編號引用＋檔尾參考清單）；(b) 有 `data: confirm emitted-cache 2026-07-25` commit；(c) `emitted_items.json` 當日確認率 66/66=100%（對比前一日 1/54）；(d) watchdog 首跑（run #1）於 07-26 以 origin 內容重算四項判準全綠、無 GitHub 失敗通知信。薄殼 trigger → runbook 執行路徑確認可用。

- ✅ **雲端 routine 無法 commit `task_scheduler.log`**（2026-07-25 雲端回報、同日收斂）：根目錄 `.gitignore` 的 `logs/`（無前導斜線）連帶忽略 `CLAUDE_NEWS/src/logs/`，而 runbook 明文要求該檔必須 commit + push。已改用「放行目錄 → 排除目錄內容 → 單獨放行 task_scheduler.log」三行寫法（git 規定父目錄被排除時無法單獨 re-include 子檔案），並以 `git check-ignore` 驗證三種情境：目標檔可加入、同目錄其他 log 仍忽略、其他專案的 logs/ 仍忽略。雲端不再需要 `git add -f`。

- ✅ **「每日新聞摘要呈現優化」第一波接回真實生成路徑**（收斂於 2026-07-25，原登記 2026-07-24，複查日 2026-08-07 提前完成）：`.claude/commands/news-pipeline-steps.md` Step 1b 的日報格式規格已改為與 `digest.py::reformat_presentation()` 輸出一致——📌 今日聚焦區塊改用 `[N]` 編號引用（不再是行內 `（ref: URL）`），選材門檻說明與「今日聚焦參考連結」清單移到 📡 來源狀態表格之後的檔尾附錄，並附上完整範例供 LLM 手動生成時照抄格式。`python scripts/run_tests.py`（含 `check_rules.py` 全部同步配對）與 `/review-commands` 皆零錯誤。因規格是給 LLM 手動生成用（非確定性腳本），另登記一列「待驗證」追蹤下次實跑輸出是否確實照規格產出。
- ✅ **每日自動化分裂架構首跑驗證**（2026-07-15 提前收斂於 2026-07-14）：雲端 routine `daily-news-pipeline-cloud`（trig_01AWf2wwmVeL3ykPCSyxyvzw）於 2026-07-14 13:00 UTC 首次真正排程執行，成功產出完整 4 筆 commit（`news: daily digest 2026-07-14` → `wiki: auto-ingest 2026-07-14`（分兩次 commit，因執行中 Stop hook 觸發中繼 commit）→ `web: rebuild 2026-07-14`）並統一 push（`1b90dfb..8b6793e`）。分裂架構（① GH Actions 抓料 + ② 雲端 routine 產 news/wiki/web）確認穩定跑通，根因修復生效。
