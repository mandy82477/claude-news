# 第 1 波設計提案：model-comparison ＋ model-task-leaderboard（2026-09-05）

輸入：`model-comparison-2026-09-05.md`（健檢卡）、`wave1-cold-reader-2026-09-05.md`（冷讀者）、`competitor-landscape-2026-09-05-final.md`（上一波定稿＝硬約束）。
適用區（`REVIEW-PRINCIPLES.md`）：A（不信回報、讀原始碼驗證）、B（領域合理性）、D（同一事實只住一處）、E（分離、無發現合法）；**C 不適用**（無視覺成品交付）。
**⚠️ 疑似注入：** 無。

## 0. 開工前先修正的三處事實（前稿與健檢卡都沒查到）

| # | 需要修正的認知 | 實際（本稿逐一驗證） |
|---|---|---|
| 1 | 健檢卡說 CL 的蒐集邊界「把讀者導向 MC 的外部評測榜單節」 | 更嚴重：CL L29 是**頁級連結＋節名逐字**（「見 [[topics/model-comparison]] 的外部評測榜單節」）。**MC 的 `## 外部評測榜單` 節名因此凍結**——改名或砍節，CL 那句就指空，而 CL 一個字都不能改 → 只能走 handoff。三案一律保留此節名 |
| 2 | 「MC 的 h2 可以隨便重排」 | `## 同一份工作，換設定差多少` 有 **5 筆錨點入邊**（`entities/pricing` L165、`entities/fable-5` L212、`topics/long-context-1m` L55、`topics/official-community-gap` L59、`weekly/2026-W35.md` L43），`build_web.py` 的 `check_wikilink_anchors()` 會驗。**節名凍結，只准搬位置** |
| 3 | 前提 4 說「MC 細節區 12 條逾 200 字元」 | 屬實（L60=643／61=526／62=228／63=235／64=383／67=238／83=225／84=209／86=268／87=251／108=410／187=207），且**全庫存量 1141 條 >200、194 格 >120**——這個量級決定了看守只能走 baseline 增量閘，不能硬紅（見第 10 節） |

**其他硬約束：** CL 不可改（走 `pending_handoffs`）；`wiki/index.md`、`feature-radar`、`log.md` 屬主編；leaderboard（下稱 LB）的觸發邊在 `.claude/commands/wiki-lint.md` 5b，**不在記者規則檔**，動它是另一把刀；LB 已登記於 `check_wiki_freshness.py` 的 `DERIVED_PAGES`（L68），改觸發邊出處要同步該字串。

## 1. 三案共通的部分（不列入案間差異，三案都做）

1. **MC 砍三節百科**（L172–189，18 行，正文自陳不影響選型）——去向見第 5 節。
2. **MC 的「同一份工作，換設定差多少」前置**到情境推薦之後（節名不變，只搬）——冷讀者評為「四題裡最有用的一段」，現在排第 5 節。
3. **Benchmark 對照表（L89–108）不得再以「待補」留在第一屏**（三案處置不同，見各案）。
4. **LB 摘要補一句終點宣告**、**index 需主編加一條開發實務入口路由**（三案都需要，走「需主編」項目）。
5. **兩條字元上限補 baseline 增量閘**（第 10 節）。
6. **MC 的 effort callout（L56）下移**到情境推薦表之後——冷讀者原句：「它出現在我剛拿到答案的正下方，第一次讀會以為前面的建議被推翻」。

## 2. 案 A｜終點升格在 leaderboard（LB 變成「跨家＋跨工具」的答案頁）

**一句話：** MC 保持家內純淨，把「誰強」整包（含工具／harness 層）交給 LB，並給 LB 補上它現在缺的那一層。

### LB 骨架（改後）

| # | 節 | 使命一句 | 型態 |
|---|---|---|---|
| 0 | 標頭＋**新增 `**蒐集邊界：**`** | 誠實說「哪些榜抓得到、哪些抓不到」 | 覆寫 |
| 1 | `> 本週快照重點` | 這禮拜哪幾榜換人 | 覆寫 |
| 2 | `## 摘要`（改） | **跨家與跨工具的「誰強」到這頁為止；家內選型見 MC** | 覆寫 |
| 3 | **`## 我在比的是哪兩個東西`（新）** | 讀者問的常是工具（Codex CLI）不是模型——先把工具換算成模型 | 覆寫，≤8 列 |
| 4 | `## 本週快照` | 模型層 18 榜的本週領先者 | 覆寫（5b） |
| 5 | `## 本週註記` | 只列異常 | 覆寫 |
| 6 | `## 評比方式索引` | 這個數字是怎麼算出來的 | 靜態參考層 |
| 7 | `## 相關頁面`（原「相關實體」改名） | 三頁分工一句話 | 覆寫 |

### 新表：`## 我在比的是哪兩個東西`

**判準句（可貼進 5b）：** 一個**工具（harness）**一列，只收「讀者真的會拿來替換 Claude Code 的東西」；本表**不放分數**（分數在下方快照表），只回答三件事：它跑什麼模型、哪個榜量得到它、量到的是工具還是模型。**「它跑什麼模型」查不到就標懸置，不得推測**——這一欄整欄空白也比猜一個模型名誠實。上限 8 列，90 天無新資訊者移出。

| 工具（harness） | 它跑什麼模型 | 哪個榜量得到它 | 榜量的是工具還是模型 |
|---|---|---|---|
| Claude Code | Sonnet 5 預設，可切 Opus 5／Fable 5.1（見 [[topics/model-comparison]]） | [Terminal-Bench 3.0](#eval-terminal)、[SWE-bench](#eval-swebench) | 工具＋模型合測（換 harness 分數就變） |
| OpenAI Codex CLI | ❓ **待查證**（標 2026-09-05｜查 Codex CLI、GPT-5.6 Sol、harness） | [Terminal-Bench 3.0](#eval-terminal) 有 GPT-5.6 Sol 列，是否即 Codex CLI 未證實 | 同上 |
| OpenCode | ❓ **待查證**（標 2026-09-05｜查 OpenCode、Composio、harness） | 無榜；成本實測見 [[topics/competitor-landscape]]「硬答案」 | — |
| Pi coding agent | Opus 4.8 high（Databricks 實測條件，2026-08-13 查證） | 無榜；同上 | — |

> 示範說明（不進頁面）：四列有兩列的核心欄是 ❓——**這正是誠實的樣子**。庫內對「Codex CLI 跑什麼模型」零記載（已 grep 全 `wiki/`，只有安全事件與 AGENTS.md 提及 Codex），冷讀者的原話也是「我不知道 Codex CLI 現在跑的是哪個模型，表上也沒說」。填一個猜的模型名會直接製造假結論。

### MC 在 A 案的處置

- Benchmark 對照表（L89–108）**整表砍**，換一行歷史註記：「07-09 世代的橫向對照已隨陣容換代失效，跨家與同口徑排名見 [[topics/model-task-leaderboard]]」——與 CL 定稿「不寫跨家跑分數字、改指活榜單」同一把尺。
- `## 社群實測觀察` **整節砍**：跨家 2 列（L116 Fable 5 vs GPT-5.6 Sol、L120 MineBench）走 handoff 給 5b／CL；家內 4 列的結論已在情境推薦「生產環境求穩」列，併入該列細節一句。
- `## 外部評測榜單` **節名保留、位置上移**到情境推薦之後，內容瘦成一句＋原任務→榜對照表（該表是 CL 蒐集邊界指名的東西）。

**代價：** 5b 每週的 Haiku 抓取任務要多一張表的維護，而那張表的核心欄（工具跑什麼模型）**外部榜單頁不一定寫**——會長期掛 ❓。LB 仍是冷讀者眼中的「百科」，只是多了一層換算。

## 3. 案 B｜終點收回 MC（LB 降為證據層）— **本稿推薦**

**一句話：** 讀者已經在 MC（全站唯一被冷讀者認證的雷達），就在 MC 給「別家 vs Claude 現在的答案」，但**只抄結論不搬數字**；LB 改口宣告自己是「原始排名與算分方式」的證據層。

### MC 骨架（改後，節序即讀序）

| # | 節 | 使命一句 | 型態 |
|---|---|---|---|
| 0 | 標頭＋callout | 這週陣容變了什麼 | 覆寫 |
| 1 | `## 摘要`（改末句） | 現行陣容一行＋**跨家答案在下面第 4 節**，不再是勸退句 | 覆寫 |
| 2 | `## 快速選型表` | 我這份工作用哪個 Claude 模型 | 覆寫，4 欄 5 列 |
| 3 | `## 情境推薦`（effort callout 移到此節之後） | 常見情境的單一建議 | 覆寫，一情境一列 |
| 4 | **`## 別家 vs Claude：現在的答案`（新）** | 我該不該換去別家／別的工具 | 覆寫，≤5 列 |
| 5 | `## 同一份工作，換設定差多少`（節名凍結，位置上移） | 換了實付差多少 | 覆寫＋兩張小表 |
| 6 | `## 外部評測榜單`（節名凍結） | 想自己看榜去哪看 | 覆寫 |
| 7 | `## 家內同口徑對照（2026-07-09，不回訪）` | 上代陣容的凍結證據 | **凍結區，到期 2027-01-05** |
| 8 | `## 相關實體`／`## 時序` | — | 累積 |

### 新表：`## 別家 vs Claude：現在的答案`

**判準句（可貼進 `.claude/rules/wiki-ingest-models.md` 新增 J 條）：**

> 一個對手一列，只收「讀者真的會拿它替換 Claude 模型或 Claude Code」的具名產品。**本表只抄結論、不產生結論**——每列的事實必須已經住在 [[topics/model-task-leaderboard]] 或 [[topics/competitor-landscape]]，本表寫的是那個結論的一句話版＋去哪看最新；**本表不寫任何分數數字**（數字留在它的家，這裡寫了就是第二個家）。
> 「現在的答案」寫不出一句可讀的結論 → 不進表。**「查證日」超過 90 天者，結論句後加「（YYYY-MM-DD 查證，此後未回訪）」；超過 180 天整列移除**（與 CL `## 硬答案` 同一把尺）。上限 5 列。

| 對手 | 現在的答案 | 這個答案有多硬 | 看最新去哪 |
|---|---|---|---|
| **OpenAI Codex CLI** | 沒有單一答案——不同基準各有勝負，選型看任務類型不看單一分數（2026-08-13 查證，此後未回訪） | 第三方彙整＋官方基準，已 3 週未回訪 | [[topics/model-task-leaderboard]] 的電腦操作／寫 code 兩列 |
| **OpenCode** | 便宜但慢：每任務約 Claude Code 的 1/2.7 價，Claude Code 是最快的（2026-08-13 查證） | 單一第三方 30 項任務實測（Composio） | [[topics/competitor-landscape]]「硬答案」 |
| **GLM 5.2＋Pi harness** | 省約 34%，而**差異多來自 harness 不是模型**——換 harness 比換模型影響大（2026-08-13 查證） | 單一實測（Databricks） | [[topics/competitor-landscape]]「硬答案」 |
| **GPT-5.6 Sol** | 電腦操作榜互有勝負且**版次結果相反**（TB 3.0 與 2.1 版排名相反），不可跨版比（2026-08-28） | 媒體轉述，版次歧異已載於 LB 本週註記 | [[topics/model-task-leaderboard]] 電腦操作 agent 列 |
| **Gemini CLI** | 已無免費層——2026-06-18 起併入 Antigravity CLI，個人開發者需轉付費 API key（2026-08-13 查證） | 官方頁查證 | [[topics/competitor-landscape]]「硬答案」 |

> 示範說明（不進頁面）：五列全部是**庫內既有的原句**（CL `## 硬答案` L75–79、LB 快照表 L58 與本週註記 L73），一個新數字都沒有——這是本表「只抄不產」紀律的示範。三列帶「2026-08-13 查證」，離 90 天門檻還有約 60 天；到期時是加註記不是刪，符合「不虛報優先於行動導向」。第四列刻意不寫 42.7%——數字留在 LB。

### LB 在 B 案的處置

- 摘要改寫：「這頁是**各活榜單的原始排名與算分方式**。想知道『所以我該用哪個』，見 [[topics/model-comparison]]——那頁的『別家 vs Claude』一節已經把本頁的排名換算成建議。」
- 新增 `**蒐集邊界：**`（同 A 案）。
- **不新增表、不改 5b 的抓取清單與 4 欄形狀**——B 案對 5b 零改動，這是它與 A 案最大的成本差。
- `## 本週註記` 增設一條**常設**「工具層對照」註記（非表）：一行寫「本頁量的是模型；Codex CLI／OpenCode 等工具跑什麼模型未見公開資料 ❓ 待查證（標 2026-09-05｜查 Codex CLI、harness、GPT-5.6 Sol）」。

### Benchmark 表在 B 案的處置：凍結區（借鑑 CL）

改名為 `## 家內同口徑對照（2026-07-09，不回訪）`，**只減不增**，節內首行一句「本節是 2026-07-09 那天的同口徑數字，之後沒有再更新；現行陣容的選型見上方快速選型表」。**到期日 2027-01-05（建立日 +180 天）**，屆時由 lint 移除整節。理由：它是庫內唯一的家內同口徑橫向表，直接砍掉會讓「Fable 5.0 vs Opus 4.8 誰強」在庫內零記載；而 CL 已證明凍結區＋到期日是可行的處置。

**代價（B 案最大風險，必須配套）：** MC 的維護者是**模型記者，無 web 工具**，而新表的料來自 CL（商業記者）與 5b（主編）。所以 J 條必須寫死「只抄不產」＋「料的來源頁有更新時由對方走 handoff 通知」，否則這張表會在 3 個月內變成第二個過期凍結區。

## 4. 案 C｜只修橋接與過期，不新增任何表

**一句話：** 不搬答案、不建新表，只把「出口位置」與「過期內容」處理掉，讀者拿到的是「去哪看＋為什麼這裡沒有」。

- MC：砍三節百科、Benchmark 整表→一行歷史註記、`## 社群實測觀察` 整節砍（同 A）、`## 外部評測榜單` 上移到情境推薦之後、摘要末句從勸退句改成出口句。
- LB：摘要補終點宣告、`## 本週註記` 加常設工具層註記（同 B 的最後一項）、新增蒐集邊界。**不新增節、不改 5b。**
- 規則只改 models E 條（待補時限）與 F 條（加第 3 題）；A/B 要改的 C 條、J 條、5b 全部不動。
- **代價：** 冷讀者的 Q1 仍拿不到「現在的答案」，只會從被踢 2 次變成被踢 1 次且知道為什麼。誠實但不行動導向——與總則「行動導向仍要有」只達一半。
- **優點：** 零新增維護負擔、零跨維護者依賴、改動面最小，是三案中最不可能退化的。

## 5. 現有內容逐行號去向（一行都不憑空消失）

### model-comparison（220 行）

| 現址 | 內容 | A | B | C |
|---|---|---|---|---|
| 1–24 | frontmatter | 機器生成，不動 | 同 | 同 |
| 25–38 | H1／標頭／callout | 留 | 留 | 留 |
| 40–42 | 摘要 | 末句改出口句 | 末句改「跨家答案見第 4 節」 | 末句改出口句 |
| 44–55 | 快速選型表＋不進表註 | 留（不動） | 留 | 留 |
| 56 | effort callout | **下移**到情境推薦表之後 | 同 | 同 |
| 58–67 | 選型細節 8 條 | 留；L60/61/62/63/64/67 六條逾 200 字元→切短，背景移至各 entities 頁 | 同 | 同 |
| 69–79 | 情境推薦表 | 留 | 留 | 留 |
| 80–87 | 情境推薦細節 | 留；L83/84/86/87 切短；「生產環境求穩」列吸收社群實測家內結論一句 | 同 | 同 |
| 89–93 | Benchmark 節頭＋範圍 callout | **砍**，換一行歷史註記＋指 LB | 改為凍結區節頭（含「不回訪」與到期日） | 同 A |
| 95–100 | Benchmark 表 4 列 | **砍**（已無同口徑對照對象） | **原地凍結**（不新增、不回訪） | **砍** |
| 102–108 | 表格細節 5 條 | L108（410 字元，MarkTechPost 三模型對照）**移交 [[entities/sonnet-5]]**（handoff→模型記者自己可寫）；其餘 4 條隨表砍 | 全數留在凍結區，L108 切短至 ≤200 | 同 A |
| 110–113 | 社群實測節頭＋警語 | **節刪** | **節刪** | **節刪** |
| 114–121 | 社群實測表 6 列 | L116／L120 跨家 2 列→handoff 給主編（判是否入 LB 註記或 CL 硬度欄）；L117／L118／L121 家內 3 列→結論併入情境推薦「生產環境求穩」細節一句；L119（effort 非單調，已由官方證偽）→併入 L122 Effort dial 細節 | 同 A，惟 L116 另評估是否成為新表「GPT-5.6 Sol」列的來源 | 同 A |
| 122–125 | Effort dial 細節＋小結 | Effort dial 細節**併入下移後的 effort callout 下方**；小結（L125）砍——內容已在被砍的表裡 | 同 | 同 |
| 127–129 | 「同一份工作」節頭 | **上移**至情境推薦之後，節名逐字不動 | 同 | 同 |
| 131–138 | token 成本兩說 | 留（prepend 表，補上限 4 列判準） | 同 | 同 |
| 140–158 | 換代實付成本＋表＋兩結論 | 留（表補「Opus 4.6 為換代前基準線、非可選項」標記） | 同 | 同 |
| 160–168 | 快取費率大降／媒體不一致／機制解釋／使用限制 | 留 | 同 | 同 |
| 170 | 「選了不一定算數」callout | 留 | 同 | 同 |
| 172–176 | 跨模型跨語言行為研究 | **handoff→安全政策記者**評估落點（對齊研究，庫內無專頁）；對方回不適用則降為 `## 相關實體` 一行連結 | 同 | 同 |
| 178–182 | 安全分類器評測（含**逾期懸置**） | **handoff→功能記者**寫入 [[entities/claude-code]] Auto 模式；**懸置標記整筆隨段搬，不得刪**（標 2026-08-15，已逾複查日） | 同 | 同 |
| 184–189 | 36氪單一來源聲明（含懸置，複 2026-09-07） | **移至 `## 時序` 一行**＋懸置標記隨行保留（未證實的能力傳聞，時序是它的家）；L187 逾 200 字元→切短 | 同 | 同 |
| 191–204 | 外部評測榜單（節名凍結） | **上移**至情境推薦之後；正文瘦成一句，任務→榜對照表保留 6 列 | 上移；**表保留**（CL 蒐集邊界指名），正文改成「本表給想自己看榜的人；別家 vs Claude 的結論見上一節」 | 同 A |
| 206–210 | 相關實體 | 留（A 案另收行為研究一行） | 補三頁分工三句 | 留 |
| 212–220 | 時序（陣容變化） | 留 | 留（另收 36氪一行） | 留 |

### model-task-leaderboard（195 行）

| 現址 | 內容 | A | B | C |
|---|---|---|---|---|
| 1–25 | frontmatter | 機器生成 | 同 | 同 |
| 26–33 | H1＋標頭 | **新增 `**蒐集邊界：**` 一行**（在「領域」之後） | 同 | 同 |
| 35–36 | callout | 留 | 留 | 留 |
| 40–42 | 摘要 | 改：宣告「跨家與跨工具到此為止」＋家內指 MC | 改：宣告「本頁是排名與算分的證據層」＋建議指 MC | 同 A |
| —（新節） | — | **新增 `## 我在比的是哪兩個東西`**（置於第 2 節） | 不新增 | 不新增 |
| 44–65 | 本週快照 18 列 | 留（5b 覆寫）；L49 Aider 2025-08-23 停更列依 5b 既有規則處置 | 同 | 同 |
| 67–77 | 本週註記 | 留 | 留＋**新增常設「工具層對照」一條**（含懸置標記） | 同 B |
| 79–189 | 評比方式索引 111 行 | 留（全站最好的解釋層，冷讀者未抱怨）；L69／L74 的「尚未定案」等內部語言改讀者語言或移進 `%% %%` | 同 | 同 |
| 191–195 | 相關實體 | 改名 `## 相關頁面`＋三頁分工三句 | 同 | 同 |

## 6. 鄰居分工（可直接貼上的三對句子，三案共用）

寫在 MC `## 相關實體`／LB `## 相關頁面`；CL 那側**已經有對應句**（CL L200–201），不需要改 CL：

| 對 | 貼在哪 | 逐字 |
|---|---|---|
| MC ↔ LB | MC 相關實體第 2 行；LB 相關頁面第 1 行 | 「[[topics/model-task-leaderboard]] — 各活榜單的原始排名與算分方式；本頁只把它換算成『你該用哪個』。」／「[[topics/model-comparison]] — 決定『我這份工作該用哪個模型、實付差多少』；本頁只給排名與算分方式。」 |
| MC ↔ CL | MC 相關實體第 3 行（CL 那側 L200 已有，不動） | 「[[topics/competitor-landscape]] — 外面出現了什麼、對用 Claude 的我有多大衝擊；本頁只管『所以我該用哪個』。」 |
| LB ↔ CL | LB 相關頁面第 3 行（CL 那側 L201 已有，不動） | 「[[topics/competitor-landscape]] — 這些排名對選型代表什麼、廠商動態如何；本頁只給榜上的數字。」 |

**唯一落點原則（三頁各留一句、不留副本）：** 跨家與跨工具的**排名數字**＝LB；**「所以我該用哪個／該不該換」的結論**＝MC（B 案）或 LB（A 案）；**廠商動作與衝擊**＝CL。

## 7. 維護者同步（可貼上的規則條文）

### `.claude/rules/wiki-ingest-models.md`（模型記者）

- **C 條**（社群效能討論一律留數字）補一段（A/B/C 皆需）：
  > **跨家數字不寫進本頁** `[加入: 2026-09-05]`：社群或媒體出現 Claude vs 非 Anthropic 模型／工具的數字時，本頁不記——在回報「同步自查」欄寫「⚠️ 需主編轉知：跨家數字 [議題]，建議落點 [[topics/model-task-leaderboard]]（`/wiki-lint` 5b）或 [[topics/competitor-landscape]] 雷達硬度欄」。理由：同一個數字在兩頁就有兩個家，而本頁沒有回訪跨家數字的機制。
- **E 條**（陣容換代同步骨架）補一句（A/B/C 皆需）：
  > 標「待補」的對照表，**自標記日起 60 天未補齊同口徑數據即整表退場**——A/C 案退為一行歷史註記並指向活榜單；B 案退為凍結區（節名加「（YYYY-MM-DD，不回訪）」並設建立日 +180 天的到期日）。「待補」不是可以無限期停留的狀態。
- **F 條**（3 跳自檢）加第 3 題（A/B/C 皆需，合格條件依案不同）：
  > 3. 「Codex 和 Claude 誰強」——**A/C 案合格條件**：3 跳內離開本頁並落在 [[topics/model-task-leaderboard]] 的具體列或工具層表（驗轉出，不驗作答）。**B 案合格條件**：3 跳內在本頁 `## 別家 vs Claude：現在的答案` 拿到單一結論句＋一個「看最新去哪」的連結。
- **G 條**自查指令旁補一句（A/B/C 皆需）：
  > 儲存格量測只量表格；**表格下方細節區的條列上限 200 字元**由 `python scripts/check_cell_limits.py --page topics/model-comparison` 量（見第 10 節）。
- **I 條**補一句（A/B/C 皆需）：換代前的基準線列至多 1 列，且儲存格內須標「非可選項」。
- **新增 J 條（僅 B 案）**：第 3 節的判準句整段照貼，標題 `## J. 「別家 vs Claude」表：只抄不產 [加入: 2026-09-05]`。

### `.claude/commands/wiki-lint.md` 5b（主編，另一把刀）

- **A 案**：5b 第 2 點的「表格形狀固定 4 欄」需改寫為「快照表固定 4 欄；另維護 `## 我在比的是哪兩個東西` 表（4 欄，判準見本步驟）」，第 1 點的抓取 prompt 需加「順帶回報各 harness 官方文件所載的預設模型，查不到明說」。
- **B 案**：5b 只在第 2 點末補一句「`## 本週註記` 的常設『工具層對照』條在懸置解除前不得移除」。
- **C 案**：5b **零改動**。

### 需主編（記者無權）

1. `wiki/index.md` 開發實務入口表（L25–34）加一列：`| 別家（Codex／OpenCode／GLM）跟 Claude 比，現在誰強 | [[topics/model-comparison]]「別家 vs Claude」（B 案）／[[topics/model-task-leaderboard]]（A/C 案） |`
2. `wiki/index.md` Topics 表 LB 那列摘要格改寫（現為「18 類任務的活榜單每週速讀」，沒說它是誰的終點）。
3. `wiki/reader-notes.md` L43 的 ⏳（三頁互踢）於實作後結案。

## 8. 交辦帳本（跨維護者，一律走指令不直接改他人頁）

```
python scripts/pending_handoffs.py open --from 模型 --to 功能 --page entities/claude-code \
  --note "接收 model-comparison L178-182 Auto 模式分類器評測（89%/13.6% 兩組數字並陳）＋其逾期懸置標記（標 2026-08-15），標記整筆隨段搬不得刪"
python scripts/pending_handoffs.py open --from 模型 --to 安全政策 --page topics/ai-agent-safety \
  --note "評估 model-comparison L172-176 官方跨模型/跨語言價值觀研究（2026-07-15，HN 32）落點；不適用請回報，本頁將降為一行連結"
python scripts/pending_handoffs.py open --from 模型 --to 商業 --page topics/competitor-landscape \
  --note "MC 社群實測 L116（Fable 5 vs GPT-5.6 Sol 單一使用者）／L120（MineBench 已查證不可採信）跨家 2 列移出 MC，請判是否入雷達表硬度欄；CL 本體不由模型記者改"
```

**CL 蒐集邊界（L29）不需 handoff**——三案都保留 `## 外部評測榜單` 節名，那句話仍指得到。**若使用者裁決要改該節名，必須先開一筆 handoff 給商業記者改 CL L29，且兩者同一 commit。**

## 9. 機械契約（先 grep 再宣稱）

已跑 `grep -rn "<字串>" scripts/ web_reader/assets/ src/tests/ .claude/review-registry.json`：

| 字串 | script 消費？ | 處置 |
|---|---|---|
| `## 同一份工作，換設定差多少` | 只在 `src/tests/test_weekly_deepdive_len.py` L42 當**樣本字串**（測 `visible()` 長度，不驗錨點存在）；真正的看守是 `build_web.py` 的 `check_wikilink_anchors()`＋**5 筆錨點入邊** | **節名凍結**；登記進 `.claude/review-registry.json` 的 `anchors`（現無此筆），讓 `check_rules.py` 檢查 3 也看得到 |
| `## 外部評測榜單` | 零命中（無 script）；但 **CL L29 逐字引用節名** | **節名凍結**；登記 `anchors`（規則端 `.claude/rules/wiki-ingest-commercial.md` 未逐字提及此節名，故不宜登 `sync_pairs`） |
| `## 快速選型表` | 零命中；`wiki/CLAUDE.md` L67 用它當語法範例 | 不改名（三案都沒要改） |
| `## 本週快照`／`## 評比方式索引`／「固定四欄位」 | 零命中於 `scripts/`，只在 `.claude/commands/wiki-lint.md` 5b 有明文 | **A 案**改欄位形狀時，把 5b 的「4 欄」與頁面表頭登記為 `sync_pairs`（現無看守，改了不會紅）；B/C 案不動 |
| `topics/model-task-leaderboard` | `scripts/check_wiki_freshness.py` L68 `DERIVED_PAGES` 的 key，value 字串寫著觸發邊出處 | A 案若改動觸發邊描述，需同步該 value |
| 新增的 `## 別家 vs Claude：現在的答案`（B 案） | 尚無消費端 | **不登 registry**——沒有 script 消費的判準塞進 registry 是零看守力（試點教訓）。它的看守是 J 條＋F 條第 3 題 |

## 10. 上限看守的補法（前提 4）

**現況（已讀 `scripts/` 原始碼確認）：** 120／200 兩條上限**零腳本看守**；`inquiry_bank.py` L87 只是叫人手動跑一次量測，不是閘。實測存量：**全庫 194 格 >120、1141 條 >200**——硬紅會讓人第一天就把檢查關掉。

**提案：新增 `scripts/check_cell_limits.py`，照 `check_reader_language.py` 的 baseline 增量閘複製結構**（該腳本 2026-09-05 剛上線，模式已驗證）：

- 掃描範圍同 `check_reader_language.py`（entities／topics／feature-radar／overview／index 的正文；frontmatter、code fence、`%% %%`、HTML 註解跳過）。**額外排除 `*-archive.md`**——蒸餾契約明定 archive 是「原文一字不刪」照搬，對它設字元上限會與該契約直接衝突。
- 量測前剝掉 markdown 連結 URL（`\]\([^)]*\)` → `]`），與現行兩條規則的自查指令同口徑；**純引用來源格豁免**（格內僅「標題＋連結＋日期」者），照 G 條既有豁免。
- `data/cell-limit-baseline.json` 記「頁 → 命中指紋」，**只對基線外的新增 FAIL**，基線內印 WARN 摘要；清乾淨一頁就把該頁移出基線（棘輪只往下轉）。
- 掛進 `scripts/run_tests.py`（與 `check_reader_language.py` 同一段），並在 `.claude/rules/wiki-ingest-format.md`「表格放結論，細節下沉」與 `.claude/rules/wiki-reporter-shared.md`「書寫風格」兩處各補一句「本條由 `scripts/check_cell_limits.py` 看守」。
- **驗紅（REVIEW-PRINCIPLES 第 2 條）：** 實作時必須親手在一頁塞一條 250 字元條列，確認 exit code 非 0；只讀原始碼不算驗過。
- 本波實作順手清掉 MC 的 12 條與 LB 的 1 條（L70，203 字元），清完直接不進基線。

## 11. 拆頁三題（三案皆判定：不拆）

對 `## 同一份工作，換設定差多少`（健檢卡列為唯一拆頁候選）：

| 題 | 判定 | 證據 |
|---|---|---|
| 有自己的問題 | ✅ | 「換代帳單為什麼漲」讀者會單獨問（`weekly/2026-W35.md` 深挖過） |
| 有自己的結論與時序 | ❌ | 節內無時序、無自己的 callout；結論全部依附快速選型表的牌價 |
| 被獨立引用 | ⚠️ | 5 筆錨點入邊——**數量夠但語意不對**：`pricing`／`long-context-1m`／`official-community-gap` 引的都是「MC 的換算那一段」，屬 part-of（搬回母頁很自然），不是獨立故事 |

**三題不齊 → 不拆**，該做的是蒸餾（本波已在做：切短細節區、砍百科節）。B 案的新表也**不拆**：≤5 列、只抄不產，沒有自己的時序。

## 12. 風險：這案在什麼情況下退化回舊形狀

| 案 | 退化路徑 | 煞車 |
|---|---|---|
| A | 5b 是 Haiku 抓取任務，工具層表的核心欄長期是 ❓ → 三個月後被當成「沒用的表」默默不更新，LB 回到純模型層，兩條箭頭再度指向接不住的終點 | 5b 第 2 點加一句「工具層表連續 4 週全 ❓ → 回報使用者評估撤表」；不讓它靜靜爛掉 |
| B | MC 記者無 web、料靠他人 handoff；若 CL／5b 那側忘了通知，新表會在 90 天後變成第二個「查證快照」凍結區——**這正是本波在修的病** | (1) J 條「只抄不產」＋每列必帶查證日；(2) 90 天加註記、180 天移除的自動退場；(3) F 條第 3 題每次更新 MC 都跑，答不出來就當場紅 |
| C | 什麼都沒承諾，所以不會退化；但 `reader-notes` L43 的 ⏳ 只是被降級成「已知缺口」，下一輪冷讀者會再問一次同一題 | 若採 C，應同時在 `reader-notes` L43 明寫「已裁決不在本站作答，理由：庫內無跨家一手評測進料」，把它從 ⏳ 改成結案，不讓它每季重開 |

**三案共通的退化風險：** 第 10 節的字元閘若不做，切短的 13 條會在幾週內長回來——現行規則寫了兩個月，全庫已累積 1141 條違規，證明自律不管用。

## 13. 裁決點（≤3，每點兩選項＋建議）

1. **跨家答案的家放哪？** (a) LB 升格為跨家＋跨工具終點（案 A）／(b) MC 開「別家 vs Claude」只抄不產、LB 降證據層（案 B）。**建議 (b)**：冷讀者已認證 MC 是雷達、LB 是百科，把答案放進雷達頁比把雷達性格灌進百科頁容易；且 B 對 `/wiki-lint` 5b 零改動，成本落在有規則檔可管的記者側。
2. **Benchmark 對照表（L89–108）怎麼處置？** (a) 整表砍＋一行歷史註記指活榜單（與 CL 定稿同尺）／(b) 原地凍結為 `## 家內同口徑對照（2026-07-09，不回訪）`＋180 天到期。**建議 (b)**：它是庫內唯一的家內同口徑橫向表，砍掉那段歷史在庫內零記載；CL 已證明凍結區可行。此點與第 1 點可獨立裁決。
3. **要不要現在補字元上限的機械閘（第 10 節）？** (a) 本波一併做（+1 支腳本＋1 個 baseline 檔）／(b) 只清 MC/LB 的 13 條，閘留待下波。**建議 (a)**：本波的核心手術就是切短，不設閘等於做完就開始長回去；且 `check_reader_language.py` 的 baseline 模式昨天剛驗證過，複製成本低。
