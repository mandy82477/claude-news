# 評審：feature-radar ＋ entities/claude-code 設計提案（第 4 波 P2「該不該升版」）

2026-09-06｜評審 agent｜適用區 **A／B／D／E**，**C 不適用**（無視覺成品／現實體裁交付）
⚠️ 疑似注入：無（兩頁、鄰居頁、三份規則檔、設計逐字稿皆無指令式文字）
受評：`p2-2026-09-06-proposal.md`（147 行，V8 第二輪）＋ `-proposal-map.md`（86 行）＋ `scratchpad/p2-draft.md`
**本評審已納入 `p2-2026-09-06-verified.md` 第六節**（完整 CHANGELOG 6,358 行，17 條預設值改變）——該節在設計者寫階梯表之後才產出，🔴-5／🔴-6 兩條由它而生。

## 0. 我親自重驗了什麼（A-1／A-2）

| 提案宣稱 | 我的驗證 | 結果 |
|---|---|---|
| `check_feature_radar.py` 只對帳當月條目數、不讀熱度欄 | 讀原始碼：`parse()` L45–79 不取熱度、`main()` L95–109 只比數量、L14–15 舊月豁免 | ✅ 屬實 |
| `## 版本更新` 是全站唯一被錨點指入的節 | `wiki_graph.py explain entities/claude-code --section "版本更新"` → 錨點 1（feature-radar ⚠️ 升版風險 :L27） | ✅ 屬實 |
| 要改名的六節錨點邊為 0 | 逐節查 `升版風險／本週推薦／功能全覽表／現況／核心功能／歷史記錄`，全回「（無）」 | ✅ 屬實 |
| 兩支閘的常數位置 | `check_cell_limits.py` L50／L51／L54、`check_reader_language.py` L134／L135 | ✅ 逐行對上 |
| 逾期懸置 claude-code 13 筆＋L215 語意反轉＋radar L763 | 實跑 `check_pending_markers.py` 逐行相符；讀 L159–162 確認 🔎 獨立成行後警告會消失 | ✅ 屬實 |
| 「一次性機械編輯約 42 行」 | 自算：`**發布：**` 標頭 39 行全含熱度與試用價值，推薦節 3 行 → 42 | ✅ 屬實 |
| 熱度不一致 8 處、另 20 條配不上 | 自寫配對重算：91 表列中可配對 25 對、**不一致 10 對**、**配不上 66 列** | ⚠️ 兩個數字都要改，見 🟡-1 |
| 「現在會咬到你的」七列每列都過三條判準 | 逐列開 `claude-code.md` 核對：七個 issue 皆 🔴、皆填得出平台或使用限定、皆有繞法或明寫無繞法 | ✅ 屬實（#13354 依判準 ① 退出，正確） |
| 逐行號去向表 | 抽驗 **6 段**親自開原頁：radar L11–19／L23–53／L219–220／L232–234／L445／L763；claude-code L509–523／L139／L193／L215／L586–599 | ✅ 全部對上 |
| 蒸餾門檻「07 月下月到期」 | 月份序 9−7=2 < 3，10 月才達標 | ✅ 算術正確 |

三支相關機械閘今日**全綠**（讀者語言存量 25 筆、字元上限存量 1172 筆，均未增加；radar 當月對帳 3=3）；草稿七列表格的儲存格與條列長度我複算過，零超限。

**第二輪已解掉的兩條**（不再列為缺陷）：「以上皆非 → 可直接升級」改為補限定詞而非搬結論，提案與去向表已一致；結論表 8→7 列且判準 ① 補上「GitHub 標 `enhancement` 者不入表」，示範列與判準終於自洽。

---

## 🔴 阻擋級（6）

### 🔴-1 v2.1.233 那一列是錯的——auto 預設化根本不綁版本

階梯表第 3 列寫「v2.1.233｜8/14 起 auto 成為 Pro／Max／Team 預設權限模式」。兩份證據都推翻它：`claude-code.md:411` 的 v2.1.233 是 **GitLab MR URL 支援**，verified 第六節從 changelog 逐字抓到的 v2.1.233 是 **Todo／Task 追蹤工具在 Opus 4.8／Sonnet 5／Fable 5 起被拿掉**。而 auto 預設化是 radar L459–460 自己寫的「2026-08-14 生效（08-07 公告）」**方案層變更**，任何 CLI 版本都擋不住。整張表的語意是「你若停在這一版之前」，塞一個與版本無關的事實進去，等於告訴 v2.1.220 讀者「這件事還沒發生在你身上」——它 8/14 就已經生效了。這是本波唯一會直接騙到目標讀者的一列（B-7 領域合理性優先於規格正確性）。

**修法**：該列改寫為真正的 v2.1.233（見 🔴-5 的新表第 8 列），auto 預設化移到表下、`**這張表不涵蓋什麼**` 之前，逐字：

```
**與版本無關、已經生效的一件**：8/14 起 auto 已是 Pro／Max／Team 的預設權限模式，不論你裝哪一版都已套用。靠手動確認做安全把關的人，到 `/permissions` 的 Auto 分頁看一次。
```

### 🔴-2「熱度只住全覽表」缺頁面範圍限定，會讓記者刪掉 entities 頁必填的熱度表

§7(a) 要寫進規則的逐字是「**熱度與試用價值只寫在 `## 📋 功能全覽表`**」。但 `.claude/rules/wiki-ingest-format.md` 的 entities 模板明訂「功能類型且熱度 ≥ 🔥🔥🔥 **必須含** `## 熱度與試用價值` 區塊」，`claude-code.md:78–88` 正是這張表。§7(f) 又補一句「熱度只有全覽表一個家，所以降格只改一處；跨頁同步仍指 entities 頁的熱度表」——**同一句話前後打架**。下週記者兩條讀下來只會二選一，錯的那一選會刪掉格式規則要求的表。

**修法**：§7(a) 首句改為 —— `**在 feature-radar 頁內，熱度與試用價值只寫在 ## 📋 功能全覽表**（詳細條目標頭與推薦節不再重複）。entities 頁的 ## 熱度與試用價值 表不受本條影響，它是跨頁常態值，同步規則見本檔「熱度降溫」。`；§7(f) 那句改為 `radar 內部只有全覽表一個家，降格改一處即可；entities 頁的熱度表照舊同步。`

### 🔴-3 改節名沒同步四個消費端，明天主編會把舊節重建回來

`本週推薦` → `這禮拜值得跟的三件`、`升版風險` → `從你現在的版本升上去，會遇到什麼`。提案 §7 只改 `.claude/rules/wiki-ingest-features.md`。我 grep 到四個沒被列進交付的消費端：

| 位置 | 現況 | 不改的後果 |
|---|---|---|
| `.claude/commands/wiki-ingest.md:120–121` | 「覆寫 `## ⭐ 本週推薦`」「更新 `## ⚠️ 升版風險`」 | 明天 ingest 主編找不到節 → **重建一個舊名的空節**，版面一天內磨回去 |
| `wiki/index.md:23` | 散文寫「[[feature-radar]]「⚠️ 升版風險」」 | 非 wikilink 錨點，`build_web.py` 的錨點致命閘**抓不到**，靜默指向不存在的節 |
| `wiki/overview.md:133` | 「完整功能熱度評分、升版風險與倒數中事件見 [[feature-radar]]」 | 同上，文案漂移 |
| `web_reader/assets/app.js:371` | CTA「看功能熱度雷達的升版風險與建議 →」 | 網站上唯一從日報通往本頁的入口，說的是不存在的節名 |

**修法**：以上四處與規則檔、頁面**同一 commit** 改完。commands 兩行節名直接替換；index L23 照 §7 已擬的表列一行；overview L133 改「完整功能熱度評分、**升上去會遇到什麼**與倒數中事件見 **[[feature-radar]]**」；app.js CTA 改「看熱度雷達：升上去會遇到什麼 →」，並把 `web_reader/index.html` 中 `app.js?v=` 換成當前 Unix timestamp（`.claude/rules/web-reader-design.md`「版本號更新」）。

### 🔴-4 轉知帳本的指令會直接報錯

提案 §5 表末列與 map C 表頭都寫 `python scripts/pending_handoffs.py open --from features --to community …`。`scripts/pending_handoffs.py:37` 的 `CATEGORIES = ("模型","功能","商業","安全政策","社群","人物")`，L75–76 對不在清單者 `raise SystemExit`。照抄會四筆全開不成，而實作者多半會以為「帳本壞了」而略過。

**修法**：兩處一律改成 `--from 功能 --to 社群 --page topics/code-quality-decline --note "…"`。

### 🔴-5 階梯表少了 §6 的必列版本，而漏掉的兩條正好打中本頁讀者

verified 第六節從完整 changelog 整理出 2.1.207–261 共 **17 條預設值改變**，設計者寫表時只有第一節那份殘缺原料。缺口不是「不夠完整」而已，有兩處是實質錯誤：

- **v2.1.232 只寫了一半**。提案寫「subagent forking 預設開啟、fork 繼承完整對話與 prompt cache」，漏掉 changelog 的另一半——**互動 session 裡非 teammate 的 agent 生成預設改成背景執行**。本頁設定的讀者身分就是「每天跑多 agent 流程」，通知路徑改變比 fork 繼承 context 更會咬到他。
- **v2.1.222 寫成純安全修復，「你要做的事」還寫著「這是升版的理由，不是風險」**。changelog 同版還**移除了 ultraplan**、且 Remote Control 不能再由 repo 設定開啟。對用 ultraplan 的人，那一列現在是反話。

**修法**：整張表換成下列 12 列（維持提案自訂的 12 列上限與版本降序），並在表下加一句 `其餘預設值改變（2.1.217–219 巢狀 subagent 深度、2.1.218、2.1.221、2.1.224、2.1.229、2.1.251）見[官方 changelog](https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md)。`：

```
| 你若停在這一版之前 | 升上去會遇到 | 型態 | 你要做的事 |
|---|---|---|---|
| v2.1.261 | `keybindingFlavor` 設定失效，鍵位一律改成 Bash 式 | 預設值改變 | 設過 readline 模式的人：那行設定已無作用，可刪 |
| v2.1.260 | subagent 背景指令的 1 小時上限移除 | 預設值改變 | 長跑 agent 不再被自動截斷，改用自己的逾時控制 |
| v2.1.257 | 預設 Fable 模型換成 5.1；auto 模式新增 Containment Escape 規則 | 預設值改變 | Pro／Team standard 的 Fable 走 usage credits，升完看一次帳單通道；雲端 CI 的 metadata／egress 動作不再自動核准 |
| v2.1.243 | 沙盒 Bash 提示不再列出允許網域；`CLAUDE_CODE_SUBAGENT_MODEL` 由「覆蓋全部」改為「預設值」 | 預設值改變 | 靠該變數強制全部子 agent 用同一模型的人，改在派工處指定 |
| v2.1.239 | 成本估算對「資料常駐工作區」計入 1.1 倍純美國推理附加費 | 預設值改變 | 不在資料常駐工作區的人不受影響；在的人 `/cost` 數字會變高，不是你多花了錢 |
| v2.1.234 | `/config` 移除「Default teammate model」，teammate 改用 leader 的模型 | 預設值改變 | 靠該設定讓 teammate 跑不同模型的流程要改寫 |
| v2.1.233 | Todo／Task 追蹤工具在 Opus 4.8、Sonnet 5、Fable 5 上被拿掉 | 預設值改變 | 依賴 TodoWrite 的 hook／skill：設 `CLAUDE_CODE_ENABLE_TODO_TOOLS=1` 找回 |
| v2.1.232 | Subagent forking 預設開啟（fork 繼承完整對話與 prompt cache）；互動 session 中非 teammate 的 agent 生成改為背景執行 | 預設值改變 | 需要乾淨 context 的委派改用非 fork 型別；靠前景輸出接結果的流程改看背景 session |
| v2.1.222 | 移除 ultraplan；Remote Control 不能再由 repo 設定開啟；修掉 worktree 隔離 session 可對主 checkout 下破壞性 git 指令的漏洞 | 破壞性變更＋安全修復 | 用 ultraplan 的人要換做法；靠 repo 設定開 Remote Control 的改用 user／managed 設定 |
| v2.1.215 | `/verify`／`/code-review` 不再自動觸發 | 破壞性變更 | 在 CI 或 hook 補上顯式呼叫 |
| v2.1.212 | `/fork` 改為複製對話進新背景 session，同 session 子代理改名 `/subtask`；Task 工具 `mode` 參數作廢 | 破壞性變更 | 依賴舊行為的 skill／hook 改寫為 `/subtask` |
| v2.1.207 | auto 模式不再讀 repo 內 `.claude/settings.local.json` 的 `autoMode`；plugin 設定只讀 user／managed | 預設值改變 | 靠 repo 設定開 auto 的人改用 user 或 managed 設定 |
```

v2.1.160（`workflow` → `ultracode`）降到表下的 catch-all 句，理由是它距今逾 3 個月且低於本頁鎖定讀者的版本；若要保留，請同時把 12 列上限改成 13 並寫下理由。

### 🔴-6 radar 現在正在推薦一個 v2.1.261 已經廢掉的設定

`keybindingFlavor readline 模式` 在 radar 有兩處：全覽表 **L155**（⚡ 有條件推薦）與 08 月詳細條目 **L347–356**（含 `{ "keybindingFlavor": "readline" }` 的快速上手範例）。verified 第六節：v2.1.261 起該設定**失效**，鍵位一律改 Bash 式。也就是說本頁一邊在階梯表告訴讀者「升到 261 沒問題」，一邊在 16 行外教他設一個升完就不會生效的設定。這比熱度不一致嚴重——那是數字打架，這是**照做會白做**。

**修法**：三處同改。(a) L155 表列功能名後加註 `（v2.1.261 起失效）`、試用價值欄改 `❌ 暫不推薦`；(b) L350 的「是什麼」句末加 `**2026-09-04 起失效**：v2.1.261 把鍵位一律改為 Bash 式，此設定不再有作用。`，L356 的程式碼範例整段刪除（留著等於還在教）；(c) 階梯表 v2.1.261 那一列（🔴-5 新表第 1 列）即為讀者端的對照。

---

## 🟡 應修（13）

**🟡-1 熱度數字要重算，兩個數都不對。** §5 末句與 §7(a) 條文寫「8 處不一致、另 20 條名稱不同無法配對」。我自寫配對重算：91 個表列中**可配對 25 對、不一致 10 對、配不上 66 列**（健檢卡另報 12）。三個數字都不假，只是配對法不同——這恰好是「別做名稱配對式看守」的證據，但不該把爭議數字寫成永久條文。**修法**：規則括號改為 `（2026-09-06 實測：名稱配得起來的 25 對中 10 對打架，另 66 列名稱不同到無法機械配對——所以看守單一家，不做配對比對）`；§5 末句同步。

**🟡-2 兩筆懸置結案／事實更正沒有走探針回掃。** `/wiki-lint` 5c 第 5 步與 `wiki-reporter-shared.md`「事實更正必回掃」都要求查實後拿探針字串回掃全庫。本案有兩筆——CQD L40（探針 `[[entities/claude-code]]、效能退步恢復、engineering missteps`）、claude-code L76 Apps Gateway 事實更正（「屬第三方非官方生態」）。提案兩處都沒寫回掃。**修法**：實作單加一步 `Grep "效能退步恢復|engineering missteps|屬第三方" wiki/`（排除 `log.md`），命中處逐一判——描述「現在」的改，描述「當時」的 append 一句「**後續（2026-09-06）：…**」；回報寫 `回掃：入邊 N 處（改 M）＋關鍵字 K 處（改 J）`。

**🟡-3 L215 拆分很可能踩到字元上限閘的新增線。** `data/cell-limit-baseline.json` 以 **page＋內容指紋** 為 key（`check_cell_limits.py` L151／L226），claude-code 該頁存量 296 筆。把 900+ 字元條目的 08-08／08-09 沿革**搬進 `## 歷史記錄` 表格**，會產生新內容的儲存格 → 全新指紋 → 判為新增超限、測試紅。**修法**：沿革不進歷史記錄表格，改寫進原條目正下方的 `%% … %%`（兩支閘都以 `INLINE_PCT_RE` 剝除 `%%`，已讀原始碼確認）；動完先跑 `python scripts/check_cell_limits.py` 確認「無新增超限」再收工。

**🟡-4 新 callout 超過硬上限。** `p2-draft.md` L6 含 `> ` 共 126 字元、本文 124，而 `wiki-reporter-shared.md` 的硬上限是 **120**。無腳本看守，但它是明文。**修法**：改為 `> **這禮拜動了什麼**（2026-09-06）` ／ `> 最新 v2.1.261（09-04），v2.1.238 起無 breaking change，但有預設值換了：Fable 預設模型改 5.1、`keybindingFlavor` 失效。`

**🟡-5 表下第三條界線與 §12 已被 §6 推翻。** 界線現寫「v2.1.242–244 與 v2.1.252–257 未見報導，本表只列本庫查得到使用者端異動的版本」；§6 說得很清楚：**2.1.242、249、250 官方根本沒發行**（跳號不是漏報），而 243／251／257／260 的內容現在都拿到了。留著這句會讓讀者以為表有洞，其實洞已經補上。**修法**：該條界線整條刪除，改為 `**本表的依據是官方 changelog 逐版核對（2026-09-06），不是本庫的報導覆蓋率**；2.1.213／230／242／249／250 是官方未發行的跳號，不是缺漏。`；§12「我最沒把握的一處」整節改寫為「靠每週 lint 逐版核對 changelog 維持，沒有腳本在數」——原本那個「覆蓋率有洞」的自白已不成立。

**🟡-6 §7(a)(b) 把教訓敘事寫進規則檔。** `.claude/rules/claude-md-edit.md` 明訂教訓敘事進 `docs/rules-changelog/`，條文只留判準＋「沿革檔 日期」指路，且 registry 有一條 sync_pair 在驗兩檔互相指認。**修法**：兩條的括號敘事移入 `docs/rules-changelog/wiki-ingest-features.md` 新增的 `## 2026-09-06` 段，規則檔句尾改「（教訓見沿革檔 2026-09-06）」。

**🟡-7 改 §7(c) 時不可刪掉三個 registry 看守字串。** `.claude/review-registry.json` 的 sync_pair「本週推薦選取邏輯必須保留時間閘」要求 `wiki-ingest-features.md` 同時含 `時間閘`、`不補舊條目湊數`、`本週無新達標功能`。§7(c) 要重寫該格式區塊並把「本週無新達標功能」那行搬進 `%%`——**搬的是頁面，不是規則檔**。**修法**：實作單明寫「三個字串在規則檔內原樣保留」，改完跑 `python scripts/check_rules.py` 確認綠。

**🟡-8 題庫探針寫死舊節名。** `scripts/inquiry_bank.py:66`（Q3 沉默質疑）：「feature-radar「本週推薦」最後輪替日 >14 天 → 依防霸榜規則覆核」。改名後主編照探針去查會查無此節。這不是加題／刪題（同一題、同一判準），屬維護性同步，不需使用者裁決。**修法**：該行節名改為「這禮拜值得跟的三件」；改完跑 `python scripts/run_tests.py`，確認 `test_inquiry_bank` 的題數自檢仍過。

**🟡-9 網站硬編摘要沒跟。** `scripts/build_web.py:454` 為 radar 硬編 `summary`「追蹤 Claude / Claude Code 每個新發布功能的社群熱度、試用價值與快速上手方式」——它是搜尋結果與卡片的文案，與新使命句已不同調。**修法**：改為 `這禮拜官方動了什麼、值不值得現在跟，以及跟上去會遇到什麼。`

**🟡-10 §7(e) 的 90 天退場線立刻自我違規，且指向不存在的頁。** 版本表現有 121 列，90 天線一寫下去當天就有上百列違規；`entities/claude-code-archive` 尚未建立，而頁級斷鏈只是 WARN（`build_web.py` L333 註明 never raises），會靜默掛著。「先立一版要廢的規則」在射程內。**修法**：條文加生效條款 —— `本線自 archive 子頁建立之日起生效（下一波蒸餾工程）；在此之前版本表只增不汰，不得據本條刪列。`

**🟡-11 裁決 3 的五行守單一家：順序與範圍要寫死，且必須驗紅。** 若先加檢查再做 42 行機械編輯，`run_tests.py` 會整段紅（存量 39 筆）；範圍若寫成全檔搜「熱度 🔥」，會掃到 `## 評分說明` 的圖例（L129、L134–138）。**修法**：(a) 同一 commit 內順序固定為「先刪 42 行副本 → 再加檢查」；(b) 範圍寫死兩條——僅檢查以 `**發布：**` 開頭的行是否含 `**熱度：**`，以及僅在 `## ⭐` 開頭的節內是否出現 `熱度 🔥`；(c) 依 A-2 改壞驗紅：手動在任一詳細條目標頭補回 `**熱度：** 🔥` → 應 FAIL 並指名該行，還原 → exit 0。**未驗紅不得宣稱有看守。**

**🟡-12 外洩清單四條的落點寫錯了。** #1 的 `coding-workflow-guide` 是**功能記者自己的頁**（`wiki-ingest-features.md` 負責頁面表），不是「他頁」；#9／#10 的 `skill-interest-watch` 是機器整頁覆寫的快照頁，手改頁面下次快照會被蓋掉，要改的是 `data/skill_interest_watch.json`（`wiki-ingest-community-lint.md` 明文）。「交主編」不是落點，是消失點。**修法**：#1 記進 `docs/page-audits/ledger.md` 待辦；#9／#10 走帳本 `--from 功能 --to 社群 --page topics/skill-interest-watch --note "外洩句住設定檔 data/skill_interest_watch.json，改設定不改頁"`；#6 index 那條併入 🔴-3 的同一 commit。

**🟡-13 §6 帶來的新規則缺口：階梯表的進料出口沒寫進條文。** §7(d) 已寫「新增列的依據只有官方 changelog，記者無 web 工具只能提列，由主編於 `/wiki-lint` 查證後填」，但沒說**用什麼指令、抓哪一份**。§6 證明一次 `gh api` 就能拿到 6,358 行完整 changelog，這是本波最有價值的可複製動作。**修法**：§7(d) 末補一句 —— `主編查證動作：抓 anthropics/claude-code 的 CHANGELOG.md 全文，對「上次核對版本 → 最新版」逐版掃 breaking／removed／no longer／default 字樣；核對到的最新版本號寫在表上一句，下次從那裡接續。`

---

## 1. 沒有問題的地方（明說，免得下一輪重問）

- **拆頁判斷正確**：claude-code「已知問題」子故事第 ② 題（自己的結論）現在確實不成立，先補結論層再談拆是對的；radar 月節 05／06 已封存、07 未達 3 個月門檻，拆頁確實只是提前做同一件事。
- **不做名稱配對式看守的理由成立**：我獨立配對只認得出 25/91 列，66 列配不上——配不起來的比對就是假看守。
- **節名凍結判斷正確**：`## 版本更新` 是全站唯一被錨點指入的節（錨點閘已是致命、存量 0），其餘六節錨點邊皆 0。
- **claude-code 九組組頭統計自洽**（健檢卡 N5），本案不動它是對的。
- **結論表七列全部通過自訂三判準**，我逐列開原頁核對過；#13354 以 `enhancement` 標籤退出是正確判斷。
- **`code-quality-decline` 只動兩處**合乎派工授權；四筆走帳本的分類正確；「你的選項」段列選項不下指令。
- **兩支閘確實在量這份稿**：設計者的 A-2 驗紅（注入禁詞＋133 字元儲存格各命中一次、移除後回綠）是合格的紅燈證據。

## 2. 照順序執行清單（實作單）

| # | 做什麼 | 驗證與預期輸出 |
|---|---|---|
| 1 | 依 🔴-2 修正 §7(a)／(f)、🟡-1 換掉兩個爭議數字、🟡-5 改寫界線與 §12、🟡-6 教訓敘事移進沿革檔 `## 2026-09-06`、🟡-13 補查證動作 | — |
| 2 | 改 `.claude/rules/wiki-ingest-features.md` 六處（§7 a–f，含 🟡-10 生效條款）；確認三個 registry 字串仍在檔內 | `python scripts/check_rules.py` → 零錯誤 |
| 3 | 機械編輯 radar 42 行：刪 39 個詳細條目標頭的熱度與試用價值欄、刪推薦節 3 條的熱度括號（可派 sonnet） | `grep -c "發布：.*熱度" wiki/feature-radar.md` → 0 |
| 4 | 加 `check_feature_radar.py` 五行（範圍依 🟡-11 (b)），**改壞驗紅**：補回一個熱度標頭 → FAIL 指名該行；還原 → exit 0 | 紅燈證據貼進回報，未驗紅不得宣稱有看守 |
| 5 | radar 版面：新 callout（🟡-4）、推薦節改名＋每則補「怎麼開始」、**階梯表換成 🔴-5 的 12 列**＋表下 catch-all 句＋🔴-1 的「與版本無關」段＋改寫後的界線＋「以上皆非」收尾句、L27 錨點原樣保留、L17／L19 進 `%%`、L219／L220 改日期式註記、L445 改指路、L763 加註記與複查日、評分說明下沉 | `check_feature_radar.py` OK；`check_cell_limits.py`／`check_reader_language.py` 無新增 |
| 6 | **🔴-6 keybindingFlavor 三處同改**：radar L155 表列加註＋改 ❌、L350 加失效句、L356 範例刪除 | `grep -n "keybindingFlavor" wiki/feature-radar.md` → 剩兩處且都帶「失效」 |
| 7 | claude-code 版面：callout 改寫、L60–68 七筆依 verified 結案並移出「現況」、L76 事實更正、新增 `## 現在會咬到你的`（七列）、L139 留言數 72→80、L193 補狀態、L215 沿革進 `%%`（🟡-3）、L523 移入歷史記錄、L586 補 guide 出口、競品／實用工具併入相關議題、參考來源留最近 10 則 | `check_pending_markers.py` → 語意反轉警告消失、逾期筆數下降；`check_cell_limits.py` 無新增 |
| 8 | **裁決點 1 預設處置**（使用者未回覆時不砍）：只把「⚠️ 安裝安全警示」移進 `## 已知問題` 的 🛡️ 組並把組頭統計 +1，節內「已完整記錄於他處」改寫為一行指路 | 逐組重數，組頭數字與實際條目數相符 |
| 9 | `code-quality-decline` 兩處：摘要懸置結案、「當前狀態」三段改寫 | `check_reader_language.py` 無新增（L40「已掃日報至」隨改寫消失） |
| 10 | 🟡-2 探針回掃三個關鍵字，逐一判改 | 回報寫 `回掃：入邊 N（改 M）＋關鍵字 K（改 J）` |
| 11 | 🔴-3 四處同步：`.claude/commands/wiki-ingest.md:120–121`、`wiki/index.md:23`（＋L47／L97 依 §7 末段）、`wiki/overview.md:133`、`app.js:371` ＋ `index.html` 的 `?v=` | `check_rules.py` 綠；`grep -rn "升版風險|本週推薦" .claude/ wiki/ web_reader/` 只剩沿革檔與 log |
| 12 | 🟡-8 `inquiry_bank.py:66` 節名同步；🟡-9 `build_web.py:454` summary 改寫 | `python scripts/run_tests.py` 全綠 |
| 13 | 🔴-4 開四筆帳本（`--from 功能 --to 社群`）＋🟡-12 兩筆 skill-interest-watch；guide 三條記進 `ledger.md` 待辦 | `python scripts/pending_handoffs.py list` 看得到 6 筆 |
| 14 | `python scripts/gen_wiki_frontmatter.py` → `python scripts/run_tests.py` → `python scripts/build_web.py` | 三者全綠；build 尾行「錨點 WARN」筆數**不增**（現為 0） |
| 15 | 單一 commit（指名路徑，禁 `git add -A`），`wiki/log.md` append Query 條目 | commit 訊息說得出每個檔為什麼在 |

## 3. 設計者第二輪必須親自改的項目

- **🔴-5**（階梯表整表換料）——我給的 12 列可直接貼上，但「哪九條進表、v2.1.160 要不要為它把上限改成 13」是判準層的取捨，且 §6 的 17 條原料他還沒看過，該由他確認過再定稿。

其餘 5 個 🔴 與 13 個 🟡 的修法都給了可直接貼上的文字，實作者照做即可，不需回設計者。
