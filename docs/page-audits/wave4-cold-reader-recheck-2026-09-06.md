# Wave4 冷讀者複驗（2026-09-06）

**身分：** 每天用 Claude Code CLI 跑多 agent 流程的工程師，停在 v2.1.220 兩週，今天花五分鐘決定升不升版。
**規則：** 只從 `wiki/index.md` 出發、只沿 `[[連結]]` 點；不猜檔名、不 grep、不讀 docs/scripts/news。

## 走過的路徑（共 6 跳）

1. `wiki/index.md`（入口，全讀 120 行）
2. `wiki/feature-radar.md`（由 index:25、index:15 進）
3. `wiki/entities/claude-code.md`（由 index:25、feature-radar:46 進）
4. `wiki/topics/skill-interest-watch.md`（由 index:31 進）
5. `wiki/topics/code-quality-decline.md`（由 index:100 進）
6. `wiki/topics/coding-workflow-guide.md`（由 index:34 進）

---

## Q1 這兩週有新版嗎？值得升嗎？升了會壞什麼 — ✅ 拿到（2 跳）

feature-radar:22–41「從你現在的版本升上去，會遇到什麼」正是為我這種人寫的。我停在 v2.1.220，表上 v2.1.222 以上每一列都落在我身上，而且三列直接打到我用的東西：

- `v2.1.232`「Subagent forking 預設開啟；互動 session 中非隊友的代理生成改為背景執行」（:35）
- `v2.1.243`「`CLAUDE_CODE_SUBAGENT_MODEL` 由『覆蓋全部』改為『預設值』」（:31）
- `v2.1.215`「`/verify`／`/code-review` 不再自動觸發」（:37）

最幫到我的一句在 :24：「**本表的依據是官方 changelog 逐版核對（核對到 v2.1.261，2026-09-06），不是本庫的報導覆蓋率**」——它讓我敢相信這張表是完整的。收尾句 :46「上表你都對不上號，版本面就沒有東西擋著你升級」也給了明確結論。MCP 我有用，但升版表沒有 MCP 破壞性變更，這件事我是靠 :46 指去 claude-code 才確認的。

## Q2 品質退步現在怎樣了 — ✅ 拿到（2 跳）

code-quality-decline:40–44 三段就答完：4 月那次官方結案（原因三項、修於 v2.1.116、重置用量）；06 下旬 token 異常與 07-25 起 Opus 5 品質感知**官方都沒說明**；:44「現有證據不足以支持『換一個模型就好』——三批訊號沒有一批指向特定模型版本」直接回答我「該換模型還是等」。這是我今天讀到最誠實的一段。

## Q3 最值得試的新功能 — ⚠️ 半拿到（2 跳）

feature-radar:12–16 給了三件並附「怎麼開始」，形式正確。但三件裡兩件不是「這禮拜」的：Auto 模式是 08-14 生效、Opus 5 是 07-25 發布，區塊卻叫「這禮拜值得跟的三件」。而且第二件 Fable 5.1 的「怎麼開始」說用 `/model` 確認（:15），同頁詳細條目 :95 卻寫「官方公告未列出確切 `--model` id 字串，暫不提供指令範例以免誤導」——同一頁自己打架。

## Q4 subagent 說做完沒做完 ＋ MCP 連不上 — ✅ 拿到（4 跳，但兩題分兩條路）

- **MCP：** claude-code:89–97 的表用「誰會遇到」欄讓我當場對號：#24057 改設定要重啟整個 session、#5706 沒有 token 刷新機制，兩條都寫「用 MCP 的人」，狀態 🔴、我對得上；#52871（Entra ID）、#61015（排程 routine）我對不上。往下 :250–265 的 MCP 分組還有 13 條，包括 :254「宣告 draft-07 `outputSchema` 的 MCP server 完全無法使用」。**全部未修，官方尚未回應。**
- **subagent 回報完成卻沒做完：** index:31 帶我到 skill-interest-watch:52，只給我一個工具（Groundtruth，證據 🟡 04-27），沒告訴我這是不是已知問題。真正的答案在第 6 跳 coding-workflow-guide:510–524：官方替它命名為「the trust-then-verify gap」、修法是「要證據不要斷言」，社群面列出 4 種靜默失敗模式與對抗性雙 Claude 審查，:524 還誠實寫出「還沒解掉的那一塊」。**這一段值 4 跳，但我差點沒走到。**

---

## 特別提問

### 1. 入口：首屏有沒有帶我到升版

有，而且是第一眼就看到。index:25 是一列獨立的單列表格「我在舊版本，升上去會壞什麼 → feature-radar『從你現在的版本升上去，會遇到什麼』；壞掉的東西見 entities/claude-code『現在會咬到你的』」，排在主表之前。到的是 feature-radar 的版本階梯節與 claude-code 的結論層表，兩個都對。第一次掃過去看得見——它單獨成表反而醒目，只是版式怪（看起來像一張只有表頭的表）。

### 2. 記「版本／新功能」的地方有幾個、矛不矛盾

碰到 **4 個**：feature-radar 升版階梯表（:26–39）、feature-radar 功能全覽表（:121–150）、claude-code 現況版本動態（:44–49）、claude-code 版本更新表（:401–）。**同一版本號在不同處寫的內容不一樣：**

- **v2.1.233**：階梯表 :34 說「Todo／Task 追蹤工具在 Opus 4.8、Sonnet 5、Fable 5 上被拿掉」；全覽表 :137 與版本表 :427 都只寫 GitLab MR URL 支援與 `forward_user_identity`，隻字未提 Todo 工具被拿掉。
- **v2.1.234**：階梯表 :33 說「`/config` 移除 Default teammate model」；全覽表 :136 與版本表 :426 都說是新增 `CLAUDE_CODE_PROJECT_DIR_NAME`。
- **v2.1.234 的日期**：版本表 :426 記 2026-08-17，同頁歷史記錄 :658 記 2026-08-18。v2.1.222 同樣是 :440 記 08-04、:668 記 08-05。

版本表上方 :399 有一句「本表只收本庫日報報導過的版本」，事後回想能解釋前兩條，但我當下的感受是「這兩張表在講不同的事，我不知道該信哪張」。

### 3. 每頁總評：百科還是雷達

| 頁面 | 判定 | 一句話 |
|---|---|---|
| index | 雷達 | 開頭那列升版路由直接接住我的問題 |
| feature-radar | 雷達 | 階梯表＋「你要做的事」欄是全站最像雷達的東西 |
| entities/claude-code | 半雷達半百科 | 「現在會咬到你的」8 列是雷達；往下 796 行、上百條已知問題是百科 |
| topics/code-quality-decline | 雷達 | 有摘要、有「你的選項」、有假說證據分級 |
| topics/skill-interest-watch | 百科 | 決策表是雷達，但下面一長串星數榜對我今天的決定毫無作用 |
| topics/coding-workflow-guide | 雷達 | 每段都答「現在該怎麼做」，但標題掛著內部標籤 |

### 4. 內部用語外洩（原句＋行號）

1. coding-workflow-guide:230 `## 2. 探索 codebase … 　[已深查]`（:341、:407 同）
2. coding-workflow-guide:444 `## 6. 測試與上線 …　[社群面待補]`（:469、:490 同）
3. coding-workflow-guide:384 `## 4. 實際動手寫 — 錯誤何時被攔　[已補：庫內證據]`
4. coding-workflow-guide:51「本頁第 4 段（實際動手寫）補齊社群面證據」——頁面在跟自己講進度
5. code-quality-decline:89「🧰 現在就能下的解：—（…｜**候選症狀：感覺變笨，想先量測歸因**）」
6. code-quality-decline:107「具體結果 ❓ 待查證 **⟨Q-01⟩**」——尖括號編號沒人解釋
7. skill-interest-watch:31「🗓️ 每日快照（**機器產出；決策表抄自社群工具目錄、最多落後一天**）」
8. skill-interest-watch:72「⚠️ 星數記錄目前只涵蓋 3 天（需 7 天），『本週竄升』欄**尚在冷啟動**」
9. claude-code:105「上方『現在會咬到你的』為本組的**結論層索引，非搬移**」
10. claude-code:35「**（2026-09-04，補跑日報）**」＋ :646「（補跑：09-04 排程抓料失敗，隔日現抓補齊）」

另外 claude-code.md 檔案開頭 :1–25 整段 `pending_count`／`signal: 健康`／`generated_by: scripts/gen_wiki_frontmatter.py` 我完全不知道在說什麼。

### 5. 「這是在騙我／這數字哪來的」

- **skill-interest-watch:82**「multica-ai/andrej-karpathy-skills … **210,287** ★」——一個 CLAUDE.md 範本 repo 的星數，比 claude-code:66 寫的「Claude Code GitHub Stars 達 **131,000+**」還高六成。同站兩個數字擺在一起就不可信，而榜上沒有任何抓取日期或校驗說明。同類還有 :48「08-31 訊號 11.3 萬星」、coding-workflow-guide:49「claude-mem：9.3 萬星、已存在逾一年」。
- **feature-radar:12「⭐ 這禮拜值得跟的三件」**：內容含 08-14 與 07-25 的條目。區塊名對讀者的承諾與內容不符，這是最直接的「感覺被騙」。
- **feature-radar:14**「靠手動確認做安全把關的人尤其要看一次」是判斷，但同一頁沒說為什麼；直到讀 claude-code:110 才知道 Auto Mode 提示注入成功率 60–80%、官方評測宣稱 0%。**推薦與風險被拆在兩頁**，只讀推薦的人會低估。
- **正面：** claude-code:89 那張表的「誰會遇到」欄我能逐列對號（「用 MCP 的人」對得上、「Windows 上用 Cowork 的人」對不上），是全站最讓我信任的一欄；skill-interest-watch:56「證據等級為收錄或查證當時的判定，括號內即判定日，**不隨時間自動回訪**」也是誠實的自我限縮。
- **撐不起的結論：** skill-interest-watch:52 用「🟡（04-27）」單一實測就把 Groundtruth 定為「它說做完了但根本沒做」的唯一首選，判定日是四個多月前，而 coding-workflow-guide:522 對同一症狀列出的社群做法完全沒提這個工具。兩頁對同一症狀給不同答案。

### 6. 我最想改的三件事

1. **把「這禮拜值得跟的三件」裡放不進本週的條目拿掉**，寧可只列一件——區塊名就是承諾。
2. **升版階梯表與版本表對不上的版本（v2.1.233／234）標一句話說明兩張表的來源不同**，否則讀者只會覺得站上自相矛盾。
3. **把 `[已深查]`／`[社群面待補]`／`⟨Q-01⟩`／`候選症狀` 這類寫給維護者的標記從標題與正文拿掉**，它們佔的是我掃視標題的注意力。
