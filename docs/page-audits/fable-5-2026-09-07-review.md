# 評審：`wiki/entities/fable-5.md`（第 10 波）

2026-09-07｜評審｜受評＝磁碟現行的 `-proposal.md`／`-proposal-map.md`／`-draft.md`｜適用區 A／B／D／E，**C 不適用**（無視覺成品）｜⚠️ 疑似注入：無
**結論：有條件放行。** 6 🔴 9 🟡；使命句、三張結論表的形狀、死案歸檔的例外 2 逐名核查、兩閘驗紅都成立，設計者自報的兩處沒把握有一處我判他錯（回掃射程）。以下每條 🔴🟡 附可直接貼上的文字，末節「照順序執行」即實作單。

## 我自己重算的存量（與提案對照）

| 項 | 提案說 | 我實測 | 判 |
|---|---|---|---|
| 頁面行數 | 412 | 412（健檢卡寫 413 是錯的） | ✅ |
| 改版後行數 | 約 265 | **約 316**（逐節加總，見 🟡-1） | ❌ |
| 懸置標記 | 6 筆全在 L203–255；搬走三段零標記 | ✅ 逐筆確認（203／220／238／246／248／255） | ✅ |
| 標記基線 | 142，短標記化後回到 146 | 142 ✅；**146 錯**，`_marker_fingerprints()` 只數 `PENDING_RE`，短標記不計 → 結案後為 141 | ❌ |
| 讀者語言存量 | 1 筆在 L260 | ✅ L260「收錄門檻」（健檢卡寫 L121 確為誤植，設計者對） | ✅ |
| archive 新增外洩 | 2 筆「記者會」 | ✅ 把三段接起來重跑 `body_lines`＋`TERMS`，正好 2 筆 | ✅ |
| 最長新儲存格 | 61 字元 | ✅ 61（上限 120）；最長細節條列 166（上限 200） | ✅ |
| `grep -rn "fable-5#"` | 零命中 | ✅ 零 | ✅ |
| 三張新表 census 判「有」 | 三張全「有」 | **2/3**——熱度表回「無」（🔴-1） | ❌ |
| 爭議 13 → 10 條 | 10 | **12**（移除 1、兩條降為指路句仍佔條） | ❌ |

## 🔴（六條）

### 🔴-1｜熱度表的 h3 過不了 `_mechanism()`，收工判準 4 當場做不到

我把 B-2 的四個 h3 餵進 `table_census._mechanism(page_slug="entities/fable-5")`：結論表「有」、護欄表「有」、**熱度與試用價值「無」**、爭議「無」。原因是 `MECH_RE = 淘汰|移除|保留|封存|覆寫|蒸餾|汰除|到期|清理`（`table_census.py:30`）必須命中**同一行**，而該 h3 整行只有「同步對象是…那一列」。設計者 §8 宣稱「三張全『有』」不成立。

**修法（B-2 第三個 h3 整行替換）：**

```
### fable-5 的 `## 熱度與試用價值`：同步 feature-radar 全覽表現行世代那一列，不一致即覆寫
```

（實測改成這行後回「有（wiki-ingest-models.md）」。）

### 🔴-2｜爭議節自訂上限 10，第一天就是 12 條——退場條文沒拿現有列跑過

現有 13 條（L109–121 逐條數過）。A-9 只移除 L110（併入護欄節），L119／L120 降成指路句但**仍是條列**，故落地是 12 條。B-2 又寫「上限 10 條」，等於實作者收工時必須再砍兩條真事實，或默默違規。

**修法（B-2 爭議節上限句替換）：**

```
**上限 12 條**（其中「家在別頁、本節只留一句指路」的條目至多 2 條，不抄互動數字）
```

提案 §6 與 A-9 標題的「13 → 10」一併改為「13 → 12（含 2 條指路句）」。

### 🔴-3｜`## 相關議題` 有一行憑空消失：`[[topics/ai-agent-safety]]`

現行 5 條為 mythos／pricing／gov-policy／anthropic-business／**ai-agent-safety**。A-10 的 6 條裡 ai-agent-safety 不見了，去向表只記「移除 anthropic-business、新增 claude-code 與 long-context-1m」——`model-comparison` 是未登記的新增、`ai-agent-safety` 是未登記的刪除。而本頁 L121 與 L280 兩處正文都寫著「安全政策脈絡另見 [[topics/ai-agent-safety]]」，刪掉出口等於留下兩個孤兒引用。

**修法（A-10 補回一行，成 7 條）：**

```
- [[topics/ai-agent-safety]] — 護欄被繞過、越獄與提示注入這類攻擊面（本頁爭議節那幾條的安全政策脈絡）
```

去向表 §二 L158–164 那格補一句：「新增 model-comparison；ai-agent-safety 留」。

### 🔴-4｜H-754509 的收尾內容錯三處，且據以開了一筆打不到人的帳本

帳本 note 的行號是 2026-09-06 的快照，設計者原樣沿用未核。逐檔實查：

- **漏一處**：`entities/fable-5:325`「**管制事件歷時 18 天**（2026-06-13 至 2026-07-01）」在 `### 解禁後` 內（L199–326），去向表寫「L223–326 留，一字不動」，所以它會活下來，本頁仍自帶兩個結束日。
- **誤指一處**：close 訊息寫的「L303 端點」，L303 實為「免費期再延長…延至 2026-07-12」，與管制無關。
- **兩筆已不存在**：`entities/tom-brown` 全檔 36 行、`page_role: redirect`、正文只有「已併回 [[topics/anthropic-government-policy]]」——沒有 L42／L67；`entities/opus-4-8` 全檔 grep「出口管制」零命中。B-5 第二條 `open --to 人物` 會派一個人去改不存在的行。
- **真正殘留的第四處**：`entities/mythos:227` 節標「### 出口管制期（2026-06-13 至 06-26）」——第三個結束日，且屬同一維護者。

**修法：** (a) 去向表 §三 加一列「列首『- **管制事件歷時 18 天**』（L325）→ 改寫為 `- **管制解除**：2026-06-30 宣布、07-01 全球恢復（天數口徑見 [[topics/anthropic-government-policy]]）`」；(b) B-5 刪掉 tom-brown 那條 `open`；(c) close 訊息改成：

```
python scripts/pending_handoffs.py close H-754509 --by 模型 --result "本頁四處已處理：L48 現況改寫、L325 改為指路不寫天數、L327 節標隨 2026-06 封存消失、L103 靜默 fallback 依官方 06-30 原文改寫並補回指政策頁。帳本原列的 entities/tom-brown L42／L67 與 entities/opus-4-8 L46 經 2026-09-07 實查已不存在（前者第 6 波併頁後為 redirect 殼，後者全檔無出口管制敘述）。全庫僅餘 entities/mythos:227 節標『出口管制期（2026-06-13 至 06-26）』，同維護者，同批改為 06-30"
```

### 🔴-5｜懸置基線的算術錯了，寫法會誘導實作者跳過 rebuild

B-6 寫「5 筆短標記化後總數回到 146、不會觸發 FAIL」。實測 `check_pending_markers._marker_fingerprints()`（`:210-227`）逐頁跑 `iter_pending()`，只認 `PENDING_RE` 標準式；`SHORT_RE` 只在 `:173` 的雙向對帳用，**不計入總數**。短標記化把一筆標記拆成「短標記＋定義」，定義行仍是同一個 `PENDING_RE`，總數不變。落地後是 **141**（142 − 費馬 1），低於基線即 FAIL，rebuild 是必要不是保險。

**修法（B-6 第二點整段替換）：**

```
**懸置標記基線**：費馬結案後全庫由 142 降為 141（短標記化不改變總數——基線只數 PENDING_RE 標準式，見 check_pending_markers.py:210-227）。必跑：
python scripts/check_pending_markers.py --rebuild-count --reason "entities/fable-5 費馬歸屬經官方一手查證結案（內部研究模型，非 Fable 5／5.1）"
```

### 🔴-6｜三張表沒有任何週期性看守，而提案 §1 說有

§8 誠實寫著「`/wiki-lint` 現在不會跑本頁三張表的退場」，但 §1 可行性前提又寫「新增的週期性動作…掛在既有的模型記者 lint」——`.claude/commands/wiki-lint.md` 全檔 `fable` 零命中，5b 只做跨家榜單。第 5／7／8／9 波各自加了 5i／5j／5k，本波沒有，等於三張覆寫式表的「資料截至逾 60 天重查」是一句無人執行的話。

**修法：** §1 那句改成「本波不新增週期性動作，改由下述 5l 承接」，並在 `.claude/commands/wiki-lint.md` 5k 之後新增一步（比照 5i／5j）：

```
### 5l. 模型頁世代表複查（主編親做）`[加入: 2026-09-07]`

對 `wiki/entities/fable-5.md` 三張表跑一次（判準見 `.claude/rules/wiki-ingest-models.md`「entities/fable-5 的三張表」）：① `## 你現在拿到的是什麼` 表上「資料截至」距今逾 60 天 → WebFetch 官方模型總覽頁重查七列並更新查證日；② `## 護欄會怎麼改寫你的請求` 四類是否仍為官方公布的四類；③ `## 熱度與試用價值` 對 feature-radar 全覽表**現行世代**那一列，不一致以 radar 為準覆寫。

模型頁世代表（5l）：結論表資料截至 YYYY-MM-DD（重查 N 列）／護欄四類 一致 or 變動 M 項／熱度表 一致 or 已覆寫
```

並在步驟 8 的 lint 紀錄清單加同一行。

## 🟡（九條）

- **🟡-1 行數自報偏低約 50 行。** 逐節加總：frontmatter 25＋標頭 15＋現況 6＋結論表 18＋熱度 11＋護欄 20＋使用指南 10＋核心功能 7＋爭議 15＋相關議題 9＋參考來源 31＋歷史記錄 149（130 原文＋懸置細節 8＋2026-06 總結 12）＝**約 316**。把提案 §0、去向表表頭與 §五 的「約 265 行」全改成「約 315 行」——健檢卡的「250 行以內」本來就沒扣掉 07–09 三個月與 30 列參考來源。
- **🟡-2 `L299` 標題還寫「官方基準」，與改後的 L101 同頁互斥。** L299（歷史記錄 07-08 條目）標題逐字為「**官方基準：「Fable 5 orchestrates, cheap models execute」— 46% 成本達 96% 效能**」。歷史條目不改寫，但要 append 一句（共用規則「後續（日期）」款）：`**後續（2026-09-07）**：本站核查未見原始官方發布連結，此數字改列為社群整理轉載，非官方基準。`
- **🟡-3 `L275` 指向一個不存在的節。** 該行寫「見 [[topics/anthropic-government-policy]]「## 目前局勢」與「## 三個戰場」」——gov-policy 第 6 波已把該節改名為 `## 現在有哪幾條線在動`，全庫除 `log.md` 外**只剩這一處**還在指舊名。修法：把「「## 目前局勢」與」改為「「## 現在有哪幾條線在動」與」。
- **🟡-4 archive 兩個死案節沒有反向指針，且 A-11(2) 末行指錯錨。** 總結第 8 條寫「見下方連結」，而末行只給 `[[entities/fable-5-archive#2026-06]]`；雙方立場表住的是另一個 h2。修法：archive 的節名去掉括號改為 `## 出口管制：雙方立場`，總結末行改為 `原始條目見 [[entities/fable-5-archive#2026-06]]；雙方立場的兩張表見 [[entities/fable-5-archive#出口管制：雙方立場]]。`
- **🟡-5 本頁把讀者送去 pricing，pricing 六列旗艦零處寫 5.1。** 冷讀者 Q1 的半個缺口（pricing:61–63）本波無人處理，而 A-4 第 7 列與 A-7 都指過去。跨維護者 → 走帳本：`python scripts/pending_handoffs.py open --from 模型 --to 商業 --page entities/pricing --note "「我的方案現在有什麼」表 L61-63 三列旗艦欄只寫 Fable 5，未提 Fable 5.1；官方說明中心載明兩代同一套規則（Max／Team premium 週用量 50% 內免費、Pro／standard 走 usage credits），07-19 到期的促銷只適用 Fable 5。請在旗艦欄補寫 5.1 並標查證日"`
- **🟡-6 同型編輯台語言只改一處。** A-9 改掉 L121 的「日報收錄／弱訊號／score 恆 0」，但 L280 是同一批事實的歷史條目，逐字帶「弱訊號，社群主觀」「同期日報另收錄」「score 恆 0」。事實不改，但這三個詞是編務語彙：建議同批把「（弱訊號，社群主觀）」改為「（單一來源，社群觀點）」、「同期日報另收錄」改為「同期另有」。
- **🟡-7 archive 標頭「最後新聞更新 2026-06-29」與內容不符。** 搬進去的 `## 配額與計費過渡` 條目日期到 2026-07-03。改成 `2026-07-03`；母頁 callout 09-01 仍 ≥ 它，`check_hierarchy.py:136-138` 的 hub 不落後照樣過。
- **🟡-8 radar L181 的試用價值仍是「✅ 推薦」。** A-13 只改該列狀態欄。一個官方建議遷移的 Legacy 版掛「✅ 推薦」，與同表 L126 現行世代的「⚡」並排會誤導；給主編的文字建議一併把 L181 試用價值改 `⏳ 觀望（Legacy，改用 5.1）`。
- **🟡-9 A-4 第 1 列與 A-7 重複同一句操作。** 兩處都寫「用 `/model` 確認」；照「同一事實同頁至多兩處」不算違規，但表格格子留結論即可，建議第 1 列縮成「是——v2.1.257 起 Fable 預設即 5.1」。

## 回掃射程：我的判定（設計者 §11 自報沒把握處）

**設計者判錯，model-comparison 三處本波要改。** `wiki-reporter-shared.md`「事實更正必回掃」的措辭是「把某個事實從 A 改成 B 時，**改完不算完**……描述『現在』的句子一律改」，且該節第 3 步明寫兩組命中「逐一判斷」，沒有給「同維護者其他頁」任何例外。skill 邊界的「同維護者鄰居只改入口句與互指句，不重寫」管的是**版面重設計**的射程，不是事實更正——把三個儲存格的來源等級由「官方基準」改成「社群轉載」不構成重寫。且 `:77` 是**快速選型表的依據欄**，讀者拿它決定要不要走多模型協作，屬「描述現在」。三處實查：`:77`「官方基準：Fable 5 調度 46% 成本達 96% 效能」、`:86`「…為官方基準（Reddit 轉載，週熱門，2026-07-08）」（同一句自打）、`:165`「46% 成本達 96% 效能」無來源標。

**修法：** 本波同批改 `:77` 與 `:165` 的「官方基準」為「社群轉載」、`:86` 整句改為「Fable 5 調度 46% 成本達 96% 效能為社群整理轉載（Reddit，週熱門，2026-07-08），未見原始官方發布連結」。提案 §9 與去向表 §四第 4 點的「本波不改、列 09-14 回訪」刪除。跨維護者的 patterns 與 trends 照設計者原案走帳本，**不變**。

## 去向表抽驗（8 段，逐段開檔核）

| 抽驗 | 去向表寫什麼 | 實查 | 判 |
|---|---|---|---|
| L26 | `# Claude Fable 5` | 逐字相符 | ✅ |
| L50–56 | 指標表 5 列（Input／Output／Context／最大 Output／免費期限） | 五列逐列相符 | ✅ |
| L69 節名 | 帶已過期日期 | `## 配額與計費過渡（原訂 7/7，已於 7/19 到期；後續分界已確認）` | ✅ |
| L89–92 | code block `claude --model claude-fable-5-20260609` | 相符；`claude-code:175` 確載 `--model` 只接 family 名、⛔ 官方拒修 | ✅ |
| L107–121 | 爭議 13 條 | 13 條；但落地為 12 不是 10（🔴-2） | ⚠️ |
| L158–164 | 相關議題 5 條 | 5 條；A-10 少掉 ai-agent-safety（🔴-3） | ❌ |
| L218 錨點 | `[[topics/model-comparison#同一份工作，換設定差多少]]` | 在 L218；`review-registry.json:158` 確列本頁為五筆入邊之一（該欄標的 L187 是舊行號） | ✅ |
| L327／L400 節標 | 出口管制期／管制前 | 兩個節標行號正確；L329–412 共 86 行 | ✅ |

## 四視角各一句

- **明天的維護者**：三張表誰填寫得清楚（主編查證後填、記者不得自填），但沒有觸發邊叫他去填（🔴-6）；5.1 的動態依 B-1 進「世代動態」無歧義。
- **機器**：archive 契約四項（上層／`resolved（封存頁）`／領域繼承／hub 不落後）全部核過原始碼，`check_cell_limits.py:78` 的 `-archive` 排除也對；allowlist 綁 page、搬家即失效這一點設計者實測正確，是本案最容易漏的一步；壞的是 census（🔴-1）與基線算術（🔴-5）。
- **冷讀者**：Q1 由第三頁第 62 行前移到本頁第 7 列、Q2 的「會不會下線」第一次有官方答案、Q4 的護欄節能答「我做什麼會踩到」——三題都應由半拿到轉拿到；Q5 的分工句到位。殘留風險是 index 新路由列與 L76 鉤子屬主編區，實作者改不到，複驗前必須先落地。
- **治理**：節點三問答得住（留頁、不拆、不併，B 群無別家是唯一留頁理由）；死案歸檔的例外 1／2 逐名核過我認可；跨維護者走帳本正確，但同維護者的回掃被誤劃出射程（見上節）。

## 照順序執行（實作單；先貼新表再砍舊節）

1. **改規則檔**：`.claude/rules/wiki-ingest-models.md` 依 B-1／B-2／B-3 寫入，其中 B-2 第三個 h3 用 🔴-1 的整行、爭議段用 🔴-2 的上限句；B-4 寫入 `wiki-ingest-features.md`。驗：`python scripts/check_rules.py`，預期 exit 0。
2. **加 lint 步驟**：`.claude/commands/wiki-lint.md` 插入 🔴-6 的 5l 全文＋步驟 8 回報行。驗：`python scripts/check_rules.py` exit 0；`grep -c "5l" .claude/commands/wiki-lint.md` 預期 ≥ 2。
3. **建 archive**：`wiki/entities/fable-5-archive.md` 照 A-12，節名用 🟡-4 的無括號版、最後新聞更新改 2026-07-03；三段原文**先複製後刪除**，用 `sed -n` 取原行。驗：`python scripts/check_hierarchy.py`，預期不報「archive 未掛父」「archive 狀態非封存頁」。
4. **加 allowlist**：`data/reader-language-allow.json` 加 B-6 第一筆。**驗紅**：先不加跑 `python scripts/check_reader_language.py --page fable-5-archive` → 預期 2 筆新增 FAIL；加了再跑 → 預期「OK: 讀者語言閘 — 無新增命中」。
5. **母頁上半改寫**：A-1／A-2／A-3（含 🔴-4 的現況不寫天數）→ 插入 A-4 結論表 → A-5 熱度表 → 插入 A-6 護欄節 → A-7 使用指南 → A-8 核心功能。驗：`python scripts/check_cell_limits.py --page fable-5`，預期「無新增超限」。
6. **母頁下半**：A-9 爭議（12 條）、A-10 相關議題（7 條，含 🔴-3 補回的一行）、砍 L69–86 與 L123–157（內容已在步驟 3）。驗：`python scripts/table_census.py entities/fable-5` → 預期結論表／護欄表／熱度表三列「機制」欄皆為「有（wiki-ingest-models.md）」。
7. **歷史記錄**：費馬條目改寫並移除該筆標記；🟡-2 的後續句 append 至 L299 條目；🟡-3 改 L275 節名；🟡-6 改 L280 三處措辭；🔴-4(a) 改 L325；5 筆標記短標記化＋`### 解禁後` 末建「懸置細節」區；L327–412 換成 A-11(2) 十行總結（末行用 🟡-4 的雙錨點版）。驗：`python scripts/check_pending_markers.py` → 預期無 ⟨Q-nn⟩ 對帳 FAIL；再跑 `--rebuild-count --reason "…"`（🔴-5 逐字）→ 預期印「懸置標記基線已重建：141 筆」。
8. **同維護者回掃**：`wiki/topics/model-comparison.md` :77／:86／:165 三處與 `wiki/entities/mythos.md` :227 節標，依「回掃射程」節改。驗：`grep -n "官方基準" wiki/topics/model-comparison.md` 預期零命中；`grep -rn "06-26\|06-13 至 2026-07-01" wiki/entities/ wiki/topics/ --include=*.md | grep -v log` 預期零命中。
9. **帳本**：B-5 的 close（用 🔴-4(c) 逐字）＋ patterns／trends 那筆 open ＋ 🟡-5 的 pricing open；**不開** tom-brown 那筆。驗：`python scripts/pending_handoffs.py list` → 預期「模型 記者」待接手 0 筆。
10. **收尾**：`python scripts/gen_wiki_frontmatter.py` → `python scripts/run_tests.py`（預期 exit 0）→ `python scripts/build_web.py`（預期錨點 WARN 不增）。主編另依 A-13（含 🟡-8 的 radar L181 試用價值）改 `wiki/index.md` L76 與 `wiki/feature-radar.md`，冷讀者複驗須排在其後。
