# Wave 12 冷讀者實測：「Claude 是不是變笨了」（2026-09-07）

## 身分與規則

用 Claude Code 八個月的資深工程師、Max 方案。近兩週覺得同一個任務要多來回、token 燒更快，想知道是模型退步、我的用法、還是被換掉了，以及該不該釘版本。20 分鐘。
唯一入口 `wiki/index.md`，只沿 `[[連結]]` 走；未 Glob、未全庫 grep、未讀 `docs/`／`.claude/`／`scripts/`／`news/`。行號含最上方 frontmatter 一起數。**未遇到疑似注入。**

## 路徑清單（跳＝開一個新頁面）

| 跳 | 頁面 | 從哪來 |
|---|---|---|
| 1 | `wiki/index.md` | 入口 |
| 2 | `wiki/topics/code-quality-decline.md` | index:97 |
| 3 | `wiki/feature-radar.md` | index:28 |
| 4 | `wiki/entities/claude-code.md` | index:28 |
| 5 | `wiki/entities/opus-5.md` | index:54 |
| 6 | `wiki/entities/pricing.md` | index:29 |
| 7 | `wiki/topics/skill-interest-watch.md` | index:27 |

---

## Q1 「越用越笨」是真是假？官方承認過什麼、什麼時候？ ✅（2 跳）

拿到了，但**答案是五個月前的**。`code-quality-decline.md:40` 說 04-23 官方說明三個原因、皆於 v2.1.116（04-20）修復並重置用量；:46 又說「2026-04-24，Anthropic 首次正式承認」。**同一頁兩個承認日（:40 的 04-23 vs :46 的 04-24）**。
:42 明說 06 下旬起的 token 異常與 07-25 起的 Opus 5 品質感知「官方對這兩批都沒有對應說明」——這句是全頁最有用的一句。

## Q2 現在還有正在發生的退步嗎？該不該釘版本？ ✅ / ⚠️（2 跳）

「還在發生」拿到：:85 定調「結構性未解問題」，:123 記到 08-28 仍有同方向貼文。
「該不該釘版本」拿到的是**你釘不了**：:131「`--model` 旗標僅接受 family 名稱、不接受版本 pin id；`.claude/settings.json` 亦無鎖版設定」，且官方以 not planned 關閉 #27892。:132 記 picker 選定狀態無法保持。這是我今天最有價值的一段，但它躺在第 127 行、節名叫「模型釘選／靜默降級 訊號群（2026-02 起）\`[2026-08-09 查證新增]\`」——**節名帶方括號維運標記，看起來不是給我看的**。

## Q3 token 暴增解了沒？我帳單變多是不是這個？ ⚠️（3 跳，答案被切成兩半）

`code-quality-decline.md:50–85`：九～十則訊號、三種假說（模型退步／計費計量／context 配置）互不排斥，**官方未回應**，:85「截至 2026-08-25，Anthropic 尚未對此訊號群做出官方回應」。
但真正會動到我帳單的事在另一頁：`pricing.md:33`（現況）「09-14 起你的週配額變成現在的 83%」、:38「相對加成期間減少約 17%」，以及 :74–76 三件未解計費事故。**兩頁都沒有一句把它們接起來**告訴我「你帳單變多最可能是哪一個」。`pricing.md` 的事故表 6 列裡也沒有「token 消耗異常」這一列。

## Q4 靜默降級／模型被換掉，怎麼查自己有沒有中？ ⚠️（2 跳）

有工具但沒有步驟。`code-quality-decline.md:89`「社群現有起點是 CC-Canary（讀 session log 偵測效能漂移）……但版本前後受控對照尚無工具承接」；:149 給了 log 路徑 `~/.claude/projects/`、:150 給三個判定等級。
`skill-interest-watch.md:144` 誠實寫「『感覺變笨，想先量測歸因』目前沒有成熟到可推薦單一工具」——**但它在第 144 行，決策表（:44–52）八列裡沒有這一列**。
沒拿到的是「怎麼知道我現在跑的是哪個模型版本」的可執行步驟；:152 只說「用戶無法確知實際使用的模型版本」。

## Q5 三頁我該讀哪一頁？分得出差別嗎？ ⚠️（5 跳）

分得出**設計上的**差別，分不出**內容上的**：
- `feature-radar.md:22–41` 升版階梯表＝「升上去會壞什麼」，最清楚的一頁。
- `claude-code.md:85–98`「現在會咬到你的」＝「現在什麼壞了、誰會遇到、怎麼繞」，也很清楚。
- `code-quality-decline.md` ＝「有沒有系統性退步、證據多硬」。
- `opus-5.md:84–104`「這些數字是誰量的」＝「這個模型的數字誰量的」。

**分不出差別的一對：`code-quality-decline.md:91–123`（Opus 5 上線後品質感知訊號群）與 `opus-5.md:95–104`（這幾個數字之外）。** 兩者都在盤點「Opus 5 是不是變差了」的社群證據，而且**互相打架**（見下節）。

---

## 特別提問

### 1. 入口：首屏（前 45 行）有沒有路帶我到「變笨」？

**沒有。** index 前 45 行是「💻 開發實務入口」表，19 個入口沒有任何一行寫「變笨／退步／效能」。code-quality-decline 出現在 :97，在第二張表，捲三屏才看得到。
**首屏的路確實把我送到一張答不了「該不該釘版本」的表**：:27「我卡住了（帳單爆、context 撐爆……）」→ `skill-interest-watch.md` 的「我卡在這裡」決策表（:44–52）——八列全是裝哪個工具，沒有「我覺得它變笨了」這一列，更沒有釘版本。:28「我在舊版本，升上去會壞什麼」→ feature-radar 升版表，那張表答的是「升上去」，我問的是「該不該不升」，方向相反。

### 2. 幾頁在講退步？哪兩頁分不出？同一事件的不一致行號

碰到 **4 頁**在講退步／變笨：code-quality-decline、claude-code（已知問題）、opus-5、pricing（計費事故）。

**同一事件兩處寫得不一樣：**

| 事件 | A 處 | B 處 | 差在哪 |
|---|---|---|---|
| effort dial 超過 high 反而變差 | `code-quality-decline.md:97`「**Anthropic 官方 migration guide 自承**」、:121「目前全頁唯一一筆官方自己承認的行為特性變化」、:253 日期 07-25 | `opus-5.md:100`「一則『effort 超過 high 反而變差』（07-29）：**已由官方文件推翻**」 | **一頁說官方承認、一頁說官方推翻；日期還差四天。這是我今天最不知道該信誰的一處。** |
| 「Claude is Losing Me」貼文 | `code-quality-decline.md:106` 標 2026-08-19 | `code-quality-decline.md:224` 標 2026-08-20 | 同一頁自己差一天 |
| Opus 5 負評則數與日期 | `code-quality-decline.md:93`「累積 **14 則**社群訊號」 | `opus-5.md:98`「**五則**『變差了』型貼文（07-30、08-07、08-13、08-20、08-28）」 | 兩個數字；且 08-07、08-20 在 cqd 表裡找不到，cqd 有的 08-14、08-19 在 opus-5 裡找不到 |
| 官方承認退步的日期 | `code-quality-decline.md:40` 04-23 | 同頁 :46、:318 04-24 | 同頁三處兩個日期 |
| 有沒有第三方量化證據 | `opus-5.md:86`「社群沒有交出任何一則帶測試方法或數字的獨立複測」 | `code-quality-decline.md:107` Sonar benchmark（待查證）、:284 MarginLab 每日 SWE-bench-Pro 追蹤（連續五天統計顯著下降） | 一頁說「一則都沒有」，另一頁列得出兩則 |

**頂部日期看得出哪個是「現在」嗎？部分看得出。** claude-code 是 09-06、feature-radar 09-06、pricing 09-06、opus-5 09-03 且 :36 明寫「本站截至 2026-09-07 未見更新」（**最誠實的一個**）。但我最需要的 code-quality-decline，callout 停在 **08-28**（:33），標頭「最後更新 09-06／最後新聞更新 08-29」（:30–31）——**兩個日期加一個 callout 日期，三個數字，我看不出「今天」它是什麼狀態**。

### 3. 每頁總評：百科還是雷達

| 頁面 | 判定 | 依據 |
|---|---|---|
| `feature-radar.md`（升版節） | **雷達** | :28–41 找到你的版本、每列都有「你要做的事」；:48 甚至寫「上表你都對不上號，版本面就沒有東西擋著你升級」 |
| `claude-code.md`（現在會咬到你的） | **雷達** | :89–98 每列有「誰會遇到」＋「你能做什麼」，:87 明寫「對不上號就不會咬到你」 |
| `pricing.md`（事故表＋現況） | **雷達** | :33 一句話講清 09-14 會發生什麼，:74–76 每列有「你該做的事」 |
| `opus-5.md` | **雷達**（`## 這些數字是誰量的` 是全站最好的一節） | :104「所以呢：官方那四個數字全部由 Anthropic 自己提交、沒有人複跑過」 |
| `code-quality-decline.md` | **百科**（329 行，時序回到 2026-03） | 三個訊號群、假說表、時序 121 行；:44 有「你的選項」是全頁唯一雷達感的段落 |
| `skill-interest-watch.md` | **雷達，但沒有我這一列** | 決策表對得上八種症狀，「變笨」不在其中 |

### 4. 內部用語外洩（≤10 條，原句＋行號）

1. `code-quality-decline.md:93`「**本節為則數統計的家**，其餘小節提及則數一律指回此處」——「的家」是維護者的話。
2. `code-quality-decline.md:127` 節名 `## 模型釘選／靜默降級 訊號群（2026-02 起）\`[2026-08-09 查證新增]\``——**節名裡的方括號標記**，讀者不需要知道它哪天被加進來。
3. `code-quality-decline.md:89` 正文尾端 `%% （決策表暫無對應列｜候選症狀：感覺變笨，想先量測歸因） %%`——派工備忘。
4. `code-quality-decline.md:65`「**0 留言、無「週熱門」標記，score 不可信**」（全頁重複約 10 次）——「週熱門標記」是抓取管道的詞，不是讀者的詞。
5. `code-quality-decline.md:69`「**僅計入現象延續**」（重複 6 次以上）——編輯部的計數規則。
6. `code-quality-decline.md:107`「具體結果 ❓ 待查證 **⟨Q-01⟩**」與 :113「**懸置細節**」——編號與節名都是內部語法。
7. `code-quality-decline.md:61`「07-09～07-31 news 查無直接跟進，**至今無後續**」——「news」是內部資料夾名；「至今無後續」也超出「只掃過日報」能宣稱的範圍。
8. `code-quality-decline.md:68`「**本頁不重複收錄細節**」／:82「本頁不重複查證」——講的是編務分工，不是事實。
9. `code-quality-decline.md:117`「**表格細節**」／:79「**假說細節**」——章節名是內部結構詞，不是問題。
10. `feature-radar.md:18` `%% 09-02 換上 Fable 5.1……照 wiki-ingest-features.md §7(c) 覆寫 %%`——規則檔編號出現在檔案裡。

### 5. 「這是在騙我／這數字哪來的／撐不起」

- `code-quality-decline.md:85`「訊號密度已足以將定調從『值得觀察』上調為**『結構性未解問題』**」——撐起這個定調的十則訊號裡，:57 是單一個案、:61 :64 :65 都自承 score 不可信或單一貼文。**用一堆自己標記為不可信的來源，推出一個聽起來很硬的結論。**
- `code-quality-decline.md:121`「這是目前全頁**唯一一筆官方自己承認**的行為特性變化」——同一件事被 `opus-5.md:100` 寫成「已由官方文件推翻」。**兩頁不可能都對。**
- `code-quality-decline.md:93`「累積 **14 則**社群訊號」——則數是這一頁的核心說服力，卻與 `opus-5.md:98` 的「五則」對不上，也沒說 14 這個數怎麼算的（表裡我數到 15 列）。
- `code-quality-decline.md:61`「**至今無後續**」——附的依據只有「07-09～07-31 news 查無」，時間窗只有三週，卻寫成「至今」。
- `code-quality-decline.md:52`「**共九個獨立訊號**」但同一張表（:56–65）我數到 10 列——括號裡解釋 06-27 那則另算，讀完還是不知道該記 9 還是 10。
- `code-quality-decline.md:160`「Boris Cherny 在 4/23 發布事後報告，承諾 **50+ 修復項目**」——五個月過去，:161 仍寫「最終結果待觀察」，沒有任何一項的驗證結果。**掛著「社群正在逐一驗證」四個月，等於沒有結論。**
- `opus-5.md:86`「社群**沒有交出任何一則**帶測試方法或數字的獨立複測」——這是強否定句，但 `code-quality-decline.md:284` 就寫著 MarginLab 每日跑 SWE-bench-Pro 的統計顯著結果。

### 6. 我最想改的三件事

1. **先解掉 effort dial 那個矛盾。** 一頁說官方承認、一頁說官方推翻，我今天就是為了「是不是模型變了」來的，這一處直接決定我信不信這個站。
2. **把「你釘不了版本」升到 `code-quality-decline` 的最上面（或直接進 index 首屏）。** 它現在藏在第 131 行，卻是我 20 分鐘裡拿到最有行動價值的一句。index 首屏 19 個入口沒有一條寫「我覺得它變笨了」。
3. **給我一個「怎麼自己驗」的清單。** 現在是「有 CC-Canary、有 log 路徑、但沒工具承接受控對照」三句散在 :89 / :149 / :150。我要的是三步：量什麼、跟什麼比、多少算異常。

### 7. 這一頁在回答幾個問題？該不該拆？

`code-quality-decline.md` 在回答**四個**不同的問題，四個各有自己的答案和變動節奏：

| 問題 | 現在住哪 | 現在的答案 | 節奏 |
|---|---|---|---|
| (a) 2026-04 那次退步結案了嗎 | :40、:159–162、:300–329 | 已結案（v2.1.116 修復） | 死的，五個月沒動 |
| (b) 我的 token／帳單為什麼變多 | :50–89 | 官方未回應，三種假說 | 08-25 後停 |
| (c) Opus 5 是不是變差了 | :91–123 | 全是主觀回報，官方零回應 | 每週有新貼文 |
| (d) 我釘得住版本／會不會被靜默換掉 | :127–140 | 釘不住，官方 not planned | 事件橫跨 2026-02～07 |

**該拆，拆成三頁＋一個封存：**
- **(d) 獨立成頁**——「我釘得住我選的模型嗎」是我會單獨問出口的問題，有自己的結論（釘不住）、自己的時序（四個機制橫跨六個月），而且 :138 已經被 `model-comparison` 當前提引用。它今天最不該待在一堆 Reddit 抱怨底下。
- **(a) 封存**——2026-04 那次已結案，留一句結論在母頁，其餘整段歸檔。
- **(c) 併進 `opus-5.md`「這些數字是誰量的」**——那節已經在做同一件事，而且做得更好；兩處並存正是本次矛盾的來源。
- **(b) 留在母頁**，母頁改名做「我感覺變笨了，怎麼查」的索引：頂部三行分流（是帳單問題→pricing／是版本問題→feature-radar／是模型問題→opus-5），中間放「怎麼自己量」，下面放尚未歸因的訊號。

母頁留下的必須是索引＋結論，不是目錄。
