# 評審：`wiki/topics/code-quality-decline.md`（第 12 波）

受評 `cqd-2026-09-07-proposal.md`＋`-proposal-map.md`＋`-draft.md`（磁碟現行檔）。行號＝檔案原始行號（含 frontmatter 24 行）。

**我自己跑過的**：去向表抽驗 12 段（§一全段、§三 L52／L61／L65／L85／L89、§四 L93／L107／L113、§五 L127／L133、§六 L152、§七 L159／L161、§八 L280／L300／L326）——**行號全部對得上，一段未錯**；跨頁行號 `claude-code:177/178/213`、`model-comparison:138`、`long-context-1m:69/90`、`opus-5:86/175`、`discussions` 第 6 列 :57／:325／:486、`index:28/97`、`pricing` 事故表 6 列，逐一開檔核對，**全部成立**。

**閘**：以 `check_reader_language.scan`／`check_cell_limits.scan` 掃 draft A 欄全部 23 個逐字塊（我自己的 harness，非設計者的）——**命中 0 筆／超限 0 筆**；注入 121 字元儲存格＋201 字元條列＋禁詞句後 **命中 4 筆／超限 2 筆**，還原歸零。設計者 §10 的自報屬實。

**懸置**：`pending_markers` 對 draft 逐字稿 → 2 筆、⟨Q-01⟩ 短標記與定義成對（`SHORT_RE` 抓到 `Q-01`、`QDEF_RE` 抓到定義行）、legacy 0；全庫 live 140 ＝ 基線 140，**零餘裕**，本案不動 ✓。

**錨點**：`ANCHORED_WIKILINK_RE` 對 `#2026-03～04` 的全形波浪成立，`_headings_of` 逐字比對 `## 2026-03～04` ✓；全庫 `code-quality-decline#` 入邊 **0**（設計者宣稱屬實）。

**check_hierarchy**：archive 三條（掛父／領域繼承 🌐／`resolved（封存頁）`）draft A-18 齊備；hub 不落後＝母頁 callout 09-03 ≥ 子樹最新新聞 05-29 ✓（`CALLOUT_DATE_RE` 吃得下 A-2 的格式，已實測）。

---

## 🔴 七條

### 🔴-1 主張表則數 6／4／3／1／1 算錯——正確是 6／3／3／1／1（表內 14 則）

我逐列重算 L97–L111 十五列。三個錯：`08-07「I defended Opus 5」不在本頁`（它在 `opus-5:187`／`:98`，設計者自己的註記行也承認取不到本頁出處）；`08-20「Claude is Losing Me」`（:106）**沒有被分進任何一列**；`08-28 nerf legal` 同時被算進「泛化變慢 6」與「A/B 測試 3」**雙計**。正確歸屬：泛化＝07-29 dumber、08-14×2、08-19 degraded、08-20 Losing Me、08-26 miss old（6）；過度自信＝07-29 tame、07-30 not as good、08-13 rage（3）；A/B＝08-22 digging、08-26 nerfd、08-28 nerf legal（3）；Sonar 1；二進位 1。**表內 14 ＋ 已證偽的 effort dial 1 ＝ 15。**

修訂（A-8 表第 2 列，與導言同批）：

```
| **Opus 5 過度自信、難以調教** | 3 | 2026-08-13「rage-inducing to use」 | 單一貼文（其中一則登上該週熱門榜）|
```

### 🔴-2 導言「四種主張」對 5 列、「全部是單一使用者觀感」被自己表上第 4／5 列推翻（同句副本在 A-4 第 3 列）

A-8 導言寫「**15 則**社群訊號——四種主張，全部是單一使用者觀感，沒有一則帶測試方法或數字」，但表是 **5 列**，第 4 列是第三方 benchmark、第 5 列是 GitHub issue #80988（已查證屬實）。這正是冷讀者抓到的頁內計數病（:52「九個獨立訊號」對 10 列）在新版重演，還多了一句被自己表格推翻的全稱句。A-4 第 3 列儲存格「十五則全是單一使用者觀感」同病。

修訂（A-8 導言整段取代）：

```
[[entities/opus-5]] 於 2026-07-24 上線後，本頁累積 **15 則**訊號：下表五種主張收 14 則，另一則（effort 旋鈕非單調）已證偽，見表下。收料起點是 Opus 5 的上線日，不是「這些抱怨都指向 Opus 5」——其中六則沒有指名模型版本。十四則裡十二則是單一使用者觀感，另兩則有外部依據（一則第三方基準待查證、一則 GitHub issue 已查證屬實）。逐則原文與 Opus 5 這個模型自己的官方基準見 [[entities/opus-5]]「這些數字是誰量的」；本頁只記它作為第三條退步線的狀態。
```

A-4 第 3 列第 4 欄同批改：`十五則裡十二則是單一使用者觀感，不足以據此換模型；逐則見 [[entities/opus-5]]「這些數字是誰量的」`

本條同時吃掉設計者 §12 自報的「節名圈住不指名模型的抱怨」——不必加第四條線，導言明說起點是時間不是歸因即可。

### 🔴-3 判準／上限／退場條文寫進頁面正文四處——第 6 波已立法，閘不擋

A-4 表下第 1 條（「三列不增減…日後真出現第四條線才加第四列」）、A-7 blockquote（「三列固定不增減…仍留在表上」）、A-8 blockquote（「上限 6 列…逾 90 天且無新事實，該列移出表格」）都是 B-2 規則檔的逐字副本。`check_reader_language` 對它們回 0（「上限／移出表格／不加列」不在 TERMS，我實測確認），只有評審擋得住。同病第 6 波（gov-policy 27 行）、第 7 波（biz）各中一次。

修訂：三處 blockquote 只留讀者語言那半句，條文移進 `%% %%`；A-4 表下第 1 條整條刪除（其讀者價值「一條線結案本身就是答案」已由第 1 列儲存格「已結案（#41930…）」呈現）。

```
> 資料截至 2026-09-07。「現在還在嗎」看的是官方說明與 GitHub issue 的開關狀態，不看討論熱度。
%% 維運備忘：上限 4 列、現有三列固定不移除、入口與退場判準見 .claude/rules/wiki-ingest-community.md「code-quality-decline 的三張表」第 1 條 %%
```

```
> 三種解釋互不排斥。有新證據時改寫的是格子，不是加一種解釋。
%% 維運備忘：三列固定不增減；被排除者改寫「已排除（日期＋依據）」仍留表，見規則檔第 3 條 %%
```

```
> 一種主張一列，同一批貼文不會在兩列各算一次。
%% 維運備忘：上限 6 列、新貼文只改最後動態與則數、逾 90 天無新事實即移出，逐則原文留時序，見規則檔第 4 條 %%
```

### 🔴-4 B-3 的 lint 步驟沒有觸發邊，而且那行 `gh` 指令跑不起來

兩個獨立錯。(a) B-3 把一個**主編親做**的步驟寫進 `wiki-ingest-community-lint.md`，該檔開頭寫著「由社群記者載入」——主編不會讀到，且 `.claude/commands/wiki-lint.md` 現有 5a–5l **沒有加新步**，整節等於無人執行（`wiki-ingest-commercial-lint.md` 5e 是正確先例：規則檔＋wiki-lint.md 同批登記）。(b) `gh issue view 41930 65687 77136 83510 --json state,updatedAt` 不成立——`gh issue view` 只吃**一個** issue，且本 repo 的 remote 是 `mandy82477/ObsidianLab`（我跑過 `git remote -v`），不加 `-R` 會查到錯的 repo。

修訂：B-3 的社群記者部分留在 `wiki-ingest-community-lint.md`，**另在 `.claude/commands/wiki-lint.md` 5l 之後新增 5m**：

    ### 5m. code-quality-decline 三條線 issue 狀態複查（主編親做）`[加入: 2026-09-07]`

    「三條線現在到哪」的「現在還在嗎」欄吃 GitHub issue 狀態，不在日報來源清單內，記者無 web 工具。逐一跑：

    for n in 41930 65687 77136 83510; do gh issue view $n -R anthropics/claude-code --json number,state,updatedAt; done

    與表上三列比對，不一致即改寫該格並更新表上方「資料截至」；任一列由「還在」翻「已結案」時，同批改寫 `## 摘要` 第一段並回報 index 鉤子。**⚠️ 雲端跳過**（egress 封鎖，同 5b／5c／5h），改寫成待辦交本機 `/weekly` 承接。

    **回報格式（納入步驟 8）：**
    code-quality-decline 三條線（5m）：N 列比對／M 列已改／資料截至 YYYY-MM-DD ／雲端 egress 封鎖，跳過

B-2 §1 第 4 點的「複查於 `/wiki-lint` 由主編執行（見 `wiki-ingest-community-lint.md`）」同批改指 `.claude/commands/wiki-lint.md` 5m。

### 🔴-5 分工只做了單邊：`entities/opus-5` 對本頁**零入邊**

我掃了 opus-5 全頁 24 個 wikilink 目標，**沒有一個指向 `topics/code-quality-decline`**。冷讀者判「分不出的一對」是本頁 L91 節 vs opus-5「這些數字是誰量的」；draft 在本頁側寫了分工句（A-8 導言、A-13），但從 opus-5 進來的讀者仍看不到本頁存在。單邊分工句解不掉「分不出」。跨維護者，只能走帳本。

修訂：B-6 加第 7 筆（兩個裁決點選哪一版都要開）。

```
| 7 | 模型 | `entities/opus-5` | 「這些數字是誰量的」表下條列缺一句出口：該頁收 Opus 5 這個模型的逐則觀感，「有沒有系統性退步」這條線的狀態（三條線、官方回應到哪、怎麼自己量）在 [[topics/code-quality-decline]]。該頁目前對本頁零連結，讀者從模型頁進來看不到那一題有家（2026-09-07 冷讀者 Q5）|
```

### 🔴-6 `community-tech-discussions:325` 逐字引用舊節名，改名後指空

`:325` 收斂結論末句寫「已與 [[topics/code-quality-decline]]**「Opus 5 上線後品質感知訊號群」**互相引用」。A-8 把節名改成「Opus 5 上線後品質觀感」，那句就指了一個不存在的節。設計者的 map §四只保護了 `discussions:486` 引用的 Token 節名（`:590` 也引同一節名，該節名不改，兩處皆安全），漏了 `:325`。discussions 是**同維護者**，屬「只改互指句」的合法範圍，與 B-7 同批。

修訂（`:325` 末句）：

```
已與 [[topics/code-quality-decline]]「Opus 5 上線後品質觀感」互相引用
```

### 🔴-7 規則檔 B-2 §3 指向一個頁面上不存在的節

B-2 §3 標題寫 `` `## 三種解釋，目前各自站在哪` ``，但 draft A-7 在頁面上把它做成**粗體行**，不是標題。我跑了 `table_census.py topics/code-quality-decline`：該表的「節」欄回報的是 `Token 消耗異常訊號群`（`_mechanism()` 取最近的 markdown 標題，不看粗體行）。條文指向不存在的節，是第 5 波（ai-agent-safety `## 事件記錄`）與第 6 波（gov-policy `## 目前局勢`）的同一種斷鏈。

修訂：A-7 的粗體行升為 h3，規則檔標題同批對齊。

- 頁面（A-7 第一行）：`### 三種解釋，目前各自站在哪`
- 規則檔（B-2 §3 標題）：``### 3. code-quality-decline 的 `### 三種解釋，目前各自站在哪`（覆寫式，三列固定不增減）``
- B-2 §6 末句「`## 三種解釋` 表第 3 列」同批改為 `### 三種解釋，目前各自站在哪`

---

## 🟡 十條

1. **callout 首句不是當日內容**：A-2 標日期 2026-09-03，內文卻完全沒講 09-03 發生什麼（`wiki-ingest-format.md`：callout 首句必須是當日新增的內容本身）。改：`> 09-03 全模型錯誤率升高、同日排除，屬服務穩定性事件不是退步。04 月那次官方認了也修了；06 月起的 token 異常與 07-25 起的 Opus 5 品質觀感，到今天都沒有官方說法。`（84 字元，仍在 callout 120 上限內）
2. **硬編碼鄰居頁的列數**：A-3 第 3 段與 A-10 第 4 條寫「四件計費爭議」——pricing 事故表現在 6 列（4 🔴／1 ⛔／1 ⚖️），會退場會補位，下週就可能錯。改「幾件未解的計費爭議」，不寫數。同段「09-14 起的週配額換軌」在 09-14 之後即成過去式，改「週配額換軌（09-14 生效）」。
3. **「週熱門」外洩詞只清了一半**：冷讀者第 4 條原句是「0 留言、無『週熱門』標記，score 不可信」，設計者 §6 只處置了「score 不可信」。A-8 表第 2、3 列硬度欄仍寫「其中一則週熱門」，A-15 也保留 `#### 2026-08-20（週熱門重浮上，原發時間較早）`。閘對它 0 命中（我實測）。硬度欄改「其中一則登上該週熱門榜」；時序標題屬既有存量，本波可不動但要記進 ledger。
4. **09-03 那筆事實更正的帳本只開了三分之一**：`grep "14:38"` 命中 `opus-5:175`、`fable-5:187`、`mythos:129` 三頁同一句「14:38 UTC 仍未標記已解決」，而 `sonnet-5:114` 已寫「同日解決」自相打架。三頁同屬模型記者，B-6 第 5 筆的 note 改成一次點名三頁。
5. **`boris-cherny:186` 沒有跟上結案**：A-10 第 5 條宣告「本站 2026-09-07 停止追蹤 50+ 逐項驗證」，人物頁 `:186` 仍是「2026-05-03，社群開發者開始逐一獨立驗證」的現在式敘事。跨維護者（人物記者），B-6 補一筆。
6. **`overview:77` 未列入主編同步清單**：現寫「Max 額度異常耗盡訊號群持續累積」，與新結論層（三條線）不同口徑。B-4 只涵蓋 index 與 radar。
7. **規模數字對不上**：用同一組去向 span 重算，我得約 **308 行／21,900 字元**，提案寫 318 行／20,846 字元（原始基線 28,410 字元我算得完全相同，差在增減量的估法）。兩者都是實作前估值，實作後必須以實檔重量再回填 ledger，不要照抄提案數字。
8. **A-13 最後一條四個裸連結沒有分工句**：導言承諾「相鄰的頁各答一題」，最後一行卻是四個裸連結。補一句：`以上四頁是本頁事實的上下游：選型與成本換算、1M 這個旋鈕、以及兩代舊模型自己的頁。`
9. **A-5 第 3 步與 `skill-interest-watch:144` 互斥**：本頁正文寫 CC-Canary 的判定值「可直接拿來當自己的判斷線」，tools 側的判斷卻是「『感覺變笨，想先量測歸因』目前沒有成熟到可推薦單一工具」；A-5 自己的 `%%` 備忘也寫「決策表暫無對應列」。補一句對齊：`本庫沒有把它列為推薦工具（社群工具目錄對這個症狀還沒有首選）——它給你的是自己跟自己比的一條線，不是外部基準。`
10. **B-2 §1 內部小矛盾**：同一節既寫「現有三列固定不移除，增減這三列屬使用者裁決事項」，又寫「上限 4 列、第 4 列適用退場」與判準句「三條全過才入表」，第 4 列到底要不要使用者裁決沒答案。補：`第 4 列的新增不需使用者裁決（判準三條全過即可），移除三條既有線才需要。`

---

## 兩個裁決點：我的判定

**裁決點 1（拆「模型釘選」子頁）——同意不拆，但理由要改寫。**

設計者的第一句理由站不住：「蒸餾後母頁只剩約 318 行、遠低於鬧鐘，所以『問題太多』不成立」——2026-09-07 使用者裁決明文把行數踢出拆分判準（「不按行數、月份、層切」），拿行數論證「問題不夠多」是同一個錯誤的鏡像。這句寫進 ledger 會污染下一波。

站得住的是另外兩句，我都核對過：子頁約 30 行、最後事件 2026-08-04，`wiki-ingest-format.md`「子頁 < 80 行且 60 天無新聞更新 → lint 提案併回」在 **2026-10-03** 成立，等於建一個下個月要提議併回的頁；回掃五處我逐一開檔確認存在（`claude-code:177/178`、`model-comparison:138`、`long-context-1m:69/90`），其中四處跨維護者只能走帳本，中間必有指路句指著舊節名的空窗。

更關鍵的是：**拆頁本來要解的問題已經被解掉了**。冷讀者的原話是「它躺在第 131 行」；draft A-3 第 2 段把「你釘不住你選的模型」升到摘要第二段、A-10 第 3 條再講一次、A-9 節名改成讀者句——可見度問題不需要用一個新頁面來解。

**裁決點 2（Opus 5 節併進 `entities/opus-5`）——同意不併，但必須配 🔴-5 的第 7 筆帳本，否則等於沒解。**

不併的機械理由成立且我驗過：併走會讓 ⟨Q-01⟩ 與其細節必須成對搬到另一頁，而全庫懸置 live 140 ＝ 基線 140、**零餘裕**，保命條款已四波四次命中在這一步；opus-5 的「五則」口徑與本頁 15 則要合併重算，那是模型記者的頁，本波動不了。

但設計者說「壓成主張表＋分工句已解掉兩頁打架」只對一半：本頁側寫了分工，opus-5 側對本頁零連結。冷讀者是從 index 分別進兩頁的，單邊分工句他看不到。第 7 筆帳本是這個裁決能成立的前提，不是附加項。

---

## 照順序執行（實作單）

先貼新表再砍舊節；每步的驗證指令與預期輸出寫在該步後面。全程 `PYTHONIOENCODING=utf-8`。

0. **記基準**：`python scripts/build_web.py 2>&1 | grep -c "WARN"` 與 `python scripts/check_pending_markers.py | tail -3`，兩個數字抄下來，第 3、7、12 步要比對。
1. **建 archive**：照 draft A-18 建 `wiki/topics/code-quality-decline-archive.md`，`## 2026-05` 與 `## 2026-03～04` 兩節先留空標題。驗：`python scripts/check_hierarchy.py` → 此時只准出現 `index 投影缺子頁`（第 9 步才綠），不得有其他新違規。
2. **搬原文**：母頁 L280–L298（05）、L300–L324（04）、L326–L329（03）**一字不動**照搬進 archive 對應 h2（04 在上、03 接在後）。驗：`python -c "print(len(open('wiki/topics/code-quality-decline-archive.md',encoding='utf-8').read()))"` → 應 ≥ 2,920（三段原文字元合計，我量得 1437＋1434＋49）。
3. **母頁時序**：以 A-16／A-17 兩段總結取代原三段；`## 時序` 標題正下方插入 A-14 的 `### 2026-09`。驗：`python scripts/build_web.py 2>&1 | grep -c "WARN"` → 不高於第 0 步基準。
4. **貼新結論層**：依序插入 A-4（blockquote 依 🔴-3 改寫、表下第 1 條刪除、第 3 列儲存格依 🔴-2）、A-5（含 🟡-9 那句）；**再**改 A-1 標頭、A-2 callout（依 🟡-1）、A-3 摘要（依 🟡-2）。
5. **改三張既有表**：A-6（導言、第 4 欄改名與逐列新值、表下兩條、圖例、末段）、A-7（**升為 `### 三種解釋，目前各自站在哪`**，見 🔴-7；blockquote 依 🔴-3）、A-8（導言用 🔴-2 逐字、表第 2 列則數改 3、blockquote 依 🔴-3、硬度欄依 🟡-3）。
6. **改其餘節**：A-9、A-10（第 4 條去「四件」）、A-11、A-12、A-13（末條補 🟡-8）、A-15 六處措辭。驗：`python scripts/check_cell_limits.py --page code-quality-decline`、`python scripts/check_reader_language.py --page code-quality-decline`，兩支末行原樣抄進回報；**驗紅**：暫貼一行 `| a | <121 個字> |` 與一條 `- <201 個字>`，重跑應各報 1 筆新增，刪回後重跑歸零。
7. **懸置對帳**：`python scripts/check_pending_markers.py` → 末行應為 `懸置標記 140 筆（基線 140，未低於）`。**若低於 140 立刻停手**——⟨Q-01⟩ 或 L140 那筆被溶掉了。
8. **規則檔與 lint 步驟**：B-1、B-2（§3 標題依 🔴-7、§1 補 🟡-10、§6 末句改節名）、B-3 搬進 `wiki-ingest-community-lint.md` ＋ `.claude/commands/wiki-lint.md` 新增 5m（🔴-4 逐字）。驗：`python scripts/check_rules.py` → exit 0；`python scripts/table_census.py topics/code-quality-decline` → 4 張表、機制欄**全部**「有（wiki-ingest-community.md）」（改動前是 3 張全「無」，我跑過）。
9. **投影與 frontmatter**：`python scripts/gen_wiki_frontmatter.py`，再 `python scripts/check_hierarchy.py` → `狀態：✅ 通過`。
10. **鄰居與帳本**：B-7（discussions 第 6 列官方欄）＋🔴-6（discussions `:325` 末句節名）同維護者直接改；`pending_handoffs.py open` 開**八筆**（B-6 六筆，第 5 筆 note 依 🟡-4 改成三頁；加 🔴-5 第 7 筆；加 🟡-5 boris-cherny 一筆）。驗：`python scripts/pending_handoffs.py list` → 新增 8 筆，目標分佈 功能 4／模型 2／商業 1／人物 1。
11. **主編文字**（實作者不寫入 index／radar／overview）：B-4、B-5 原文交主 session，另加 🟡-6 的 `overview:77` 一句。
12. **全套**：`python scripts/run_tests.py` → exit 0；`python scripts/build_web.py` WARN 數不高於第 0 步基準。

**設計者第二輪必須親改的**：🔴-1（則數）、🔴-2（導言逐字）、🔴-3（三處 blockquote 逐字）、🔴-4（5m 全文與規則檔落點）、🔴-7（h3 升格與規則檔對齊）。🔴-5、🔴-6 與十條 🟡 是實作者照本單即可執行的改寫。
