# 第 15 波評審：topics/community-tech-tools

對象：提案三件（以磁碟現行檔為準，對象頁 315 行）。行號＝各該檔原始行號。機械項全部自跑（指令與輸出見文末），提案宣稱的計數一律重算不採信。
主 session 已定的裁決處置（不併、index L27 不改、額度告警開列、不拆頁、SIW 不動、改名照 verified）為前提，本檔只審「在這些前提下提案對不對、做得到不」。
判定：**🔴 4 條、🟡 13 條**。結論：**放行進實作，但 B 區（規則檔）必須與頁面同批做**——🔴-1 若不補，這一波等於把三條守則句從頁面刪掉而不搬家。

---

## 〇、代判數字重算（不採信提案宣稱）

| 提案宣稱 | 我算的 | 判定 |
|---|---|---|
| 目錄現況 152 列 | `## 工具目錄` 區內非分隔列 −表頭＝**152** | ✅ |
| 移除 43 列、清單無重複 | 清單 **43** 筆、無重複；全庫「整列無任何連結」的目錄列實際 **46** 筆，差額恰為 L270／L275／L279（兩筆次選豁免＋graphify 補連結） | ✅ **逐列精準，無誤殺也無漏網** |
| 152 − 43 ＋ 3 ＝ 112 | 套 draft 後實測 `tools_catalog_rows()` 等價算法＝**112** | ✅ |
| 倒序違規 6 處 | 機械掃「首次出現」欄：**6**（L158／L188／L190／L195／L207／L209） | ✅ 行號全中 |
| 類型實用 12 值 | 目錄第二欄去重＝**12**（含 `模型路由` L213、`UI 工具` L214） | ✅ |
| 43／43 另有出處 | 逐列全庫 grep：42 列直接命中；**L257「/qu /ans 跨 session 插件」以原字串 0 命中**，改以別名（`/qu /ans`）才命中 `claude-code:827`、`patterns-archive:909`、`log.md:4127` | ✅（結論成立，但機械核對只有換字串才重現） |
| 改後 281 行 | 套 draft 實測 **280 行** | ⚠️ 差 1（🟡-12） |
| 全頁「存量盤點／本庫／pipeline 進料」皆 0 | 套 A-6b 後實測 **0／0／0** | ✅ |
| 懸置 114 筆、基線 106、本頁 `iter_pending` 0 筆 | 實測 114／106／本頁 **0** | ✅ |
| 舊字樣 41 → 40 | 本頁 `iter_legacy` 只有 **1 筆**（L138 圖例，`exempt='legend'`）；⟨Q-01⟩ 根本不計。降級後全庫仍 **41** | ❌ **代判錯**（🟡-5） |
| 決策表新列抄進 SIW 會不會爆格 | 十列剝 URL 後最長格 **92 字元**（上限 120），SIW 逐字抄錄同樣安全 | ✅ |
| 閘：cell 新增 0／reader 0／tools_page `[]` | 我在 scratchpad 套 draft 重跑：cell 命中 3 筆**全在基線內、新增 0**；reader_language **0**；`check()`＝`[]`、`check_spokes()`＝`[]` | ✅ **三項全數重現** |

---

## 一、明天的維護者

### 🔴-1 實作單（proposal §9 十步）沒有任何一步做 B 區——三條守則句會被刪掉而不搬家

A-4／A-5 把 L50「本表每週複查一次」、L64「首選只在出現新證據時更換」、L138 的 ✅ 判準、43 列的退場依據全部從頁面拿掉，理由都是「條文住 `weekly.md`」。但 §9 的十步只動頁面與一條同維護者回掃，**B-1～B-5 一步都沒有**；§9 第 10 步也只寫轉知與 log。照單做完＝條文人間蒸發。
**修法（插在 §9 第 1 步之前，成為新第 0 步）：**
```
0. 先改 `.claude/reporter-rules/community/weekly.md`：B-1（L21 之後新增第四條＋L19 之後補括號）、B-2（策展規則第 4 條之後新增 4b／4c）、B-3（取代 L40 末句）、B-4（L153 節名與 L155 句中「3 題」→「4 題」，第 3 題之後補第 4 題）、B-5（L16 紀律欄末補自查句）。可驗：`python scripts/check_rules.py` 仍 ✅、`grep -c "4b\." .claude/reporter-rules/community/weekly.md` ＝ 1。
```

### 🔴-2 B-1 的「新證據」界定照字面會要求換掉 Omar，與本案「0 換首選」自相矛盾

B-1 補的括號逐字寫：「**專案自述或 README 不再宣稱它解這個症狀**才算〔否定證據〕——2026-09-22 的 Omar 即屬此類」。而 `weekly.md:43`（策展步驟 5）寫「既有首選**出現否定證據**時，才動決策表對應列」。兩句串起來＝Omar 該換首選；提案 §2 卻把「0 換」當本案立場。同一份規則檔兩句互打，下一個記者兩種做法都有根據。
**修法（取代 draft L201 整個括號）：**
```
  （改名、轉手、換網域、星數增減、長期沒人回訪**都不算**否定證據，只更新描述與連結。一手自述變動要分兩種：**自述已不再解這個症狀**＝否定證據，照上面第一條換首選；**自述仍解同一症狀、只是規模或形態的宣稱變了**＝不換首選，但把證據等級降到新宣稱撐得起的那一級，並在推薦細節寫明原宣稱哪一句已不在一手。2026-09-22 的 Omar 屬後者：「TUI 管 100 個 agent」在現版 README 已不存在，🟢 降 🟡，首選不動。）
```

### 🔴-3 回掃清單漏了 `data/skill_interest_watch.json:121`——改名隔天就被機器頁重印成過期字串

該檔 `orchestration` 類的 `caveat` 逐字是：「同名提醒：決策表首選 Harness＝frenchie4111/harness（多 worktree 並行管理）；榜上的 revfactory/harness…」。`skill_interest_snapshot.py:249–250` 每天把它原樣印進 SIW（現行 `skill-interest-watch.md:196`），而 `revfactory/harness` 仍在榜（`:202`，9,045★）。本案砍掉頁面 L71 的同名提醒、把事實移進 draft L67，卻沒動這個設定檔——`weekly.md:29` 明訂「你維護的是設定檔的 `tools_symptom`／`tools_note`」，這正是社群記者的檔。提案 §7 兩張回掃表都沒有它。
**修法（`data/skill_interest_watch.json` 第 121 行 `caveat` 值整串替換）：**
```
同名提醒：決策表首選已改名 ness（ness-dev/ness，原名 Harness，多 worktree 並行管理）；榜上的 revfactory/harness（設計 agent team 的 meta-skill）不是同一個專案，裝前認清 owner。
```

### 🟡-1 omnigent 的「無第三方實測回報」判斷在 draft 憑空消失

map L46 承諾「多 agent 互踩」條改寫後保留 omnigent 數字與防刷結論，但 draft L67 整條沒有 omnigent 三個字（全檔只剩決策表第三欄裸名與目錄列 L138）。消失的是原 L72 的判斷句「無第三方實測回報，列次選、證據 ⚪」——正是審計卡校準句 #1（第三欄無欄位契約）要補的那種東西。
**修法（接在 draft L67 句末）：** `次選 omnigent 09-22 查得 10,150★／1,609 forks（15.9%，防刷結論不變），但仍沒有任何第三方實測回報，只有規模。`

### 🟡-2 §1.4「依據不是新規則」不成立，且 4b 自己規定的前置步驟本波沒做

`weekly.md:39` 是策展步驟 2（萃取**新**工具的門檻）底下的排除條款，不是退場條文；退場條文是 L42（❌／>30 天 ⏳）。把它回頭套到存量列本來就是新規則——B-2 4b 自己也寫「**回頭適用於存量列**」。另外 4b 逐字要求「先花一次查證補上連結；補不上就移除」，本波 43 列只做了 graphify 一列的補連結。
**修法（二擇一，建議前者）：** §1.4 把「依據不是新規則，是現行 `weekly.md:39` 的排除句」改成「依據是本波同批立的 `weekly.md` 4b 條，它把現行 L39 的排除條款回頭適用到存量列」；並在 4b 的「補不上就移除」後補一句 `存量首次清理得批次處理，逐列確認另有出處即可，不必逐列補查`。

### 🟡-3 ness 仍標 ⌨️ CLI，與同頁自己的細節條打架

draft L53 首選徽章是 ⌨️，draft L67 卻寫「自述已從 CLI 變成『給 agent 用的 IDE』」；verified 第六節 L76 逐字把這一對列為同頁不一致。圖例對 🖥️ 的註記（注意平台鎖定）正是讀者需要的訊號。
**修法：** draft L53 第二格 `⌨️ [**ness**]` → `🖥️ [**ness**]`，並在 draft L67 句首補 `（徽章已從 ⌨️ 改為 🖥️）`。

### 🟡-4 第三欄仍無欄位契約，B 區沒補（審計卡校準句 #1、冷讀者「最想改」第 1 條）

本案給第三欄補了連結（做得對），但 `weekly.md:13` 的欄位紀律仍只管第二欄與第四欄。下一個記者照規則寫，第三欄會再長回裸名字。
**修法（B-1 之後另加一條，放在 `weekly.md:13` 那一列的紀律欄末）：** `；第三欄的每個工具比照第二欄帶連結，並在推薦細節至少有一句寫它的證據強度——對已做過前置工作的讀者，第三欄才是主答案`

---

## 二、機器

### 已驗過、確認不會咬的（實跑，非讀碼推論）

- `check_tools_page.py`：`DATE_RE`（L28）的 `\d{2}-\d{2}\b` 吃得下 `判 04-29｜查 09-22`；`check()` 與 `check_spokes()` 套 draft 後皆回 `[]`。**「數字帶日期」對雙日期格式判為通過。**
- 三個凍結錨點（`review-registry.json:101–119`：`| 首次出現 |`／`## 我卡在這裡`／`先裝這個`）本案一項未動；`skill_interest_snapshot.py:147／167／248` 三處 regex 與寫死四欄表頭同樣未動；`legend` 抽取靠 `**圖例**` 開頭（L171），draft L59 保留。
- `skill_interest_watch.json` 五個 `tools_symptom` 字串一字未改；**新列不需要新增 `tools_symptom`**——沒有任何類別對應「額度」，`check_spokes` 實測 `[]`，提案 §5 的說法成立。
- `check_pending_markers.py`：本頁 `iter_pending` 0 筆，刪 43 列動不到 114／106 的總數閘（該閘只擋「低於基線」）。
- `check_rules.py` 現況 ✅；涉及 `community/weekly.md` 的四組 `sync_pairs`（`每日 ingest 不`／`已脫離每日 ingest`＋`策展規則`／`community-la…`／蒸餾三側）B 區全部不觸及。

### 🟡-5 「舊字樣 41 → 40」是錯的代判

見第〇節。⟨Q-01⟩ 是 `⟨X-nn⟩ 短標記`，2026-09-06 起已由 `pending_markers._exempt()` 排除在舊字樣之外（`pending-legacy-baseline.json` 的 `_note` 逐字寫）。本頁唯一一筆 legacy 是 L138 圖例的「存疑」，也是豁免。實作者若拿 40 當驗收條件會以為自己做錯。**修法：** map L6 與 proposal §6 表第四列的「舊字樣 41 → 40」刪掉，改寫 `⟨Q-01⟩ 屬 ⟨X-nn⟩ 短標記，2026-09-06 起即不計入舊字樣；降級前後全庫都是 41 筆`。

---

## 三、冷讀者（拿原四題對 draft 走一遍）

Q1（worktree 之後）**改善**（第三欄改寫＋Merge Queue 帶連結，推錯格子的根因解掉）；Q3（交付架構圖）**不退化**（archify 那格與安裝指令原樣，`user-query` 改成讀者語言）；Q4（判斷多舊）**解掉**（L50／L64 互打消失）。Q2（額度）**半解**——以下三條。

### 🔴-4 新列的證據格與新圖例互相拆台，且漏掉一個已知的壞消息

draft L59 圖例逐字定義「**查**＝最近一次確認這個專案**還在不在**、多大的那天」。新列卻寫 `⚪（查 09-22，8,713★）`：①只有「查」沒有「判」，與圖例的「括號裡兩個日期」不符；②09-22 查到的重大事實「2026-07-05 後未更新（79 天）」只寫在推薦細節，格子裡讀不到——讀者只看表就會以為它活著。這是 `不虛報優先於行動導向` 的直接違反，也是本波整個「裝時鐘」推薦案自己要治的病。
**修法（取代 draft L49 第四格，剝 URL 後 32 字元，遠低於 120）：**
```
⚪（判 09-22｜查 09-22，8,713★、07-05 後未更新）
```

### 🟡-6 首選選 Maciek-roboblog 站得住，但次選欄留下一個空洞：跨平台又還活著的那個候選沒進表

`weekly.md` 沒有任何條文禁止 ⚪ 或停更專案當首選（L13／L19／L21 只管首選唯一、證據帶日、換的門檻），所以「8,713★／79 天無 push」當首選**不違規**，加上 🔴-4 的誠實標註後可以接受。問題在第三欄：兩個次選是 `usage-monitor-for-claude`（僅 Windows）與 `CCLimitPing`（解的是自動續跑，不是提醒）。verified 第四節 L55 另有 `bozdemir/claude-usage-widget` 53★／push 09-21／**跨平台／burn alerts**——非 Windows 讀者想要一個「還在更新的告警器」時，這一列答不出來。
**修法（接在 draft L49 第三格末）：** `；要跨平台又要作者還在更新 → ⌨️ [claude-usage-widget](https://github.com/bozdemir/claude-usage-widget)（53★，09-21 仍在更新）`，並在 draft L70 三工具那條末補 `另有 bozdemir/claude-usage-widget 53★、09-21 仍在更新、跨平台，規模最小但最新。`

### 🟡-7 冷讀者自己撿到的那一列 `agent-baton` 全程沒被提到

冷讀者 Q2 逐字寫「這正是我要的東西，而且只有這一列是」，指的是本頁 L236 `agent-baton`（Reddit 一手、hook 在觸及上限前主動告警、⚡、有連結、不在移除清單、改版後仍留在目錄）。新列三個工具沒有它，推薦細節也沒解釋為什麼不選它。讀者捲到目錄還是會撞見，然後不知道該信哪個。
**修法（接在 draft L70 條末）：** `頁面目錄裡還有一個 2026-05-18 收錄的 agent-baton（Reddit 貼文，宣稱在觸及上限前主動告警並轉移工作），只有二手貼文、09-22 查不到可裝的頁，所以沒放進上面三格。`

### 🟡-8 官方出口指 [[feature-radar]]，那頁今天答不出這題

實測 grep `wiki/feature-radar.md`：有記憶體用量警示（L127／L301）、團隊用量報表（L210／L306），**沒有任何一條是 `/usage`、狀態列 `used_percentage` 或主動額度告警**。`weekly.md:20` 的三分流裡 feature-radar 那條的前提是「官方**已內建**」——本題官方是 #13585 OPEN ＋ #65292 not_planned，不適用。提案 §7 第 1 筆轉知已承認這個缺口，但 draft L69 仍把讀者送過去。
**修法（取代 draft L69 末句）：** `官方到 09-22 沒有主動告警這個功能，[[feature-radar]] 上也還沒有對應條目；有了會記在那裡。`

### 🟡-9 L50 的指路句原樣留著，而冷讀者已證實它是死路

draft L44 保留「工具出資安事故、棄坑、下架這種急事，見 [[entities/claude-code]] 與 [[topics/ai-agent-safety]]」。冷讀者逐字記錄：照這句開 `claude-code.md`，關於 Harness 的唯一內容是 `:856` 一行「04-29：…社群工具 Cockpit／Harness／CodeThis／Claude Exporter 發布」——與原判定日同一天同一件事。審計卡校準句 #3 已把「指路句要跟著走一跳」列為下波必加。
**修法（取代 draft L44 後半句）：** `工具出資安事故、棄坑、下架這種急事，會寫進該列的證據欄與下面的推薦細節；全站層級的安全事件另見 [[topics/ai-agent-safety]]。`（拿掉 `[[entities/claude-code]]` 這一跳）

---

## 四、治理

### 去向表抽驗（開原頁核行號，8 段：摘要、我卡在這裡、推薦細節、副作用節、速查、指標說明、目錄、收錄註記／懸置）

| 段 | 原文關鍵事實 | draft 有沒有 | 判 |
|---|---|---|---|
| 摘要 L44 | 四條出口（guide／patterns／discussions／feature-radar） | 四條全在，另加 OCG 與 SIW | ✅ |
| 我卡在這裡 L54–L62 | 九個症狀句原文 | 逐字比對，**九句一字未改** | ✅ |
| 推薦細節 L68／L69／L70 | 三條長條列（242／239／275 字元，在 cell 基線內） | **逐字原樣**（實測指紋仍命中基線） | ✅ |
| 推薦細節 L71 同名提醒 | `revfactory/harness` 仍在、與首選不同 | 接進 draft L67 | ✅（但設定檔沒跟，見 🔴-3） |
| 推薦細節 L72 | omnigent 星數＋防刷＋「無第三方實測」 | 數字進目錄列；**「無第三方實測」消失** | ❌ 🟡-1 |
| 副作用節 L79–L82 | 四個現象名＋`recap`／`modularity plugin`／`Mneme` | draft L78–L79 七個名字一個不少 | ✅ |
| 指標說明 L137–L140 | 證據／採用／類型／入選標準四列 | 四列都在，類型補到 12 值 | ✅ |
| 目錄 L212／L124 | Geosql「4 倍」的指路 | A-7 同批改兩處，⟨Q-01⟩ 兩條來源連結與機制句一字不失 | ✅ |
| 收錄註記 L301–L303 | 兩筆與表內「見收錄註記」成對 | 留原處，僅改「同類存量盤點案例」用語 | ✅ |

**結論：一行憑空消失的只有 omnigent 那半句（🟡-1）。其餘七段逐字對得上。**

### 〔併〕分岔自洽

draft 正文（A-3 L32、A-8 L180）照保守預設寫，〔併〕-1／-2 只替換末句與參考來源那一條，**正文沒有任何一句預設會併**；map 末節的「其餘 313 行完全相同」與我逐行比對一致。裁決點乾淨，可以直接照保守案實作。

### 規則檔改動位置

- B-1：`weekly.md` L21 之後（首選維護鐵則第四條）＋L19 之後補括號 → 位置對；內容見 🔴-2。
- B-2 4b／4c：策展規則第 4 條（L42）之後 → 位置對。4c 的 `⚡ >120 天＋news_mentions 零命中` 與第 4 條的 `⏳ >30 天` 分管不同符號，**不矛盾**；`news_mentions.py` 的用法（≥2 別名、`--since`）與 L42 現行指令一致。
- B-3：宣稱「取代第 3 條末的『廣泛採用才給 ✅』一句」——L40 那一句與前半「採用預設 ⚡，score ≥ 100 或評論 ≥ 20 維持 ⚡」在**同一行**。照字面取代會留下半句殘骸。🟡-10 **修法：** 改寫成「L40 整行改為：`- **欄位** …；採用預設 ⚡，score ≥ 100 或評論 ≥ 20 維持 ⚡`，並把 B-3 的 block 作為其下一層子條列插入」。
- B-4：只說改節名（L153）。L155 正文同樣逐字寫「重跑以下 3 題」。🟡-11 **修法：** B-4 的指示補一句 `L155 的「以下 3 題」一併改為「以下 4 題」`。
- B-5：`weekly.md` L16 紀律欄末 → 位置對。
- `check_rules.py` 實跑 ✅，四組相關 `sync_pairs` 的 pattern 在 B 區改動後全部仍命中。

### 🟡-12 改後行數實測 280 不是 281；🟡-13 A-6 三列「只換連結」不是可直接貼上的整列

draft L132–L134 寫成 `| [**Frugal Tokens**](url) |   ← 只換連結，其餘四格不動（L201）` 的示意形，實作者得自己拼回整列。**修法：** 改成完整五格整列（只換第一格 URL，其餘四格從原頁 L201／L202／L213 逐字複製）。

---

## 五、照順序執行（實作單）

實作者只看這張單即可，不必回頭讀提案。每步做完跑該步的「驗」。

0. **改 `.claude/reporter-rules/community/weekly.md`**：B-1（L21 後新增第四條；L19 後補括號，括號逐字用 🔴-2 的版本）、B-1 追加（L13 紀律欄末補 🟡-4 的第三欄契約句）、B-2（L42 後新增 4b／4c，4b 末句補 🟡-2 的批次處理句）、B-3（照 🟡-10 拆成整行＋子條列）、B-4（L153 節名與 L155 句中「3 題」→「4 題」，第 3 題後補第 4 題）、B-5（L16 紀律欄末）。驗：`python scripts/check_rules.py` ✅。
1. **改 `data/skill_interest_watch.json:121` 的 `caveat`**，逐字用 🔴-3 的替換字串。驗：`grep -c frenchie4111 data/skill_interest_watch.json` ＝ 0。
2. **標頭 L33–L34** 換 A-1（兩個日期都填 2026-09-22）；**callout L36–L37** 換 A-2；**摘要 L43–L44** 換 A-3，其中 A-3 保守版（不採〔併〕）。驗：全頁 `grep -n "本週策展"` 0 命中。
3. **`## 我卡在這裡` 整節（L48–L84）換成 A-4**，並套三處改正：第 49 行證據格改 🔴-4 的版本；第 49 行第三格末補 🟡-6 的 bozdemir；第 53 行徽章 ⌨️ → 🖥️（🟡-3）。**症狀欄九句一字不改**，表頭四欄與分隔列不動。驗：`python scripts/check_tools_page.py` exit 0。
4. **同一節的推薦細節補三句**：L67 補 🟡-1 的 omnigent 句與 🟡-3 的徽章註；L69 末句換 🟡-8 的版本；L70 末補 🟡-6 與 🟡-7 兩句。驗：`python scripts/check_cell_limits.py --page topics/community-tech-tools` 無新增超限。
5. **表上引言（原 L50 位置）** 照 🟡-9 改寫，拿掉 `[[entities/claude-code]]` 那一跳。驗：該行只剩一個 wikilink。
6. **速查**：移除 L108 awesome-ux-skills 整列；L96／L97／L98／L99 的**第二格**換成 A-5(1) 四句（第一、三格不動，`npx skills add tt-a1i/archify -g` 保留）；L129 換 A-5(3)；L124 末補「（見下方細節）」。驗：該組 4 列、`grep -c user-query` 0。
7. **指標說明 L137–L140** 四列換 A-5(4)。驗：類型列 12 值。
8. **工具目錄**，依序做：①`## 工具目錄` 與表頭之間插 A-5(5) 一句；②移除 43 列（行號清單見 map L121，逐列先確認該列確實無任何 `](` 或 `http` 再刪）；③套 10 列改寫（L205／L208／L274／L275／L286／L270／L279 用 draft L138–L144 整列；L201／L202／L213 只換第一格 URL，其餘四格逐字保留——見 🟡-13）；④在表最上方插 A-5(6) 三列新列；⑤全表依「首次出現」欄由新到舊重排。驗：目錄列數 **112**；「首次出現」欄由上到下遞減無違規（原有 6 處違規全消）。
9. **A-6b 八處內部用語**逐字替換。驗：全頁 `存量盤點`／`本庫`／`pipeline 進料` 各 0 次。
10. **L305–L306** 換 A-7（`**懸置細節**` → `**一則細節**`，⟨Q-01⟩ 降為普通散文）；**L212** 的「見下方懸置細節 ⟨Q-01⟩」→「見下方 Geosql 那則細節」。驗：全頁 `⟨Q-01⟩` 0 命中；`python scripts/check_pending_markers.py` 仍印「114 筆（基線 106，未低於）」。
11. **`## 參考來源`** 補 A-8 兩條（保守版）。
12. **同維護者回掃**：`community-large-codebase-workflow.md:63`，「（首選 Harness）」→「（首選 ness，原名 Harness）」，Omar 不動，**只改該句不重寫頁**。驗：`python scripts/check_tools_page.py` 的 spoke 對帳仍 ✅。
13. **跨維護者轉知 3 筆**（`python scripts/pending_handoffs.py open --from 社群 --to 功能` ×2、`--to 主編` ×1，內容照 proposal §7）。
14. **`wiki/log.md` append**：43 列移除的名單與理由、⟨Q-01⟩ 降級、新列開列依據（#13585 OPEN／👍 124 ＋ #65292 not_planned，**兩筆官方 issue 已達 `weekly.md:43` 的 ≥2 筆獨立需求證據；冷讀者是內部驗收不計入需求證據**）、本波未換首選與 Omar 🟢→🟡 的理由。
15. **跑全部閘並把最後一行原樣抄進回報**：
```
python scripts/check_tools_page.py
python scripts/check_cell_limits.py --page topics/community-tech-tools
python scripts/check_reader_language.py --page topics/community-tech-tools
python scripts/check_pending_markers.py
python scripts/gen_wiki_frontmatter.py
python scripts/check_hierarchy.py
python scripts/check_rules.py
python scripts/build_web.py
python scripts/run_tests.py
```
預期：`check_tools_page` 狀態 ✅；cell 閘 `WARN: 存量基線內 4 筆／1 頁` ＋ `OK: 無新增超限`（⟨Q-01⟩ 降級後可能降為 3 筆，同樣算過）；reader 閘 `OK: 無新增命中`；pending `114 筆（基線 106，未低於）`；`check_rules` ✅；`run_tests.py` exit 0。**任一項與預期不符就停下回報，不得自行改基線檔。**

---

## 文末：機械項怎麼跑的

機械項全部自跑：目錄列數／無連結列／倒序／類型值以一次性腳本掃 L148–L299；閘重跑是在 scratchpad 依 draft A-1～A-8＋A-6b 重建整頁（280 行、目錄 112 列），直接呼叫 `check_cell_limits.scan()`／`check_reader_language.scan()`／`check_tools_page.check()`／`check_spokes()` 對副本跑，**wiki 目錄未被寫入**。動手前基線四行原樣見 proposal §6，我實跑後完全一致。

---

## 實作複核（2026-09-22）

磁碟現行檔為準（未 commit）。對照 draft L275–L310「實作單（最終版）」15 步。**結論：放行**——15 步全部照做，兩條殘留缺陷都源自 draft 自己的指示不全（實作者照單做沒有錯），修法各一行，建議 commit 前順手做完。

### 逐步對照

| 步 | 判 | 證據 |
|---|---|---|
| 0 weekly.md B-1～B-5 | **照做（1 處措辭偏離）** | diff 五塊全在且逐字：L13 紀律欄末補第三欄契約、L19 後補 🔴-2 改寫版括號、L16 補倒序自查、L21 後新增「證據欄帶兩個日期」、L40 拆行＋`✅` 子條列、L42 後 4b／4c（4b 含批次處理句與「與第 4 條分工」句、寫「152 → 113」）、L156 節名與第 4 題。**偏離**：節名改成「4 **跳**自檢題」而本文仍寫「3 跳內須得到」（見最後一批 #2） |
| 1 caveat 替換 | 照做 | `json.load()` 通過；`grep -c frenchie4111` ＝ **0**；caveat 逐字等於 🔴-3 給的字串 |
| 2 標頭／callout／摘要 | 照做 | `grep -n 本週策展` **0 命中**；摘要為 A-3 保守版（六條出口，不採〔併〕） |
| 3 決策表整節 | **照做（逐字）** | 以 difflib 對 draft L42–L82 比整節：**唯一差異是節尾的 `---` 分隔線**，其餘 34 行一字不差。十列症狀欄九句未改；新列證據格＝`⚪（判 09-22｜查 09-22，8,713★、07-05 後未更新）`；ness 徽章 🖥️；第三欄含 bozdemir |
| 4 推薦細節 14 條 | 照做 | 實數 **14** 條；L68／L69／L70 三條指紋仍命中 `cell-limit-baseline.json`（閘只剩 3 筆存量、新增 0） |
| 5 速查 | 照做 | awesome-ux-skills 全頁 **0 命中**；四組各 **4 列**；A-5(1) 四句逐字在頁上；`npx skills add tt-a1i/archify -g` 保留 |
| 6 指標說明 | 照做 | 類型列 12 值（含 `模型路由`／`UI 工具`）；`✅` 補判準；入選標準改寫 |
| 7 工具目錄 | 照做 | 目錄 **113 列**；倒序違規 **0**（原 6 處全消）；無連結列只剩 claudely／Caliber 兩筆（次選豁免，簡介末已標查不到公開頁） |
| 8 A-6b 八處用語 | 照做 | 全頁 `存量盤點`／`本庫`／`pipeline 進料` 各 **0** 次 |
| 9 ⟨Q-01⟩ 降級 | **照做（draft 漏指一格）** | `⟨Q-01⟩` 全頁 0、`**一則細節**` 在 L271、兩條來源連結與機制句一字不失；L221 目錄列已改「見下方 Geosql 那則細節」。**但 L127 速查列第二格仍寫「機制已查證見懸置細節」**——draft A-7 只指定第三格（見最後一批 #1） |
| 10 參考來源 | 照做 | 六條（補 `skill-interest-watch`、`community-large-codebase-workflow`，保守版措辭） |
| 11 同維護者回掃 | 照做 | `community-large-codebase-workflow.md:63` ＝「（首選 ness，原名 Harness）」，Omar 未動，只改該句 |
| 12 轉知 3 筆 | **照做**（第 3 筆已由他人落地） | `data/pending-handoffs.jsonl` 新增 H-40b52e（feature-radar）、H-0d154f（OCG:78）；第 3 筆 `index.md:27` 磁碟上已補「額度快用完想被提醒」——**不算漏做** |
| 13 log.md | 照做 | 新增條目五項齊全；43 個名字逐一比對磁碟實際移除清單，**43／43 完全一致**，無多列無少列 |
| 14 七道閘 | 照做 | 全綠，最後一行見下 |
| 15 完工規格 | 照做 | **283 行、目錄 113 列、決策表 10 列、推薦細節 14 條**，四項全中 |

### 抽驗 (b)：43 列移除名單

以 `git show HEAD:` 取改前檔逐列比對：計畫 43 名 vs 磁碟實際消失 43 名（表面差 7 筆是 L201／L202／L205／L208／L213／L274／L275／L286 換連結導致名字字串改變，非移除），**多砍 0、漏砍 0**；`wiki/log.md` 末則名單與磁碟一致。graphify 已補連結留表，claudely／Caliber 依 `weekly.md:42` 次選豁免留表。

### `gen_wiki_frontmatter.py` / `build_web.py` 的 115 檔變動：在範圍內，無副作用

兩支都在實作單第 14 步的閘序列裡。逐檔驗證：`wiki/**.md` 的 119 個變動檔中，**只有 4 檔有正文變動**（本頁、`index.md`、`log.md`、`community-large-codebase-workflow.md`，全是計畫內），其餘 115 檔的 diff 全部是 `days_since_news` / `days_since_news_subtree` / `inbound_links` 重算（日期跨天所致）。**SIW 頁只動 frontmatter 兩欄**（`days_since_news` 0→1、`inbound_links` 7→8——本頁新增出邊所致），**body 未被重算**，決策表副本照設計由隔天 daily snapshot 自然跟上，`skill-interest-watch.md` 未被手改。`web_reader/` 的變動是 build_web 正常產物。

### 七道閘（最後一行原樣抄）

```
狀態：✅ 數字皆帶日期、首選皆唯一、全站 🧰 spoke 症狀句對帳通過
OK: 字元上限機械閘 — 無新增超限                  （WARN: 存量基線內 3 筆／1 頁，原 4 筆）
OK: 讀者語言閘 — 無新增命中                      （WARN: 存量基線內 0 筆／0 頁）
狀態：✅ 懸置標記語法檢查通過                     （114 筆／基線 106；舊字樣 41 筆／基線 42，如評審預判未動）
狀態：✅ 全部確定性檢查通過                       （check_rules.py）
狀態：✅ 通過（29 個母頁）                        （check_hierarchy.py）
OK: CSS 覆寫閘 — 1 個檔無新增死媒體查詢（存量 9 筆、簡寫提示 0 筆）   （run_tests.py，exit 0）
```

### 最後一批（兩條，各一行，可直接貼）

1. **L127 速查列第二格仍指向已不存在的「懸置細節」**——本頁已無該節名（改為「一則細節」），且「懸置」是冷讀者內部用語表第 10 列逐字點名的詞。**修法**：把 L127 第二格 `🟢（07-08，機制已查證見懸置細節）` 改為 `🟢（07-08，機制已查證，見下方細節）`。
2. **`weekly.md:156` 節名寫成「4 跳自檢題」，與同節本文「3 跳內須得到」互打**——B-4 的原意是「3 題→4 題」，不是改跳數。**修法**：節名 `## community-tech-tools 決策表驗收：4 跳自檢題` 改為 `## community-tech-tools 決策表驗收：4 題 3 跳自檢`。
