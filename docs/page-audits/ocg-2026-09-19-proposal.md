# 第 14 波設計提案：topics/official-community-gap（第二輪）

使命句（主 session 定）：**社群喊的痛，官方補了哪幾個、哪幾個還沒補（以及官方為什麼沒補），沒補的你現在能怎麼辦。**
逐字稿見 `ocg-2026-09-19-draft.md`，逐行去向見 `ocg-2026-09-19-proposal-map.md`，評審條目處置見本檔末節。
官方事實一律取自 `ocg-2026-09-19-verified.md`（含第二輪「六、補查」）；與第一輪提案衝突處一律以 verified 為準。

## 1. 推薦案（一個）

**兩張表併成一張「官方補了沒」，把成因分析提到讀者眼前，並替「官方對應」欄裝上定期對一手的步驟。**

根因（verified 五-1、健檢卡第 5 節）：這一欄**沒有人定期對官方一手**。`features/pages.md:156` 只有事件驅動的一句「官方發布新功能時檢查矩陣」，接不到「官方悄悄補了但沒上日報」；表 2 連這一句都沒有（全庫零規則提及「對照矩陣」）。結果是 14 列裡至少 5 列今天是錯的，錯的方向一致——低估官方。第二輪補查又抓出 4 列（Dynamic workflows 已非研究預覽、Agent view 是研究預覽而非無對應、Outcomes 為 beta、個人端只有 `opusplan`）。版面只是症狀。

三件事同時做：
1. **一張表、一套三值符號、一行圖例**（`✅` 能直接用／`🧪` 有但受限／`❌` 官方無對應）。原兩表對同一痛點的互斥狀態（L60 ❌ vs L106 ⚡、L58 🧪 vs L103 ⏳）與同列內欄位打架（L66）就此消失。新增「核對日」欄，`—` 代表還沒對過。
2. **成因分析升到 `## 目前結論` 並前移到結論表之後**（原 L139／L143／L161 三段，全站唯一）。寫成「到目前為止的理由」而非預言，導言明寫 AGENTS.md 曾在這一節、09-18 被官方補掉——這是使命句軟化的落點。**節名不改**（評審 🔴-4：`page-templates.md:87` 的 topics 模板必備節，另有三處 lint 步驟以節名派活）。
3. **兩個事實搬離本頁**：issue 的留言／讚數只住 `entities/claude-code`；代表社群工具只住 `community-tech-tools`「我卡在這裡」（表上不列工具名，細節區的歷史工具加一句「當時的代表案例、不是現在的推薦」）。

## 2. 不選的方向（各一行）

- **拆子頁**：健檢卡與我獨立同判候選 0——三訊號無一組同時亮，病在兩張表長成鄰居頁的舊副本，不是題目太多。
- **保留兩張表只修狀態**：治不了根因，下次官方悄悄出貨時兩張表會再度分岔。
- **把「官方對應」欄整個指路到 agent-stack 八卡**：讀者的第一題就是「補了沒」，一跳都不該跳。
- **做成「你該裝什麼」決策表**：那是 `community-tech-tools` 的家，本頁再做一份就是第三份副本。
- **把 `## 目前結論` 改名成「官方為什麼還沒補」**：改名要另配 `page-templates.md` 模板條文與兩份 lint 步驟的措辭，代價大於收益（評審 🔴-4，主 session 採第三案）。

## 3. 可行性前提（一行）

`/wiki-lint` 5n 要能跑 `gh issue view` 與 `gh api ...CHANGELOG.md`——主 session 本機今天可跑（verified 兩輪皆以此完成，評審亦獨立複跑成功）；雲端班次照既有 `cloud_egress_check.py --group github` 探測規則判斷，探不到就跳過該步並在回報寫明。

## 4. 新骨架

| 節 | 一句用途 | 預估行數 |
|---|---|---|
| 標頭＋callout | 一個日期口徑，今天最重要的一件事 | 10 |
| `## 摘要` | delta-first 三段：什麼剛被補掉、本頁只答什麼、三個狀態各幾個 | 7 |
| `## 官方補了沒` | 唯一結論表，13 列（〔併〕15 列）＋三行圖例 | 20 |
| `## 目前結論` | 粗體小標「官方為什麼還沒補」＋三段成因＋「你的選項」 | 14 |
| `**缺口細節**` ⟨G-01⟩–⟨G-13⟩ | 每列的社群起源、代表工具、官方一手逐字；標題下一句快照提醒 | 34 |
| `## 對照矩陣`（僅保守預設） | 表 1 沒有的 3 個痛點，符號改用同一套三值 | 9 |
| `## 技術彙整` | 剩下 6 個小節，證據層 | 40 |
| `## 相關實體` | 六個出口，每個一句分工 | 8 |
| `## 時序` | 原樣＋prepend 八則 | 69 |

約 211 行（〔併〕約 202 行），原 249 行。懸置細節區因 ⟨Q-02⟩ 結案而消失。

## 5. 表的完整契約

| 項 | 內容 |
|---|---|
| 欄位 | 你痛的是什麼｜官方補了沒｜官方給的是什麼｜還缺什麼、你現在能怎麼辦｜核對日 |
| 列上限 | 18；排序 `❌`→`🧪`→`✅`。**表滿時不硬塞**：新列先與最下方的 `✅` 列比「哪一個還有人在痛」，輸的那列依退場條款移出並於 `## 時序` 記一行；兩者都還在痛就把上限議題寫進 `wiki/log.md`，不自行擴表（評審 🟡-1） |
| 入口判準 | 三條全過，**只管新列**：① ≥2 個獨立社群工具或一則 issue ≥100 讚；② 對象是 Claude Code／Anthropic 的產品行為；③ 寫得出「官方補到哪」（查得到對應，或已確認官方確實沒有）。`❌` 要寫「截至 YYYY-MM-DD 官方文件未見」，把否定證明的範圍講出來 |
| 退場 | **一條、兩個訊號都成立才移出**：① `## 時序` 最後一次提到這一列是 90 天前或更早（從未提過的從開列日起算）；② 這一輪 5n 對官方一手也沒查出變動。`❌` 列為唯一例外，不因天數移出。**不用「核對日」當時鐘**（5n 每週會把鐘撥回去＝永不退場），**也不用「官方功能還在不在跑」當條件**（那會讓每個 `✅` 永遠留著）——兩者都是本波要治的病 |
| 誰在什麼觸發下更新 | ①功能記者，每日 ingest 判定為功能的條目→對得上列就更新該列與核對日；②社群記者轉知（`pending_handoffs.py`）→同一組入口判準；③**主編，每週 `/wiki-lint` 5n**，拿整張表對官方一手 |
| 看守 | **無。** `check_wiki_freshness.py:64` 只看頁面層新鮮度，`check_cell_limits.py` 只看字元數，`check_pending_markers.py` 只看懸置標記——沒有腳本看「核對日」。最小看守方案下一波做（見第 10 節） |

### 退場條文自測：現有 14 列全部重跑，每列單一答案

今天 2026-09-20，90 天線＝2026-06-22。`## 時序` 日期取自對象頁 L191–L249。

| 列 | 狀態 | 訊號①：時序最後一則 | 訊號②：本輪 5n 有無變動 | 答案 |
|---|---|---|---|---|
| 想在一個地方操作好幾家 agent | ❌ | 09-12 | 無 | **留**（❌ 例外） |
| Agent 跟 agent 做生意 | ❌ | 09-03 | 無 | **留**（❌ 例外） |
| 新開一個 session 它就忘光 | 🧪 | 08-25 | 有（auto memory、四個 hook） | **留** |
| 額度快用完、錢花到哪看不看得到 | 🧪 | 未提（開列 05-17，過線） | 有（`/usage` 歸因拆解、本機估算免責） | **留**（②不成立） |
| 想自己決定哪段用哪個模型 | 🧪 | 09-16 | 有（`opusplan`） | **留** |
| 一堆 agent 在跑，看不到誰卡住 | 🧪 | 09-03 | 有（Agent view＝研究預覽） | **留** |
| Agent 之間直接傳訊 | 🧪 | 09-19 | 有（#28300 仍 OPEN） | **留** |
| 把多 agent 工作流寫成能重跑的腳本 | 🧪 | 未提（開列 05-17，過線） | 有（去研究預覽、Projects） | **留**（②不成立） |
| 它說做完了，但品質夠不夠 | 🧪 | 07-22 | 有（Outcomes＝beta） | **留** |
| 別家 agent 的 AGENTS.md | ✅ | 未提（開列 05-17，過線） | 有（v2.1.277） | **留**（②不成立） |
| 它需要你輸入時會不會叫你 | ✅ | 未提（開列 05-17，過線） | 有（手機推播、閒置通知、`Notification` hook） | **留**（②不成立） |
| 把活分給 subagent、編排它們 | ✅ | 08-23 | 有（#24316 已出貨） | **留** |
| 它會不會跑出破壞性指令 | ✅ | 08-04 | 無（verified 六-7 本輪未重查） | **留**（①不成立） |
| **Slack 裡要一個 AI 隊友** | ✅ | **未提（開列 05-17，過線）** | **無**（只確認文件索引仍列 claude-tag） | **移出** |

14 列 14 個答案，無一列出現第二個答案。唯一移出的正是 L62 自承「社群前驅稀薄、官方主導色彩強，列入僅供對照參考」那一列——健檢卡第 5 節指的「自承弱卻無移除路徑」就此有路。Claude Tag 的事實家在 [[entities/claude-tag]]，一字不失；移出於 `## 時序` 記一行。**新表因此為 13 列（〔併〕15 列），非第一輪的 14／17。**

**保命條款：** 本頁懸置 2 筆，本波**兩筆皆經官方一手結案**（非砍列附帶）——L92 ⟨Q-02⟩ 依 verified 六-8、L153 Cowork 依 verified 六-5；事實分別落進 ⟨G-12⟩ 與新表第 12 列＋時序。全庫 188 → 186，基線 140 未跌破，實作時同批 `--rebuild-count --reason`。移出的 Slack 列與刪除的矩陣 6 列均無懸置標記（已核）。

## 6. 回掃清單

**同維護者（功能記者，本波同批做；只改該幾句，不重寫頁）**
| 頁：行 | 現況 | 要改成 |
|---|---|---|
| `entities/claude-code:375` | 🔴 未修復｜AGENTS.md 規範不支援⋯官方尚未回應 | ✅ 已修復 v2.1.277（2026-09-18）＋三個邊界一句 |
| `entities/claude-code:200／255／256／379／380` | 五筆仍標 🔴 未修復（#24316／#14227／#47023／#29006／#28322） | 依 verified 二改 ✅ 已修復（附關閉日與官方怎麼答）；**#14227 改 ⛔ 官方拒修**（NOT_PLANNED，2026-05-25）；只改狀態詞與一句官方回覆，不重寫條目（評審 🔴-6） |
| `entities/claude-code` 三個組頭 | 見下方重算 | 同批改 |
| `topics/coding-workflow-guide:134` | 「Claude Code 只讀 `CLAUDE.md`，但你可以讓它 `@AGENTS.md` import」 | 改為 v2.1.277 起原生讀；`@AGENTS.md` 降為 Bedrock／Vertex／Foundry／關遙測場合的替代法 |
| `wiki/feature-radar` | 全覽表與 `## 🆕 最新功能` 完全沒有 v2.1.277／AGENTS.md | 補條目＋表列（radar 條目須主編彙整）——verified 六-9 列為本波之後補 |
| `entities/claude-code:88／262／655／666／730／731` | **六處**引用本頁 | 只核對指向仍成立，不改字（評審 🟡-10 說 4 處，實測 6 處，見末節反駁） |

**組頭統計重算（自跑 `grep -n "^### .*條"` 定位，行號區間對照六筆 issue 行）：**

| 組（行） | 改前 | 改後 |
|---|---|---|
| 🧠 行為與品質（:182，含 :200 #24316） | 52 條未修復、2 條已修復、1 條拒修、6 條查無官方、2 條❓ | **51 條未修復、3 條已修復**、1 條拒修、6 條查無官方、2 條❓ |
| 📂 Session 與資料管理（:251，含 :255 #14227、:256 #47023） | 13 條未修復、1 條拒修、1 條查無官方、1 條已修復 | **11 條未修復、2 條拒修**、1 條查無官方、**2 條已修復** |
| 🔌 平台相容性（:305，含 :375 #6235、:379 #29006、:380 #28322） | 70 條未修復、3 條查無官方 | **67 條未修復**、3 條查無官方、**3 條已修復** |

**跨維護者（走 `python scripts/pending_handoffs.py open --from 功能 --to <類別>`，類別用中文；共 6 筆）**
1. → **社群**：`topics/community-tech-discussions:171／222` 仍把 AGENTS.md 寫成進行中的社群呼籲（狀態 `🌙靜候`、數字停在 08-27 的 385 則／6525 讚）；官方已於 v2.1.277（09-18）原生支援、issue #6235 於 08-17 CLOSED／COMPLETED，請更新表列狀態與細節條目。（評審 🔴-5）
2. → **社群**：`topics/community-tech-tools` 與 `topics/skill-interest-watch` 的「該裝哪個」是本站唯一出口，本頁已改為單向指過去；請補一句回指，並看 `community-tech-tools:83` 引用本頁處是否仍成立。
3. → **社群**：`topics/community-tech-patterns:142`「cross-session 傳訊⋯已補」與本頁「🧪 點對點已跨機器、共享頻道式 A2A 仍缺」措辭不一致，請對齊。
4. → **社群**：`skill-interest-watch:21`「帳單爆了，看不到錢花在哪 → tare」答的是事後看帳，冷讀者要的是事前告警——症狀文案建議拆成兩列（官方側現在有 VS Code 70% 橫幅可寫）。
5. → **主編**（`wiki/index.md`）：L109 鉤子「缺口矩陣：哪些痛點官方正在解決、哪些結構性缺席」已不準，建議改為「官方補了哪幾個、哪幾個還沒補、為什麼沒補」；L27「我卡住了」列與互通性／多工具並用無入口（冷讀者第 1 條）。
6. → **主編**（`wiki/log.md`）：本波 Query／定稿條目、⟨Q-02⟩ 與 Cowork 兩筆結案理由、Slack 列移出理由。

**只記 `wiki/log.md`、不另開轉知**（評審 🟡-9）：`wiki/overview.md:73`（主編週更自然覆寫）、`topics/competitor-landscape.md:264`（2026-08-26 時序內的歷史記錄）。

## 7. 裁決點（剩一個）

**「對照矩陣」整節併入新表**
- 我的推薦：**併**。9 列中 6 列是新表某列的較舊、較粗版本（官方對應停在 2026-05），留著就是兩個家、互斥狀態的來源。
- 未回覆時的保守預設：節名與節保留，只留表 1 沒有的 3 列，其餘 6 列刪除（同一事實的第二份，不是砍節）。**依評審意見加一項緩解**：保留的 3 列狀態改用新表同一套三值（`⚡`／`⏳` 不再出現），節首寫一句「符號與上表同義」，不留第二套符號。
- 兩種結果的去向差異：見 map 末節六列差異表。新表 13 列（保守）vs 15 列（併）；摘要末句與成因節導言首句同步改（逐字見 draft A-3／A-4／A-5 的〔併〕註）。
- 併案下 L107「平台可及性」**不上移新表**——它沒有任何社群工具或 issue 撐著（原表 2 工具密度 ⭐⭐ 為全表最低），入口判準①與退場條款給同一個答案；改由 [[entities/cowork]] 與 [[feature-radar]] 承接，細節家（L147–153）留原處，事實不消失。

> 裁決點 (2)（`## 目前結論` 節名）已由主 session 採評審第三案結案：節名保留、位置前移、第一行粗體小標，不再是裁決點。

## 8. 實作順序（單一序列，14 步；601a5b6e 宣告經主 session 查證為殘留，無需協調）

1. 標頭 A-1、callout A-2、摘要 A-3（保守案數字 13／2／7／4）。可驗：首屏只剩一個日期口徑。
2. 新表取代 L47–67：節名改 `## 官方補了沒`、5 欄表頭、三行圖例。**〔決1〕** 13 列或 15 列。
3. 缺口細節 A-6：標題下加快照提醒句；九條改寫／拆條。
4. **〔決1〕** 對照矩陣：併則整節刪（L96–108）；保守則只留 3 列並把 `⚡`／`⏳` 改成三值、節首加同義句。
5. 技術彙整 L131／L135／L153／L155 四處事實更正（含 Cowork 07-07／beta access／桌面才完整，並移除該懸置標記）；L139／L143／L161 三段搬出。
6. `## 目前結論`：節名留、位置前移到結論表之後、內容全換（draft A-5）。
7. 相關實體 A-8 六條；時序 prepend A-9 八則（含 Slack 移出、兩筆懸置結案）。
8. 同維護者回掃：`entities/claude-code` 六行狀態更正＋三個組頭重算；`coding-workflow-guide:134`。
9. `python scripts/check_pending_markers.py --rebuild-count --reason "第 14 波 official-community-gap：⟨Q-02⟩ 與 Cowork 兩筆經官方一手查證結案"`，確認 186／基線更新。
10. 跑閘：`check_cell_limits.py --page official-community-gap` → `check_reader_language.py --page official-community-gap` → `gen_wiki_frontmatter.py` → `check_hierarchy.py` → `build_web.py`（錨點 WARN 不增）。
11. B-1：`features/pages.md` 整節換（保留「產品化矩陣同步」字面，評審 🟡-3）。
12. B-2／B-3／B-4：sweeps SKILL.md 五處字面＋一列、sweeps.md H1＋新節、`wiki-lint/SKILL.md:15`、`docs/cloud-runbooks/weekly-lint.md` 步驟列、`review-registry.json` 第 113 組 patterns 加 `"5n\\. official-community-gap"`。動前先讀 `.claude/rules/claude-md-edit.md`。可驗：`python scripts/check_rules.py` 維持 ✅。
13. 跨維護者轉知 6 筆；`wiki/index.md` 鉤子與互通性入口列；`wiki/log.md` 條目。
14. `python scripts/run_tests.py` exit 0。

## 9. 仍需主編補查（verified 六之後的殘項）

1. 「它會不會跑出破壞性指令」列自 2026-08-04 起未對過官方（verified 六-7 明載本輪未重查）——核對日維持 `—`。
2. Dynamic workflows 的 UltraCode 1.7M token 退款爭議：官方無專文，本輪查不到，頁面兩案都不寫已解或未解。
3. `feature-radar` 缺 v2.1.277／AGENTS.md 條目與表列（verified 六-9）。
4. `check_wiki_freshness.py:64` 把本頁維護來源寫成 `features/daily.md 產品化矩陣同步`，但該字面實住 `features/pages.md:156`——既有錯指，本波不動，記 ledger。
5. **機制待辦（下一波）** `scripts/check_verify_dates.py`（評審第二節）：只掃本表「核對日」欄，`—` 不算逾期只計數，逾 45 天 WARN、逾 90 天 FAIL；**照 `data/reader-language-baseline.json` 的先例帶存量基線**，首跑把現有 `—` 全數收進基線，棘輪只往下轉。本波不做——它要掛 `run_tests.py`，而實作者不准改該檔。

---

## 第二輪：評審條目處置

評審 🔴 8 條、🟡 11 條。**接受 16、改寫 2、反駁 1。**

| 條 | 處置 | 說明 |
|---|---|---|
| 🔴-1 退場三句兩答案、90 天對 ✅ 永不觸發 | **改寫** | 接受診斷（我的自測只吃了②）。但評審修法「看社群這一側」沒定義可查的時鐘，我實跑 14 列時出現三種算法（工具日／issue 日／開列日）各給不同答案。改採**兩訊號都成立才移出**：`## 時序` 最後一則過 90 天＋本輪 5n 無變動；`❌` 為唯一例外。14 列全跑一遍、單一答案，結果表見 §5，唯一移出者仍是 Slack（與評審結論相同，日期由 09-22 提前到本波）。 |
| 🔴-2 B-1 落在他 session 宣告路徑、節名改不能延後 | **改寫** | 接受風險判斷（規則檔指向不存在的節是每日 ingest 的觸發邊）。主 session 已查證 601a5b6e 宣告為殘留（其工作 09-16 已 commit：4e47e1c1／836e71a2／64123d5a，宣告路徑工作樹乾淨），故不設第 0 步協調、不分岔；實作單改為單一序列 14 步，B-1 排在頁面之後同批。 |
| 🔴-3 5n 字面同步漏 4 處、漏 runbook、registry 寫成條件式 | **接受** | 全庫 `grep -rn "十三\|5a–5m"` 自跑覆核：7 處屬實（`wiki-lint/SKILL.md:15`、`sweeps.md:1`、sweeps `SKILL.md:3／7／9／13／43`）。B-4 照列。registry 改為明確指向**第 113 組**（`雲端 weekly runbook <-> wiki-lint-sweeps/references/sweeps.md`），pattern 用短前綴 `"5n\\. official-community-gap"` 與同組既有寫法（`"5e\\. pricing"`、`"5m\\. code-quality-decline"`）一致。另註明 `wiki-lint-rules-health/SKILL.md:236` 的「十三行」是 C 段 6a–6l 的計數，**不得一起改**。 |
| 🔴-4 節名凍結宣稱不實、推薦案破 topics 模板 | **接受** | 我第一輪的 grep 只掃 `scripts/`／`web_reader/assets/`／`src/tests/`／registry，漏掃 `.claude/reporter-rules/` 與 `.claude/skills/`，宣稱錯。採第三案：`## 目前結論` 節名留、位置前移、第一行粗體小標「官方為什麼還沒補」。裁決點 (2) 消滅。 |
| 🔴-5 回掃漏 `community-tech-discussions:171／222` | **接受** | 跨維護者清單加為第 1 筆，逐字照評審修法。 |
| 🔴-6 同維護者漏 5 筆已關 issue、組頭不只一組 | **接受** | 六行全列進回掃表，#14227 改 ⛔ 拒修。組頭自算並寫出改前改後三組（🧠 52→51 未修復、📂 13→11、🔌平台 70→67），見 §6。 |
| 🔴-7 表上引言第三行是條文洩漏 | **接受** | 照逐字換成「每個 issue 現在多少人在吵、官方回了什麼，見 [[entities/claude-code]]。」 |
| 🔴-8 細節區工具名仍是第二組「該裝哪個」答案 | **接受** | 照逐字加進 `**缺口細節**` 標題下一行，並把同一句寫進 B-1 契約。 |
| 🟡-1 上限 18 開局就 17、無滿載規則 | **接受** | B-1 補滿載句（照逐字）。實際開局 13 列，餘裕 5 列。 |
| 🟡-2 5n 第 1 步會倒出整包留言 | **接受** | B-1／B-3 第 1 步換成評審的 `-q` 版本，只取 COLLABORATOR／MEMBER 留言前 400 字。 |
| 🟡-3 `check_wiki_freshness.py:65` 硬編「產品化矩陣同步」 | **接受** | B-1 新節小標寫成 `### 產品化矩陣同步（「官方補了沒」表）`，字面保留。另記 §9-4：該腳本指的是 `features/daily.md`，而字面實住 `pages.md`，是既有錯指，本波不動。 |
| 🟡-4 核對日定義寫成「我們多久查一次」 | **接受** | 照逐字。 |
| 🟡-5 「本頁在別處找不到的東西」是自我評語 | **接受** | 整句刪，保留其後「但它們說的是到目前為止的理由，不是預言」。 |
| 🟡-6 時序末則把頁面整理寫進去 | **接受** | 照逐字換成「同一個痛點不再有兩個互相打架的狀態⋯」。 |
| 🟡-7 「門檻也不能自訂」丟了 verified 的推論口氣 | **接受** | 表格格內改「告警門檻似乎不能自訂」，⟨G-07⟩ 保留完整括號。 |
| 🟡-8 map L100 備註 `/loop`／`/batch` 歸屬寫錯 | **改寫** | 接受我原備註錯。但評審給的替代位置 `feature-radar:349` 也不成立——自跑 `grep -rn '`/loop`' wiki/` 只命中 `cat-wu:44／67`、`claude-code:786`、`feature-radar-archive-2026-05:228／233`。map 改寫為：家在 `feature-radar-archive-2026-05.md:228` 與本頁 L116。 |
| 🟡-9 overview:73／competitor-landscape:264 只記 log | **接受** | 寫進 §6 末。 |
| 🟡-10 claude-code 引用本頁「3 處」實為 4 處 | **反駁（事實有誤）** | 自跑 `grep -n "official-community-gap" wiki/entities/claude-code.md` → **6 處**：`:88`、`:262`、`:655`、`:666`、`:730`、`:731`（另 `wiki_graph.py explain` 亦列 5 正文＋1 樣板）。我第一輪寫 3 是漏，評審寫 4 也漏了 `:730／:731` 兩筆歷史記錄。§6 已改為六處。 |
| 🟡-11 需補查 9 項齊全 | **接受** | 無需處置；九項已由 verified 六全數回覆，殘項四條移入 §9。 |

**兩閘（第二輪重跑）**：`check_reader_language.scan()` 對 draft「進頁面」暫存檔（11 個 fence、109 行）**0 命中**；注入「本表由記者每日 ingest 維護，逾期列一律退場。」→ **3 命中（ingest／記者／退場），閘會紅**。
`check_cell_limits.scan()` 同檔 **0 命中**，主表 13 列、最長格 110 字元（上限 120）；中途曾抓到 ⟨G-02⟩ 一條 201 字元（超 200），拆成兩條後轉綠；注入 130 字元儲存格與 210 字元條列 → **2 命中，閘會紅**。
