# 設計提案：`wiki/entities/opus-5.md`（第 11 波）

2026-09-07｜設計者（交件前已自跑兩閘並驗紅）｜適用區 A／B／D／E，**C 不適用**（無視覺成品、無現實體裁交付）｜⚠️ 疑似注入：無（對象頁、九個鄰居、index、五份規則檔皆無指令式文字）
逐行號去向表另存 `-proposal-map.md`、逐字稿另存 `-draft.md`（分「進頁面」／「進規則檔、帳本與給主編」兩欄），兩者不計入 120 行上限。行號＝檔案原始行號（含 frontmatter 25 行），**以磁碟現行檔為準**（`git status --short wiki/` 零輸出，本頁在 git 為乾淨狀態）。

## 0. 一句推薦案

> **把散在十段散文裡的「現在」收成兩張表：`## 你現在拿到的是什麼`（Opus 4.8 vs Opus 5 兩代 × 7 列，含全站零處寫著的兩個官方退役日與兩個 breaking change），與 `## 官方宣稱 vs 社群實測`（官方四項基準 × 社群七則訊號，明說兩邊不是同一種證據）。三處「待彙整／待觀察」過期宣稱全砍、三筆逾期懸置依查證結案、跨家分數退出結論層指向榜頁、歷史記錄 15 列併成 12 列。162 行／16,593 字元 → **168 行／14,491 字元**（字元 −12.7%，行數 +6，理由見 §7）。判準與退場全部住規則檔。**

**被考慮過但不推薦的方向（各一行）：**

- **三代一表（4.8／5／Fable 5.1）不選**——第三欄會把本頁變成選型表，而選型的家是 [[topics/model-comparison]]（H 條另有欄數上限）；「何時改用 Fable 5.1」用官方原句寫進第 6 列一格就夠。
- **寫一句「社群說法站不站得住」的裁決句不選**——冷讀者實測正是因為頁面自承弱訊號才**知道不能拿社群意見做決定**，`SKILL.md` 原則第 4 條也反對；本波給的是「兩邊各是什麼證據」，不是結論。
- **拆子頁不選**（健檢卡與冷讀者同一結論）——五群裡只有兩群的家在本頁，162 行離 600 行鬧鐘很遠；**建 archive 也不選**，最舊條目 2026-07-24，本月序 − 該月序 ＝ 2 < 3。
- **`## 參考來源` 19 列重排不選**——證據等級混排是真問題但與本波使命無關，明寫「看過、不處置」，列 09-14 回訪。
- **改熱度或試用價值的值不選**——與 radar :147 本來就一致（本頁唯一乾淨的跨頁值），本波只補判定日。
- **`## 爭議` 節不新開**——健檢卡建議套 fable-5 的爭議節形狀，但本頁七則社群訊號**沒有一則有正反兩方**，只有單方觀感；套上狀態三值等於給它們不存在的重量。改用 `## 官方宣稱 vs 社群實測` 這個對照形狀。

## 1. 可行性前提（一行）

**本案不需要新進料**：兩張表的每一格都來自 `-verified.md` 已完成的官方查證（platform 模型頁、anthropic.com 公告、code.claude.com What's new W30、support 專文），沒有一格需要記者自己判斷；維護節奏維持每日；**兩張表的週期性複查本波才建**——`/wiki-lint` 5l 現行射程只寫 `fable-5`（`.claude/commands/wiki-lint.md:371–377` 實查），擴充到本頁之前，全站沒有任何步驟會回頭看這兩張表（draft B-4）。

## 2. 骨架（每節標服務哪幾題，Q 編號同健檢卡）

| 新節 | 取代 | 服務 | 型態 |
|---|---|---|---|
| 標頭＋callout（壓縮） | L26–36 | — | 導言 |
| `## 現況`（兩段） | L42–60 十段 | Q1、Q2 | delta-first |
| **`## 你現在拿到的是什麼`（7×4）** | L46／L48／L52／L56 | **Q1、Q2、Team 缺口** | 覆寫式結論表 |
| `## 熱度與試用價值`（4 列，補判定日） | L66–73 | Q5 | 跨頁值 |
| **`## 官方宣稱 vs 社群實測`（5×2＋六條）** | L48／L54＋歷史記錄的並陳 | **Q3、Q4** | 覆寫式結論表 |
| `## 核心功能`（4 條）／`## 相關議題`（11 條） | L79–84 六條／L90–98 九條 | Q2、Q4、**Q5** | 規格與出口 |
| `## 參考來源`（19 列，不動）／`## 歷史記錄`（12 列＋細節） | L100–162 | 查原文 | 證據層 |

## 3. 三題的官方答案，全部進第一張表

冷讀者卡住的三處，官方各只有一句答案（逐字見 draft A-4）：

| 卡在哪 | 官方答案 | 現在全站寫在哪 |
|---|---|---|
| Q2 後半「4.8 還能用多久」（❌ 沒拿到） | 4.8 為 **Legacy**、退役不早於 **2027-05-28**；Opus 5 不早於 **2027-07-24** | **全站零處** |
| Q1「我的 Team 方案是不是預設它」（⚠️ 半拿到） | Max、Team premium、Enterprise 隨用隨付、API 的預設 Opus；**Claude Code 整體預設仍是 Sonnet** | 本頁 L56 只寫「Max 新預設、Pro 最強」，且「Pro 最強」**無官方出處** |
| 升上去會壞什麼（冷讀者沒問，健檢卡 V4 抓到） | 兩個 breaking change：thinking 預設開啟；thinking 只在 effort `high` 以下可關 | **全站零處** |

第 7 列是**全庫第一句對 Team 說的話**——pricing :64–65 現在只寫「同 Pro／同 Max」，讀者要自己推兩層（冷讀者原話：「沒有一句話直接對 Team 說話」）。金額不複製，指回 [[entities/pricing]]。

## 4. Q3／Q4：不給假結論，給「兩邊各是什麼證據」

健檢卡把本頁的「零結論」記為主要缺陷，冷讀者實測結果**相反**：他因為頁面反覆寫「弱訊號」而知道不能拿社群意見做決定，判為「全輪證據最硬的一段」。兩者衝突時以實測為準，且 `SKILL.md` 原則第 4 條站在冷讀者那邊。所以 `## 官方宣稱 vs 社群實測` 做的是**分層**不是**收斂**：官方四項基準列成表（Frontier-Bench v0.1／CursorBench 3.2／ARC-AGI 3／OSWorld 2.0，全部量在官方自有的尺上），社群七則訊號列成條列（五則變差了、一則反向、一則已被官方推翻、一則跨模型 issue），導言明寫「44 天來社群沒有交出任何一則帶測試方法或數字的獨立複測」。**那句話是承諾**——社群第一次交出量化複測時必須同批改寫，條文寫進規則檔（draft B-3）。表刻意只有兩欄：第三欄「社群有沒有對上」五列全是同一個值，那是 models 規則 H 條明文禁止的低資訊欄。

## 5. 跨家數字：退出結論層，但一個數字都不刪

L54 的 AA Intelligence Index 60.7%／59.9%／58.9%、GDPval-AA v2 1861 Elo、AA-Briefcase 1720 Elo 與每任務 $10.41 vs $22.30，是全庫唯一一組第三方跨家數字，住在一個沒有回訪機制的地方已 28 天，且 `-verified.md` **V5 未查證現值**（該指數已跨 Fable 5.1 與 GPT-5.6 兩次更新）。處置：

- **結論層**只留一句帶查證日的指路＋[[topics/model-task-leaderboard]] 出口（同 J 條對 model-comparison 的「一句結論＋出口」）；**數字原文留在歷史記錄 L160**（07-26 那一則，歷史條目寫的是「當時」，照共用規則不改寫），**一個數字都沒有消失**。
- **榜頁要不要開一列 AA 綜合指數**：交主編於 `/wiki-lint` 5b 評估（draft B-5 末條）。**不走轉知帳本**——榜頁由主編在 5b 派工抓榜維護，帳本是記者之間的交辦。

## 6. 三筆懸置結案：**基線是 141→138，不是歸零**

`-verified.md` 第三節寫「三筆結案後本頁懸置歸零」——**逾期歸零是對的，懸置歸零不對**。`data/pending-marker-count.json` 的指紋清單裡 `entities/opus-5` 佔 **5 筆**（Q-01 1w0uyu7、Q-02 $19 Gap、Q-03 smoking gun、Q-04 SitePoint、Q-05 minor），結 3 筆後**還剩 2 筆**；141 − 3 ＝ 138 這個數字本身就證明它不是歸零。⟨Q-01⟩⟨Q-02⟩ 未逾期也未查證，短標記與細節定義**兩邊都留著**——`check_pending_markers.py:172–185` 雙向對帳，只留定義不留短標記會 FAIL（`⟨Q-nn⟩ 有懸置細節定義但無表格短標記引用`）。這是我沒有把 08-28 與 08-26 兩列一起併掉的原因。**保命條款逐筆核過**：三筆的事實本身全部留在頁面上（貼文標題、取不到正文、SitePoint 三項與官方不符），消失的只有探針與複查日這些 metadata，而那三筆的偵測條件已無意義——兩筆兩次查證都取不到原文，一筆來源被判不可靠。

## 7. 規模：字元 −12.7%，**行數 +6**（誠實記錄）

整頁組稿實測：**162 行／16,593 字元 → 168 行／14,491 字元**（皆不含 frontmatter）。健檢卡估「約 130 行」、冷讀者估「約 120 行」，**兩個估計都會落空**：原頁 L42、L44、L52、L54 四段各是 300–700 字元的**單行**長句，收斂成表格後字元大減、行數反而增加（一列一行）。**本頁該用字元量測，不是行數**——請評審連同兩個上游估計一起重算。

## 8. 自己過閘的輸出（含驗紅）

暫存檔 `wiki/entities/__w11draft.md`（整頁組稿）與 `__w11red.md`（驗紅），量完即刪；交件時 `git status --short wiki/ scripts/ data/` **零輸出**。

```
--- 進頁面全文（A-1～A-11 組成的整頁）---
OK: 讀者語言閘 — 無新增命中
OK: 字元上限機械閘 — 無新增超限
--- 驗紅（同稿注入禁詞一段＋125 字元儲存格＋205 字元條列）---
FAIL: 讀者語言閘 — 10 筆新增的內部用語外洩
  entities/__w11red.md:169  [ingest][派工][記者][主編][覆寫][回掃][轉知][退場][留表優先序][封存]
FAIL: 字元上限機械閘 — 12 筆新增的超限儲存格／條列
  entities/__w11red.md:170  [表格儲存格（>120），實測 125 字元]
  entities/__w11red.md:171  [細節區條列（>200），實測 205 字元]
```

**第一版真的被擋過一次**：熱度表「最適合」欄我原寫「數小時的自**主編**碼」——`check_reader_language.py` 是子字串比對，「自主編碼」命中禁詞 `主編`。改寫成「數小時、沒人盯著也要跑完的編碼任務」後歸零；這是真誤擋，但改寫後更像讀者語言，**不申請白名單**。

**組稿量測的一個陷阱（評審請看這裡）**：拿暫存檔 slug 跑 `check_cell_limits` 會吐出 **10 筆 FAIL**——那不是我寫的東西，是本頁 10 行既有的超長細節條目，因為暫存 slug 不在字元基線裡。我用兩件事證明：`check_cell_limits.py --page entities/opus-5` 回「存量基線內 13 筆／1 頁、無新增超限」，且那 10 行逐行 `in` 比對現行檔**全部為 True**。我實際新增或改寫的五段實測 147／97／131／81／159 字元，全在 200 以內。

| 宣稱 | 真的有嗎 | 出處 |
|---|---|---|
| 儲存格 >120／條列 >200 被擋 | ✅ 已驗紅 | `check_cell_limits.py`；本案**不需 `--rebuild`**（三處改寫都切到 200 以內）|
| 內部用語被擋 | ⚠️ 只擋固定禁詞清單，且是**子字串比對**（誤擋「自主編碼」）；不擋 ⟨Q-nn⟩、懸置 metadata、編輯台語氣 | `check_reader_language.py --list`；冷讀者本頁兩條外洩（:124、:129）閘都看不到 |
| 懸置標記總數不得靜默減少 | ✅ 有 | `check_pending_markers.py:267–306`，基線 `data/pending-marker-count.json`（141）；出口 `--rebuild-count --reason` |
| 短標記與細節定義成對 | ✅ 有，**雙向** | `check_pending_markers.py:172–185`（孤兒短標記與孤兒定義各一條 FAIL）|
| 砍的地方不是某筆懸置僅存的家 | ✅ 實查：本頁 5 筆標記全在 L149／L151／L152／L155／L158，本波動其中 3 筆且事實留頁；併掉的 L134／L137 兩列**零標記** | `grep -n "⟨Q-0" wiki/entities/opus-5.md` |
| 改 H1／節名會不會斷鏈 | ✅ 零風險：`grep -rn "opus-5#" wiki/ .claude/ scripts/` **零命中**；本波也不改名 | 錨點檢查本身只是 WARN，所以先用 grep 證明 |
| 兩張新表有淘汰機制（census 判「有」） | ✅ **實測 3/3**：把 draft B-2／B-3 的三個 h3 原文餵進 `_mechanism()`，`## 你現在拿到的是什麼` 兩頁皆「有」、`## 官方宣稱 vs 社群實測`「有」、`## 熱度與試用價值`「有」 | `table_census.py:46–70`（命中行或其最近標題須含 slug）。**現況對照組**：`table_census.py entities/opus-5` 今天回報兩張表**皆「無」** |
| `/wiki-lint` 會跑本頁兩張表的複查 | ❌ **現在不會**——5l 逐字只寫 `fable-5`；本案補上射程（draft B-4），實作前不成立 | `.claude/commands/wiki-lint.md:371–377` |

**熱度表 h3 我改過一次**：第一版寫成「### opus-5 的 `## 熱度與試用價值`」（機制詞留在內文），實測回「無」；照 fable-5 的寫法把「不一致即覆寫」搬進標題後才回「有」。**這正是第 10 波 🔴-1 的同一顆地雷**，我這次是自己測出來的，不是評審抓的。

## 9. 跨頁矛盾與同維護者回掃

| 矛盾 | 定哪個說法 | 誰去改 |
|---|---|---|
| 「定價定位待彙整」四份副本（本頁 L52／L94、index :54、radar :16／:147） | 官方 $5／$25（2026-08-08 首查、2026-09-07 複查） | 本頁兩處本波改；index 與 radar 三處給主編逐字（draft B-5）|
| 「Claude Pro 最強模型」（本頁 L56、pricing :83、radar :147） | **無官方出處**——W30 只列 Max／Team premium／Enterprise PAYG／API | 本頁改寫成官方分層；radar 給主編；pricing 走帳本（draft B-6）|
| opus-4-8 標頭寫 `active（已被取代）`，官方已標 `Legacy` 並載退役日 | 官方模型總覽頁 | **本波同批改三處**（同維護者的事實更正，非版面重寫）——map §四 |
| #77136 五份副本，缺席規則指定家 claude-code | 家頁的數字為準 | 功能記者（帳本 draft B-6）|
| pricing Team 兩列只寫「同 Pro／同 Max」 | 官方 W30 的預設分層 | 商業記者（帳本 draft B-6）|

**opus-4-8 為什麼算「事實更正」不是「重寫鄰居頁」**：「事實更正必回掃」沒有給同維護者例外（第 10 波已判過同型爭點），而改的是標頭狀態句與一句互指句、不動該頁任何一節；冷讀者 Q2 唯一的 ❌ 就卡在這裡。

## 10. 裁決點（2 個）

1. **opus-4-8 的 `## 下一代模型觀察：「Opus 5」傳聞` 12 行（opus-4-8 :154–163）要不要死案歸檔**（跨頁砍整節，交使用者）。傳聞已於 07-25 獲官方證實、事實的家已是本頁，該節四則的每一則都自己寫著「已由發布解答」。**A 版**：整節移除，opus-4-8 :44 那句「近兩週的『Opus 5』傳聞…就此獲得官方證實」保留並改指 [[entities/opus-5]]，四則原文留在 git 歷史。**B 版（使用者未回覆時的保守預設，＝本案採用）**：整節不動，只在節標後補一句「以下為傳聞階段的原始查證過程，Opus 5 現況見 [[entities/opus-5]]」——該句現在已經有了，等於零改動。**保命條款已跑**：該頁 `pending_count: 0`，該節零懸置標記。
2. **本波同批改 opus-4-8 三處事實更正**（§9 末列，map §四）。這不是砍節、不是重寫，屬主 session 代判範圍；列在這裡是因為它動到鄰居頁，且它是冷讀者唯一 ❌ 的解法。若評審或使用者認為「一次一頁」應嚴格到連狀態句都不能改，保守版是只改本頁 A-4 第 4 列，opus-4-8 那三處走轉知帳本給下一波——代價是那個 ❌ 再多活一週。

## 11. 我最沒把握的一處

**`## 官方宣稱 vs 社群實測` 這個節名會不會被讀成「本站在替官方背書或拆台」。** 節名是對照式的，但表裡只有官方那一側有列，社群那一側是條列——版面上看起來像官方贏了。真實情況是「一邊有四個數字、一邊什麼都沒有」，而那本身就是本波要告訴讀者的事。我選擇讓版面誠實地不對稱，而不是硬湊第二欄（湊出來只會是五個「無獨立複測」的低資訊欄）。若冷讀者複驗時說這節看起來像官方宣傳，替代方案是把節名改成「這些數字是誰量的」，同一套內容不動。

**次沒把握的是第 1 列與第 7 列的分工**：兩者在 Max 那一格讀起來很像，我用「第 1 列只寫 Claude Code 整體預設仍是 Sonnet、第 7 列只寫方案分層」來切，但這條線是我畫的不是官方畫的。

---

## 第二輪：評審條目→處置（7 🔴 ＋ 10 🟡 ＝ 17 條）

對磁碟現行的 `-review.md`（實作單 17 步）。三份檔已同步改完。**處置統計：接受 12／接受但改寫 5／反駁 0。**
**節名依評審 🟡-9 判定改為 `## 這些數字是誰量的`**，本節以下與 draft／map 為準；§0、§2、§4、§8、§11 留原文作第一輪紀錄，節名以本節為準。

| # | 處置 | 說明 |
|---|---|---|
| 🔴-1 導言「全部量在官方自有的尺上」事實錯 | **接受逐字，親改** | 評審對：CursorBench 3.2、ARC-AGI 3、OSWorld 2.0 都是公開基準，只有 Frontier-Bench v0.1 與 GDPval-AA 沾得上邊。這是全節的立論句，錯的話整節在替一個不存在的對比背書——而且它正是我 §11 自承的「滑進勝負框架」的證據。draft A-6 導言換成評審版；B-3 判準句①改「不論官方自有或公開基準，分數由官方提交即入表」，並加一句「導言不得寫『全部量在官方自有的尺上』」 |
| 🔴-2 「Pro 最強模型」回掃只做一半 | **接受逐字** | 全庫實掃 10 份副本，我上一輪只處置 4 份。draft B-5 補 `feature-radar:568`、`overview:34` 兩份逐字，`opus-4-8:44` 併進本波同批改；**opus-5:161、opus-4-8:169、model-comparison:187 三處明寫不動**（歷史條目寫「當時」），並要求實作單寫出來，免得實作者 grep 到順手改掉 |
| 🔴-3 map 第四節行號錯 | **接受逐字，親改** | 實查確認：`opus-4-8:44` 才是那句，`:46` 是 2026-05-28 發布沿革段——照我原稿改會把 Legacy 句補進發布沿革段尾。傳聞節節標實在 `:152`，我寫的 `:154–163` 少了節標，日後採 A 版會留孤兒節標。map 第三、四節均已改；**:44 一行同時是 🔴-2 的改法、錨點目標與 Legacy 句落點，三處併成一次改動** |
| 🔴-4 index :55 逐字沒給 | **接受逐字** | 主 session 指定的本波主編項，我上一輪只給了 :54 與 :36。`test_index_sync` 只比狀態主值（兩邊都是 `active`），機器抓不到——draft B-5 補上整列逐字 |
| 🔴-5 逐字稿六個區塊全缺 `##` 節標行 | **接受逐字，親改** | 逐字稿的契約是照抄即可，而 A-4 的節標正是 opus-4-8 新錨點 `[[entities/opus-5#你現在拿到的是什麼]]` 的目標，漏一行就是 WARN。draft A-3／A-4／A-5／A-6／A-7／A-8／A-9 七個 block 各補一行節標（節名用改名後的） |
| 🔴-6 `## 相關議題` 靜默多一條 | **接受逐字，親改** | 我 block 裡寫了 11 條，map 與註記卻只登記「九留＋一」，`claude-code` 沒進去向表——第 10 波「相關議題靜默增刪」同型復發。A-8 註記與 map 第一節都改成「九條全留，新增 3 條」並逐條寫理由 |
| 🔴-7 Q4 的答案在 managed-agents，A-8 沒有這條路 | **接受逐字，親改** | 這條補上我整份提案最大的空白：我把 Q4 只當成懸置題處理，沒發現唯一有官方數字的落點是 `entities/managed-agents:82` 的「Opus 5 跑一小時 $0.705」。A-8 加一行，相關議題成 12 條 |
| 🟡-1 硬編「上線 44 天」 | **接受但改寫** | 評審對兩件事：同型復發（本波砍的正是「發布首日資料」），且以 07-24 起算實為 45 天。我不只改句子，**把「上線天數不得硬編、一律寫『自 <發布日> 上線以來（截至 <資料截至日>）』」寫進 B-3 條文**——只改三個句子的話，下一個記者還是會硬編一次 |
| 🟡-2 B-2 判準句①自違 | **接受但改寫** | 採評審的 `—（基準世代）` 寫法，但放進**判準句①本身**而不是另加一句：`—` 與「官方未載」的差別是「這題對它不成立」vs「官方沒寫」，那是同一條判準要分的兩種情況，拆兩句會讓下一個人只讀到一半 |
| 🟡-3 Team standard 一手與推導混寫 | **接受逐字** | A-4 第 7 列出處欄改成「官方公告與說明中心（2026-09-07）；Team standard 依 [[entities/pricing]]「同 Pro」推得」；B-2 判準句②同批加「一格內混有一手與推導時，出處欄分兩截寫明」 |
| 🟡-4 歷史記錄表拿到假綠 | **接受但改寫** | 評審對：只給封存條文的話，census 判「有」而健檢卡點的「缺上限、缺退場、缺圖例」一項未解，且封存門檻 2026-10 才生效、中間三個月無人看守。我不用他建議的「補一句」，改成**獨立 h3「opus-5 的 `## 歷史記錄`：累積式，上限 20 列，逾上限先走封存」**——機制詞要在標題行才過得了 `_mechanism`，補在內文一樣是假綠 |
| 🟡-5 index :36 把「護欄會不會擋我」刪掉 | **接受逐字** | 加一個世代不該砍掉另一個世代的入口；括號改成「Fable 5 vs 5.1、Opus 4.8 vs Opus 5、退役日、護欄會不會擋我、升上去會壞什麼」 |
| 🟡-6 帳本與 H-c0b18f 撞同一張表 | **接受逐字** | `pending_handoffs.py list` 實查確認 H-c0b18f 於 2026-09-07 由模型記者開、改的正是同一張表的旗艦欄。B-6 商業那筆 note 末補「與 H-c0b18f 同一張表，請同批處理」 |
| 🟡-7 callout 看不出「解了沒」 | **接受但改寫** | 評審給「補一句或列回訪」二選一，我兩件都做：callout 末補「官方是否已標記解除，本站截至 2026-09-07 未見更新」（84 字元，仍在 120 內），同時列 09-14 回訪——只補句子的話那句自己會過期，只列回訪的話讀者今天還是看不出來 |
| 🟡-8 基準表混進「官方自陳的邊界」 | **接受逐字** | 表回到四列純基準，資安那一列降到表下條列第一條，一字未刪；B-3 判準句加「只收基準，不收官方自陳的能力邊界」 |
| 🟡-9 節名改 `## 這些數字是誰量的` | **接受逐字** | 評審第 (2) 點正中要害：我自己的導言事實錯誤（🔴-1）就是對照式節名這個滑坡的下游。五處同批改已完成，改名後 `_mechanism` 重測見下 |
| 🟡-10 收工判準少兩項 | **接受逐字** | map 第五節補 `check_rules.py`＋`/review-commands` 到零錯誤、以及「動工前先記下改前的 `build_web.py` WARN 數」（不記基準，「不增」無從驗起）|

**改完重測（不是拿上一輪的數）：** 把改名後的四個 h3 原文餵進 `_mechanism()`——`## 你現在拿到的是什麼`／`## 這些數字是誰量的`／`## 熱度與試用價值`／`## 歷史記錄` 對 `entities/opus-5` **4/4 全「有」**，`fable-5` 的兩代表仍「有」（改名沒有打斷它）。兩閘拿改後的 A 欄 12 個 block 重組整頁重跑：`OK: 讀者語言閘 — 無新增命中`；`check_cell_limits` 那 10 筆 FAIL 逐行 `in` 比對現行檔**全部為 True**，仍是暫存 slug 不在基線的量測假象，我新增或改寫的五段仍為 147／97／131／81／159 字元。暫存檔已刪，`git status --short` 只有本波三份產物。

**裁決點仍 2 個、無新增。** opus-4-8「Opus 5」傳聞節歸檔待使用者（保守預設不動，行號更正為 :152–163）；opus-4-8 三處事實更正屬主 session 代判範圍。本輪所有改動都落在條文層、儲存格層、行號與帳本，沒有產生新的拆頁、併頁或新增頁事項。
