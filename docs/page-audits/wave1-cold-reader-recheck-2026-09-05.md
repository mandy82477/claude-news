# 冷讀者複驗 2026-09-05（wave1）

受測：從 `wiki/index.md` 出發，四道原考題。走過的頁：index → model-task-leaderboard → competitor-landscape → model-comparison（共 4 頁）。

---

## Q1 Codex 和 Claude 誰強？

**答案：拿到了。** 「沒有單一答案——不同基準測不同能力面向」，且附四組實測數字（Supabase Evals Build 階段 Opus 5／Kimi K3 均 100%；SWE-bench Verified 兩者持平 ~88.6–88.7%，Opus 5 後 Claude 升 97.0%；SWE-bench Pro Claude 69.2% vs 58.6%；Terminal-Bench Codex 82.7% vs 69.4%），全部標 2026-08-13 查證。

**路徑：** index:33 →『model-task-leaderboard』:45 →『competitor-landscape#硬答案』:77 → 同頁「查證快照」:170–196。**跳數 3（頁級），加同頁 2 次段內跳。**

三個子問題：
1. **有被明確告知邊界。** 兩處，都清楚：
   - `model-task-leaderboard`:30「帶跑分數字的第三方對照評測不在本站蒐集範圍，因此本頁沒有「Claude 對某家」的頭對頭實測。」
   - `competitor-landscape`:173「本節是 2026-08-13 那一天查到的數字，**之後沒有再更新**」。
   這是這次體驗最好的部分——我知道我停在哪、為什麼停。
2. **有拿到現在的答案（而且是帶數字的）**，不是只被丟去別頁。`model-task-leaderboard`:45 就先給了結論句再給連結，這一句救了整題。
3. **沒有走進死路。** 三次轉介每一次都在落點找到東西。

---

## Q2 跨 40 檔 refactor、估兩小時，用哪個模型？

**答案：Opus 5。單一明確，無歧義。** 三處互相咬合：`model-comparison`:51「跨數十檔 refactor」、:61「比日常大一級、不到跨多天」、:62 Sonnet 5「不適合跨數十檔 refactor（改 Opus 5）」。另 :89 提醒先調 effort（`high`，最吃重才上 `xhigh`）再換模型——這句我覺得比推薦本身更有用。

**路徑：** index:32 → model-comparison:46–62。**跳數 1。**

## Q3 同一份工作換模型差多少錢？

**答案：拿到了，且是全站最好的一節。** `model-comparison`:108–126「換代的實付成本」：舊 tokenizer 10 萬 token 文件為基準的換算表（Haiku $0.13 / Sonnet 5 $0.33 / Sonnet 4.6 $0.38 / Opus 5 $0.81 / Fable 5 $1.63），並點出兩個反直覺結論——Sonnet 5 比 4.6 便宜、Opus 5 牌價沒漲但實付漲 30%。:136 明說是概值不是實測、要自己 `count_tokens` 量。

**路徑：** 同 Q2，同頁下捲。**跳數 1（0 次額外跳）。**

## Q4 畫圖哪家最強？

**答案：GPT Image 2 > MAI-Image-2.6-Preview > Reve 2.1**（09-03 抓取，AA 圖像榜）。改圖是另一張榜、排名不同（MAI 第一），`model-task-leaderboard`:117 有明說「生得好與改得好是兩種能力」——這句很有價值。

**路徑：** index:33 → model-task-leaderboard:59。**跳數 1。**

---

## 每頁：雷達還是百科

| 頁 | 判定 |
|---|---|
| `index.md` | **雷達。** 開發實務入口表（:25–35）直接用「我想……」寫，四題有三題在這裡一次命中。 |
| `model-task-leaderboard` | **雷達（表）＋百科（下半）。** 上半快照表可掃；但 :85–195「評比方式索引」佔全頁一半以上，是純知識，我今天一個決定也用不到。 |
| `competitor-landscape` | **雷達。** 「本週衝擊」三行、對手雷達的「衝擊度 vs 硬度分開兩欄」是我看過最像雷達的設計——它直接告訴我「用 Claude Code 的人這週不必動」。 |
| `model-comparison` | **雷達。** 四題裡兩題在這裡一站解決，且每條建議都帶可自我對號的分界（時長／檔數／延遲）。 |

## 三個最想改的地方

1. **`model-task-leaderboard` 的「本週註記」（:73–83）與「評比方式索引」（:85–195）該收起來。** 註記整段是「哪個榜抓不到、下週再判」——那是產製狀況，不是我的決定。索引寫得很好但體量壓垮了快照表，應折疊或另立子頁。
2. **`competitor-landscape` 的懸置標記 ⟨Q-01⟩ / 「懸置細節」對我是黑話。** 我不知道「懸置」是什麼、⟨Q-nn⟩ 的編號代表什麼、🔎 和 ❓ 差在哪（頁面沒有圖例；🏢 有圖例:64，這三個沒有）。建議至少補一行圖例，或改寫成「這個數字還沒查到」。
3. **`competitor-landscape` 該補一句「所以我現在該用什麼」。** 全頁把外面每一家都排好了，但我讀完仍不知道「我要不要動」——:43 摘要最後一句最接近，可以升成頂部第一句。

## 內部用語外洩（逐句）

讀起來像在對我報告他們自己的工作、或預先辯解：

- `model-task-leaderboard`:8 / :31「🗓️ 週更（每週抓取一次外部榜單快照；**更新日期停留數天屬正常節奏**）」——這是在跟我解釋為什麼日期是舊的，我沒問。
- `model-task-leaderboard`:37「Search Arena **連續 2 週無法取得，列入汰換候選**；……**本頁保留兩說並註明，不擇一**。」——編輯部進度報告。
- `model-task-leaderboard`:80「**是否改由 SWE-bench Pro 或 Terminal-Bench 3.0 承接「寫 code 實戰」語意，尚未定案。**」——內部待辦。
- `model-task-leaderboard`:77「**下週複抓判定**。」同上。
- `competitor-landscape`:70「AgentConnect（新聞稿自宣）**依准入不入表**。」——「准入」是內規詞。
- `competitor-landscape`:74「**每條都要能回答「依據是什麼、哪天查的」**」／:120「**只為衝擊度 🔴 的對手開節，每節只寫「現在」**」——這兩句是寫給作者的規則，貼在我眼前。
- `competitor-landscape`:85「查無具體公開數字者**依懸置標記語法標示**，不可假設。」——「懸置標記語法」我完全不知道是什麼。
- `competitor-landscape`:103 等多處「**已掃日報至 2026-09-03 無後續；官方頁面未查證。**」——工作成績單。誠實我認可，但「日報」是你們的內部產物名。
- `competitor-landscape`:112「**已移出表**」——策展紀錄。
- `model-comparison`:56「**「不適合」的判準亦全數列於選型細節，每個模型一條。**」——自我宣告完整度。

## 哪兩頁讓我搞混

`model-task-leaderboard` 與 `competitor-landscape`。前者 :43 說「跨家與跨工具的『誰強』**到這一頁為止**」，但 :30 又說工具不在榜上、:45 把頭對頭數字丟給後者。所以「到這一頁為止」實際上不成立——我還是得再跳一次。建議把 :43 那句改成「跨家**模型排名**到這頁為止；工具（harness）的頭對頭見競品頁」。

## 一句話

**夠。** 明天要決定用哪個模型，`model-comparison` 一頁就能決定（Opus 5、先調 effort、實付換算表都在），跨家比較也知道停在 08-13 且知道為什麼停——四題全部拿到答案，最大的問題不是缺資訊，是有幾段不該給我看的產製狀況混在裡面。
