# 設計提案：`wiki/topics/code-quality-decline.md`（第 12 波）

使命句（主 session 已定，不再議）：**「Claude 變笨了」這件事，官方認過哪一次、現在還有哪幾條沒答案，以及你能自己量什麼。**
行號＝檔案原始行號（含 frontmatter 24 行）；`git status --short wiki/` 顯示本頁未被他 session 改動（乾淨）。逐字稿見 `-draft.md`（A 進頁面／B 進規則檔與帳本），逐行去向見 `-proposal-map.md`。

## 0. 一句推薦案

**頁不拆、節不併，把「三條線」變成頁面的骨架：** 新增一張三列固定的「三條線現在到哪」表當入口（歷史線已結案／token 線 #65687 仍開／Opus 5 線 #77136＋#83510 仍開），把「你釘不住你選的模型」從 L131 提到摘要第二段，新增「怎麼自己量一次」三步節，並把 Opus 5 那一節從 15 列逐則表壓成 5 列主張表（逐則原文留在時序，逐則判讀指向 [[entities/opus-5]]）。三張訊號群表各補判準／上限／退場／入口，03～04 與 05 兩個時段搬進新建的 `topics/code-quality-decline-archive`。

**不選的方向，各一行：** （前兩項即兩個裁決點，兩版整頁去向見 §8）
- **拆「模型釘選」子頁**：三題確實全過，但子頁約 30 行、最後事件 08-04，2026-10-03 就撞上「< 80 行且 60 天無新聞即提案併回」；五處指路句要回掃、其中四處跨維護者只能走帳本；而拆頁本要解的可見度問題，已由把「你釘不住你選的模型」升到摘要第二段解掉。
- **把 Opus 5 節整個併進 opus-5**：素材確實重疊，但跨維護者只能走帳本，中間會出現「本頁刪了、那頁還沒補」的空窗；壓成主張表＋分工句已解掉冷讀者說的「兩頁打架」，且不製造空窗。
- **砍 `## 影響範圍` 整節**：三條泛化條列確實弱，但砍整節屬使用者裁決——改寫成三條帶出口的句子即可，不新增裁決點。
- **改 H1 或砍 `## 技術彙整`**：H1 改名會動 index 顯示名，`## 技術彙整` 是 topics 模板節名——兩者都留，只換內容。

## 1. 可行性前提（一行）：新建 `topics/code-quality-decline-archive` 後 `check_hierarchy.py` 會要求它有 `**上層：**`、領域繼承 🌐 社群、狀態 `resolved（封存頁）`（腳本 :123–129），且母頁 index 列的 `↳ 子故事：` 投影由 `gen_wiki_frontmatter.py` 產生——三者同一批做完才會綠。

## 2. 骨架（每節標服務哪幾題，Q 編號同健檢卡）

| 節 | 服務 | 現況 → 去向 |
|---|---|---|
| callout | Q2 | 覆寫成「三條線，只有一條有官方說法」（原本用最弱的一則貼文當門面）|
| `## 摘要` | Q1 Q2 Q4 | 三段：三條線一句話／**你釘不住你選的模型**／你的選項。原 L46「首次正式承認」段併入表下 |
| `## 三條線現在到哪`（新） | Q1 Q2 Q3 Q5 | 四欄三列固定＋表下四條細節（三個修法各自的版本、官方措辭、09-03 事件）|
| `## 怎麼自己量一次`（新） | Q4 | 三步各一個動作；CC-Canary 判定值與 log 路徑從技術彙整搬上來 |
| `## Token 消耗異常訊號群` | Q3 | 10 列保留，「強度」欄改「證據硬度」五值＋圖例；981 字元「目前立場」壓成 3 句 |
| `## Opus 5 上線後品質觀感` | Q1 | 15 列 → 5 列主張表；effort 非單調改「已證偽」；補與 discussions 的分工句 |
| `## 模型釘選：你選的不一定算數` | Q4 | 節名去方括號與「靜默」現在式；商業面那條改寫成「官方 06-11 已改為會通知」|
| `## 目前結論` | 全部 | 9 條 → 5 條，每條指得回上面某張表 |
| `## 影響範圍`／`## 技術彙整`／`## 時序` | Q1 | 前兩者各改寫成 3 條帶出口（LLM 無障礙偏差移出，不屬效能退步）；時序新增 09-03 一則，03／04／05 三個月份組搬進 archive、留兩段總結 |

## 3. 結論表：判準、退場、入口（拿現有列跑過一遍）

- **判準句（三條全過才入表）：** ① 有一批以上彼此獨立的回報指向同一個「變差了」的說法；② 「官方說了什麼」欄寫得出一份官方文件與日期，或明寫「無官方專文／無官方回應」，不留白；③ 「你能先做什麼」欄寫得出今天做得到的一個動作。
- **退場：現有三列固定不移除**，一條線結案時改的是「現在還在嗎」那一格。理由是本表回答「哪一條到哪一格」，一條線結案本身就是答案。**跑一遍：** 第 1 列（#41930 已 CLOSED）→ 改寫成「已結案」留表 ✅；第 2、3 列（issue 仍開）→ 留表 ✅。第 4 列（日後新線）才適用「issue 全數關閉或官方結案，且 90 天無新事實即移除，結論留時序」。
- **入口只有兩種訊號：** 出現一批與現有三線不同機制的新回報；或既有線的 issue 狀態、官方說法改變。媒體轉述與熱度回升不構成入口。
- **上限 4 列。** 這張表是本頁的入口層，不是事件流。

## 4. 三張訊號群表的機制（全部從零補）

| 表 | 型態 | 上限 | 入口 | 退場（跑一遍的結果）|
|---|---|---|---|---|
| Token 消耗（現 10 列） | 累積封頂 | 10 列 | 新訊號帶測試方法、版本號或具體數字才佔新列；否則只更新表下「現象還在」那一條 | #65687 關閉或官方發專文 → 整節壓成結論表第 2 列一句。**跑一遍**：08-25 那則無數字，依新入口不佔新列 → 但本波不追溯，10 列原樣留、數字定為 10 |
| Opus 5 品質觀感（現 15 列） | 覆寫式 | 6 列 | 一種**主張**一列；新貼文只改「最後動態」與則數 | 該主張被官方或量測結案，或距最後動態逾 90 天且本輪無新事實 → 移出表，逐則原文留時序。**跑一遍**：五列最後動態 07-26～08-28，皆未逾 90 天 → 全留 |
| 三種解釋（現 3×3） | 覆寫式 | 3 列固定不增減 | 只改格子不加列 | 某解釋被官方或對照實驗排除 → 該列改寫「已排除（日期＋依據）」，仍留表。**跑一遍**：三者皆未排除 → 三列原樣 |

**表滿載時誰讓位（Token 表，寫進規則檔不寫進頁面）：** 提供機制解釋的列與 GitHub issue 列不讓位；讓位者取「單一貼文（無互動數／無機制）」中最舊者，降為表下一行並確認時序有家。**跑一遍**：讓位者＝07-08「Max 20x 週額度不到一天用盡」（時序 :258 已有家）✅。

**證據硬度欄——那句「結構性未解問題」撐不起來：** L85 用十則訊號（其中四則自承無互動數）把定調上調為「結構性未解問題」——冷讀者判為全頁最不可信的一句。處置：**加「證據硬度」欄（五值：官方一手／GitHub issue（附留言數）／具名個案／單一貼文（附真實互動數）／單一貼文（無互動數）），圖例一行放表下**，讓讀者自己排序；定調句改成「十則方向一致、來源獨立，但沒有一則帶測試方法或版本前後對照，三種解釋都排除不掉」——比刪掉那句誠實，也比留著安全。

## 5. 數字定值（七處）

| 打架的地方 | 定值 | 依據 |
|---|---|---|
| 官方承認日 :40 04-23 vs :46／:318 04-24 | **04-23**（官方發文日）；04-24 是本站收錄日 | verified 一 |
| 「三者皆於 v2.1.116 修復」 | **三個修法各自的版本**：v2.1.116／v2.1.101／04-20 服務端回復 | verified 一 |
| 「工程疏失」 | 官方原文「three separate engineering changes」；中文「工程疏失」標明是媒體轉述 | verified 一 |
| 則數 :93 14 則 vs 表 15 列 vs `opus-5:98` 五則 | 本頁 **15 則**（表上就是 15 列）；並寫明 opus-5 的五則只收「對 Opus 5 這個模型的觀感」，兩邊口徑不同 | 本頁自數 |
| 同一貼文 :106 08-19 vs :224 08-20 | **08-20**（週熱門重浮上日），與 `opus-5:167` 一致 | 跨頁對齊 |
| effort 非單調日期 :97／:253 07-25 vs `opus-5:190` 07-29 | **07-29**，且說法標「已證偽」 | verified 二＋第 11 波定稿 |
| 「九個獨立訊號」:52 vs 表 10 列 | **10 則** | 本頁自數 |

**鄰居的強否定句**：`opus-5:86`「社群沒有交出任何一則帶測試方法或數字的獨立複測」已自帶「自 2026-07-24 上線以來」的限定，與本頁 :284 MarginLab 不衝突——**衝突來自本頁沒寫清楚對象**。定一個說法：本頁凡提 MarginLab 一律寫「對象是 Opus 4.7、不是 Opus 5」（見 draft A-5、A-13）。不動 opus-5，不開帳本。

## 6. 內部用語 10 條逐條處置（冷讀者列十條，`check_reader_language --page` 回 0——閘只擋詞、擋不了口吻）

| # | 原句（行） | 處置 |
|---|---|---|
| 1 | :93「本節為則數統計的家」 | 刪；則數只寫在該節導言一處 |
| 2 | :127 節名 `` `[2026-08-09 查證新增]` `` | 節名改「模型釘選：你選的不一定算數（2026-02 起）」，查證日寫進內文 |
| 4 | :65 等「score 不可信」（本頁 10 處） | 改成「證據硬度」欄的固定值「單一貼文（無互動數）」，正文不再逐則寫 |
| 5 | :69 等「僅計入現象延續」（本頁 10 處） | 改「只說明這個現象還在」 |
| 7 | :61「至今無後續」／:140「已掃日報至 2026-09-02」 | 改「07-14～07-31 未見同一主張的第二來源」「截至 2026-09-07 未見第二來源」。**閘的缺詞仍是 ledger 第 3 波遺留待辦，本波不動四處** |
| 8 | :68／:82「本頁不重複收錄細節」 | 刪，改成直接指路「完整追蹤在 [[entities/claude-code]] 已知問題」 |
| 3・6・9 | :89 `%%` 候選症狀備忘、:107／:113 `⟨Q-01⟩` 與「懸置細節」、:117／:79「表格細節」「假說細節」 | **三條全留**——分別是 `check_tools_page` 的缺口態格式、`wiki-ingest-format.md` 表格變體強制形式（`check_pending_markers.py` 基線 140 在數）、全站通用的下沉區名稱 |
| 10 | :34 callout 用最弱一則當門面 | 覆寫成三條線一句話 |

**新增閘詞建議（給評審判，本波不執行）：** 「僅計入現象延續」全庫 10 次／1 頁，全部在本頁——零附帶傷害，是乾淨的加詞候選；「週熱門」142 次／15 頁、「score 不可信」27 次／4 頁，屬存量遷移，不宜本波加。

## 7. 蒸餾與 archive

| 時段 | 條目 | 原字元 | 例外檢查 | 處置 |
|---|---|---|---|---|
| 2026-03～04（合為一時段，主 session 代判） | 7 | 1,461 | **例外 2 成立**：v2.1.116 三成因、Boris 50+ 的唯一細節出處在 04-23／04-24 兩則 | 先把三個修法寫進結論表**表下細節**、把 50+ 的結果寫進「目前結論」最後一條（結案，不再掛「待觀察」），例外 2 即解除 → 搬 |
| 2026-05 | 5 | 1,426 | 例外 2 不成立、例外 3 不適用（`wiki_graph explain --section` 錨點入邊 0） | 搬；MarginLab 與 11.5 倍兩個仍被上層引用的事實各留一句在正文（見 draft A-5、A-9）|

- **留正文名單（四件）**：04-23 三個修法（結論表表下）／Boris 50+ 結案句（目前結論）／CC-Canary 判定值與 log 路徑（怎麼自己量一次）／MarginLab 與 11.5 倍（怎麼自己量一次、模型釘選節）。
- **錨點與反向指針**：archive 是新建頁，不存在「archive 反指母頁被搬條目」的情況（規則檔要求先確認的那一項）；archive 開兩個 h2 `## 2026-03～04` 與 `## 2026-05`，母頁總結末行分別指 `[[topics/code-quality-decline-archive#2026-03～04]]`、`#2026-05`。`build_web.py:276` 的 `ANCHORED_WIKILINK_RE` 只要求 `#` 前至少一字元，`check_wikilink_anchors()`（:307–328）逐字比對 h2 原文，全形「～」不受影響。
- **規模**：329 行／28,410 字元（去 frontmatter）→ 約 **318 行／20,846 字元**（**行 −11、字元 −26.6%**）。行數幾乎不減是因為原頁多個 900+ 字元單行段落被拆成條列——同第 11 波的形狀。

## 8. 裁決點（2 個，各兩版整頁去向）

**裁決點 1：「模型釘選」要不要獨立成子頁**（預設不拆）

| | A. 不拆（推薦） | B. 拆 |
|---|---|---|
| 本頁 | 節名改「模型釘選：你選的不一定算數」，四機制＋11.5 倍＋成本影響留在頁上（約 14 行） | 該節整段搬出，母頁留 3 行索引＋「你釘不住你選的模型」結論句 |
| 新頁 | 無 | `topics/model-pinning-not-guaranteed`：標頭領域 🌐 社群、`**上層：** [[topics/code-quality-decline]]`、不入 index 目錄表 |
| 回掃 | 0 處 | `claude-code:177`／`:178`、`model-comparison:138`、`long-context-1m:69`／`:90` 五處指路句改指新頁——**四處跨維護者，全部走 `pending_handoffs.py`** |
| 代價 | 母頁多 14 行 | 新頁約 30 行、最後事件 08-04 → **2026-10-03 起自己符合「< 80 行且 60 天無新聞即提案併回」**；且指路句改指前有跨維護者空窗 |
| 一句理由 | 10-03 撞併回門檻＋五處回掃四處跨維護者＋可見度已由摘要升位解掉（**行數不是理由**：09-07 判準已把行數踢出拆分依據，第一輪那句已刪）| 子故事三題確實全過，且三頁逐字指名這一節 |

**裁決點 2：「Opus 5 上線後品質觀感」要不要併進 `entities/opus-5`**（預設不併）

| | A. 不併（推薦） | B. 併 |
|---|---|---|
| 本頁 | 15 列逐則表 → 5 列主張表，逐則原文留時序；補分工句「本頁記這條線的狀態，opus-5 記逐則觀感」 | 整節刪，只留結論表第 3 列＋一句「逐則見 opus-5」 |
| opus-5 | 不動 | 需補進本頁獨有的三則（08-14×2、08-19）——跨維護者，走帳本 |
| 懸置 | ⟨Q-01⟩ 留本頁，標記基線 140 不動 | ⟨Q-01⟩ 必須成對搬或改寫進時序 08-25，**否則 `check_pending_markers.py` 基線 140 會靜默下降**（保命條款已四波四次命中）|
| 空窗 | 無 | 本頁刪了、opus-5 補上之前，本頁獨有三則只剩時序有家 |
| 一句理由 | 壓成主張表＋分工句解掉「兩頁盤點同一批證據」，且不製造跨頁空窗——**但必須配帳本第 7 筆給 opus-5 一句指回本頁**（該頁對本頁零連結，單邊分工句解不掉「分不出」）| 素材重疊、結論相反，冷讀者判為分不出差別的一對 |

## 9. 與鄰居的分工句與帳本（六筆）

- **本頁側**：`## 相關實體` 改成六條分工句（claude-code 逐個 issue／opus-5 逐則觀感／radar 升上去會壞什麼／pricing 帳單／discussions 那場觀念之爭／其餘）。**discussions 第 6 列互斥**屬同維護者，只在該列「官方怎麼說」欄末加一句指本頁、本頁寫明「不重判那場爭論」，兩頁都不改對方的狀態值。
- **index**（主編文字）：首屏 20 列表在 :28 之後插一列「我沒升版，但它好像變慢了、帳單也變多」；:97 鉤子從「已承認工程疏失」改成三條線＋「你釘不住你選的模型」。
- **feature-radar**（主編文字）：升版節表下收尾句後加一句「你沒升版卻覺得變慢，見 [[topics/code-quality-decline]]」——目前 radar 無任何邊指本頁。
- **帳本八筆**（逐字見 draft B-6；第二輪由六筆增為八）：功能記者 3 筆（`claude-code:213` v2.1.116 口徑、`:177`／`:178` 稱呼、`long-context-1m:69`／`:90` 稱呼、LLM 無障礙偏差 #56079 落點）／模型記者 3 筆（09-03 已於 16:16 UTC 解決——`opus-5:175`、`fable-5:187`、`mythos:129` 三頁同批；`model-comparison:138` 稱呼；opus-5 補一句指回本頁的互指句）／人物記者 1 筆（`boris-cherny:186` 跟上 50+ 結案）／商業記者 1 筆（`pricing` 事故表六列無「token 消耗異常」這一維度，評估補一列或表下一句指本頁——冷讀者 Q3 兩頁互不接的直接原因）。

## 10. 自己過閘的輸出（含驗紅）

用 `check_reader_language.py` 與 `check_cell_limits.py` 的同一段 `scan()` 掃逐字稿（harness：`scratchpad/cqd_gate.py`；`--page` 只吃 wiki 下的檔，故改用同函式）。先驗 harness 與真閘一致：掃現行頁得「命中 0 筆／超限 29 筆」＝閘的存量基線 29 筆／0 筆，相符。

```
== 逐字稿三檔（cqd-new-body.md ＋ cqd-new-extra.md ＋ cqd-code-quality-decline-archive.md）==
  check_reader_language.scan  命中 0 筆
  check_cell_limits.scan      超限 0 筆
== 驗紅（注入 125 字元儲存格＋215 字元條列＋五個禁詞）==
  L31 [lint] [主編] [覆寫] [退場] ... L78 [汰出] [留表優先序]   命中 6 筆
  L31 table_cell len=125/120 ／ L78 list_item len=215/200        超限 2 筆
== 還原後 ==
  命中 0 筆／超限 0 筆
```

**第二輪依 7 🔴 10 🟡 改完後重跑，仍為命中 0 筆／超限 0 筆。**
過程中修掉的：3 筆 200 字元超限（effort 更正段 230、兩段時段總結 359／308）與時序 08-28 那條（227→199），已拆條或縮句後重跑歸零；archive 頁的「封存頁」狀態值命中禁詞 1 筆，是 `check_reader_language.py:287` 的 `pid.endswith("-archive")` 豁免——把測試檔改名為 `-archive` 結尾後歸零，正式頁 slug 本就以 `-archive` 結尾。

## 11. 「X 會看守」的憑據（附行號）

| 宣稱 | 看守者 |
|---|---|
| archive 掛父、領域繼承、狀態 `resolved（封存頁）`；母頁 index 列的 `↳ 子故事：` 投影 | `scripts/check_hierarchy.py:123–129`、:153（投影由 `gen_wiki_frontmatter.py` 產）|
| 兩個 archive 錨點指得到 | `scripts/build_web.py:307–328`（只發 WARN、不中斷建置——要看 WARN 數不增，不能只看 exit code）|
| 懸置標記數不得靜默下降／儲存格 120／條列 200 | `check_pending_markers.py` 對 `data/pending-marker-count.json`（`count: 140`）／`check_cell_limits.py:50–51` |
| 內部用語 | `check_reader_language.py:51–163` 的 `TERMS`（**本頁十條外洩沒有一條在清單裡**）|
| 三張表有沒有機制 | `scripts/table_census.py` 的 `_mechanism()`——所以規則檔新增的三個 h3 都逐字含 slug `code-quality-decline`（draft B-2）|

## 12. 我最沒把握的一處

**「Opus 5 上線後品質觀感」壓成 5 列主張表時，「泛化變慢」那一列的 6 則裡有幾則其實不指名 Opus 5。** 我把 07-29「Is Claude getting dumber」、08-14 兩則、08-19、08-26「I miss the old Claude Code」、08-28 歸進「泛化變慢」，但這一節的節名寫著「Opus 5 上線後」——等於用一個模型的上線日圈住一批不指名模型的抱怨。這件事原頁就有（:110 自己寫「未指名模型版本，無法歸入特定模型的品質爭議」），我沒有解決它，只是讓它變得看得見。若評審認為該把「不指名模型的泛化抱怨」再切成一條獨立的線（那會讓結論表變成四列），我沒有反對的證據。

---

## 第二輪：評審條目→處置（7 🔴 ＋ 10 🟡 ＝ 17 條）

**統計：接受 12、接受但改寫 5、反駁 0。** 磁碟現行的 `-draft.md` 與 `-proposal-map.md` 即定稿。

| # | 處置 | 說明 |
|---|---|---|
| 🔴-1 則數 6／4／3／1／1 算錯 | **接受** | 我逐列重算過，評審對：08-07「I defended Opus 5」家在 `opus-5:187` 不在本頁、08-20「Losing Me」原本沒被分進任何一列、08-28 被泛化與 A/B 雙計。改 6／**3**／3／1／1，表內 14 ＋ 已證偽的 effort 1 ＝ 15。draft A-8 表第 2 列與「則數怎麼算」整段重寫 |
| 🔴-2 導言「四種主張」對 5 列、全稱句被自己表格推翻 | **接受**，逐字照貼 | 同批改 A-4 第 3 列儲存格為「十五則裡十二則是單一使用者觀感」。本條同時吃掉我第一輪 §12 自報的「節名圈住不指名模型的抱怨」——導言明說收料起點是上線日不是歸因，六則未指名模型 |
| 🔴-3 判準條文寫進正文三處 | **接受但改寫** | 三處 blockquote 照評審逐字改、條文進 `%%`；A-4 表下第 1 條整條刪除。**我另加一件**：`%%` 內的指路句補上規則檔的節名與條號（原稿只寫「見規則檔第 N 條」找不到家） |
| 🔴-4 lint 步驟無觸發邊＋`gh` 指令跑不起來 | **接受但改寫** | 5m 全文照貼（含 `-R anthropics/claude-code`；我跑 `git remote -v` 確認 remote 是 `mandy82477/ObsidianLab`，評審對）。**我另加一段**：說明 5m 與現有 5k 不重疊——5k 派社群記者做三張表的退場，5m 只做主編才做得到的 issue 狀態那一格（我核過現有步驟為 5a–5l，確無 5m） |
| 🔴-5 opus-5 對本頁零入邊 | **接受** | 帳本第 7 筆逐字照貼，並在提案 §8 裁決點 2 的表上寫明「這一筆是不併能成立的前提，不是附加項」 |
| 🔴-6 `discussions:325` 引用舊節名 | **接受** | 逐字進 draft B-7 第二處，與 B-7 原有的第 6 列互指句同批（同維護者，合法射程內）。`:486`／`:590` 引的 Token 節名本波不改，兩處安全 |
| 🔴-7 規則檔指向不存在的節 | **接受** | A-7 粗體行升為 `### 三種解釋，目前各自站在哪`；規則檔 B-2 §3 標題與 §6 末句同批對齊。map §三 L71 那一列已標「（二輪）」 |
| 🟡-1 callout 首句不是當日內容 | **接受**，逐字照貼 | |
| 🟡-2 硬編碼 pricing 列數 | **接受但改寫** | 「四件計費爭議」→ A-3 用「幾件未解的計費爭議」、A-10 第 4 條用「數件」（兩處語境不同，不共用一個詞）；「09-14 起」→「週配額換軌（09-14 生效）」兩處同改 |
| 🟡-3「週熱門」只清一半 | **接受** | 硬度欄兩處改「登上該週熱門榜」；時序標題 `#### 2026-08-20（週熱門重浮上…）` 屬既有存量，本波不動，記進 ledger 回訪 |
| 🟡-4 09-03 更正只開三分之一 | **接受** | 帳本第 5 筆 note 改成一次點名 `opus-5:175`、`fable-5:187`、`mythos:129` 三頁，並註明 `sonnet-5:114` 已寫「同日解決」、四頁口徑不一 |
| 🟡-5 `boris-cherny:186` 沒跟上結案 | **接受** | 帳本新增第 8 筆（人物記者） |
| 🟡-6 `overview:77` 未列入主編清單 | **接受** | draft B-4 補一行逐字 |
| 🟡-7 規模數字對不上 | **接受但改寫** | 兩個都是實作前估值，我不主張哪個對。map 標頭改成並列兩組數字＋「實作後以實檔重量再回填 ledger，不得照抄本行」 |
| 🟡-8 A-13 末條四個裸連結 | **接受**，逐字照貼 | |
| 🟡-9 A-5 與 `skill-interest-watch:144` 互斥 | **接受**，逐字照貼 | 這一條補完後，本頁與工具頁對「有沒有推薦工具」給的是同一個答案 |
| 🟡-10 B-2 §1 內部矛盾 | **接受但改寫** | 補「第 4 列的新增不需使用者裁決（判準三條全過即可）；移除三條既有線才需要」；同段「重查三個 issue」順手改「四個」（表上引了四個 issue 編號） |

**裁決點仍是 2 個，沒有新增。** 評審同意兩個裁決都維持預設（不拆、不併），只要求改寫拆子頁的理由句——已照辦：刪掉「318 行遠低於鬧鐘」那句（09-07 判準已把行數踢出拆分依據，拿行數論證「問題不夠多」是同一個錯的鏡像），只留 10-03 撞併回門檻、五處回掃四處跨維護者、可見度已由摘要升位解掉三項。

