# 冷讀者實測 · Wave 1（2026-09-05）

身分：每天用 Claude Code 寫程式的付費訂閱者。只從 `wiki/index.md` 出發、只點連結。

---

## Q1「Codex 和 Claude 誰強？」

**答案：拿到一半。** 我拿到的是「沒有單一答案」＋三週前（08-13）凍結的四組跑分；**沒拿到今天的答案**。

路徑：`index.md` L95（競品動態摘要寫「戰場從誰更強移到誰更便宜」）→ `topics/competitor-landscape.md` L77「硬答案」→ 同頁 L170「查證快照」→ `topics/model-task-leaderboard.md`。**跳數 3（被轉介 2 次）。**

- L77 原句：「**Codex 和 Claude Code 誰強？** 沒有單一答案——…該看的是任務類型對應的基準與 [[topics/model-task-leaderboard]] 本週快照；當時的實測數字見下方「查證快照」節（2026-08-13 查證）。」
- 查證快照給了數字：SWE-bench Verified 持平 ~88.7%、SWE-bench Pro Claude 69.2% vs 58.6% 領先、Terminal-Bench Codex 82.7% vs 69.4% 領先。**但 L172 明寫「之後沒有再更新」**，且對照的是 Opus 4.8 世代——現在的旗艦已經是 Fable 5.1／Opus 5，這組數字我不敢拿來做決定。
- 轉介去的「本週快照」是**死路**：那張表 18 列裡**沒有一列叫 Codex**（只有 GPT-5.6 Sol、GPT-5.5、gpt-5 (high)）。我不知道 Codex CLI 現在跑的是哪個模型，表上也沒說。這是唯一被指名「去看最新」的地方，而它答不了我原本的問題。
- 卡點原句（leaderboard L52 相鄰 L44）：`[寫 code（Aider 實戰）] | gpt-5 (high) > …（⚠️ 榜停更，連續 5 週）| 2025-08-23` —— 一個 2025 年的資料日期出現在「本週快照」裡。

## Q2「跨 40 檔 refactor，估兩小時，用哪個模型？」

**答案：拿到，且唯一——Opus 5。** 通過。

路徑：`index.md` L32「寫 code 該用哪個模型」→ `topics/model-comparison.md` L44 快速選型表。**跳數 2。**

原句（L50）：「[[entities/opus-5]] | 次旗艦 | $5 / $25 · 1M | **數小時自主編碼、跨數十檔 refactor**、複雜系統工程」。選型細節 L63 還把邊界寫死：「比日常大一級、但不到跨多天」。Sonnet 5 那列反向寫「不適合跨數十檔 refactor（改選 Opus 5）」，兩邊對得起來，我不會落在兩列之間。這題是全站最好的一頁。

唯一雜訊：L57 那句「換模型不是唯一旋鈕…先跑 effort sweep」很好，但它出現在我剛拿到答案的正下方，第一次讀會以為前面的建議被推翻。

## Q3「同一份工作，換模型差多少錢？」

**答案：拿到，有具體數字。** 通過。

路徑：同上，`topics/model-comparison.md` 頁內 L127「同一份工作，換設定差多少」（快速選型表 L57 有指過去）。**跳數 2。**

拿到的是一張 10 萬 token 文件的換算表（Haiku $0.13／Sonnet 5 $0.33／Opus 5 $0.81／Fable 5 $1.63，相對倍數 1×–13×），還有兩條我自己算不出來的結論：Sonnet 5 比 4.6 便宜（牌價降 33%、token 增 30%，淨省）；**Opus 5 比 Opus 4.6 牌價一樣卻實付貴 30%**。並且誠實標了「這是換算不是實測，遷移前用 `count_tokens` 自己量」。這是四題裡最有用的一段。

## Q4「畫圖這類任務目前哪家最強？」

**答案：拿到——GPT Image 2 > MAI-Image-2.6-Preview > Reve 2.1。** 通過。

路徑：`index.md` L92 →`topics/model-task-leaderboard.md` L54。**跳數 2。**

小卡點：資料日期欄寫「未載（09-03 抓取）」，我看不懂那代表榜單本身多舊；改圖榜寫「08 月」，也不知道是 8 月幾號。

---

## 總評

### 每一頁：雷達還是百科

| 頁 | 判定 |
|---|---|
| `index.md` | **百科**——78 列實體表全部平鋪，我要找的三個入口全在 L23–L34 那張小表裡，下面兩張大表對我做決定毫無用處。 |
| `topics/competitor-landscape.md` | **雷達**（上半）＋**百科**（下半）——「本週衝擊」和「對手雷達」是真雷達，看完就知道這週不必動；但「查證快照」「懸置細節」「未列入上表」是把知道的都倒給我。 |
| `topics/model-comparison.md` | **雷達**——四題裡唯一每一節都在幫我做決定的頁。 |
| `topics/model-task-leaderboard.md` | **百科**——它告訴我 18 個榜今天誰第一，但沒有一句告訴我「所以你該換嗎」。 |

### 三個最想改的地方

1. **`model-task-leaderboard` 沒有 Codex 這一列**（該頁「本週快照」表）。competitor-landscape L77 把我送過來，我卻找不到我要比的東西。至少該在表上或註記寫一行「Codex CLI 目前跑 GPT-5.6 Sol，看該列」。
2. **`competitor-landscape` 的「查證快照（2026-08-13）」該直接消失或改寫**（L170–L189）。它是全頁唯一有跑分數字的地方，卻自己聲明過期；我看到數字就會用，警語擋不住。要嘛更新，要嘛只留一句「跨家跑分見 leaderboard」。
3. **`index.md` 的兩張大表（L40–L114）該收起來**。我在裡面找「Codex」找不到（它不是一頁），是靠 L95 摘要裡「競品」兩個字猜的。開發實務入口表（L25–L34）才是我要的，它應該是整頁的主體。

### 內部用語外洩（逐句）

- `index.md` L86：「Topics 頁面本身無「類型」欄位，故表格僅三欄（領域 / 狀態 / 摘要），**為刻意設計差異**（Entities 四欄含類型）」——這在跟誰解釋？
- `index.md` L29：「我關心的某類工具，現在誰最大、本庫怎麼判斷（**本週竄升欄累積一週星史後啟用**）」——「累積一週星史後啟用」是你們的排程，不是我的事。
- `index.md` L97：「**機器產出**、星數是規模不是品質」。
- `competitor-landscape.md` L67：「⟨Q-06⟩ 🔎 **查無官方**（**標 2026-08-29｜查 Google、企業定價、Antigravity｜複 2026-09-12**）」——`標／查／複` 這三個縮寫我完全看不懂，整串括號像是內部工單編號。同一形狀在 index L80、本頁「競品定價細節」多處重複。
- `competitor-landscape.md` L70：「**未列入上表**（**⚪ 級**，動態仍記在下方時序）…**依准入不入表**」——「⚪級」「准入」是你們的收錄規則。
- `competitor-landscape.md` L64：「**缺一不標**」——標給誰看？
- `model-task-leaderboard.md` L36／L69：「Search Arena 連續 2 週無法取得，**列入汰換候選**」「**是否改用其他榜單尚未定案**」；L74：「是否改由 SWE-bench Pro 或 Terminal-Bench 3.0 **承接「寫 code 實戰」語意**，尚未定案」——這是你們的維護待辦，對我沒有意義。
- `model-task-leaderboard.md` L30：「🗓️ 週更（每週抓取一次外部榜單快照；**更新日期停留數天屬正常節奏**）」——像在對我預先辯解。

### 哪兩頁讓我搞混

**`topics/model-comparison` vs `topics/model-task-leaderboard`。** 兩頁都在講「哪個模型強」，index 的摘要（L91／L92）也都以「我該用哪個／哪家最強」開頭。真正的分界寫在 competitor-landscape L204，不在 index 上：「model-comparison 決定我這份工作該用哪個模型；leaderboard 是跨家能力的活榜單」。我是在第三頁才學到這件事的。

同一組還有第三頁：**`competitor-landscape` 的「硬答案」節**也在回答「誰強」，於是「誰強」這個問題散在三頁，每一頁都把我推給另外兩頁。

### 一句話

**如果我明天要決定用哪個 Claude 模型，夠了（`model-comparison` 一頁就夠，含錢）；如果我要決定該不該從 Claude 換去 Codex，不夠——那個答案在這站上是三週前的。**
