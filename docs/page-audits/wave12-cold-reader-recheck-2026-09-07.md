# Wave 12 冷讀者複驗（2026-09-07）

## 身分與規則

用 Claude Code 八個月的資深工程師，Max 方案。最近兩週覺得它變笨、token 燒得快，想知道是模型退步、我的用法、還是被偷換掉了；也想知道要不要釘版本。有 20 分鐘。
唯一入口 `wiki/index.md`，只沿 `[[連結]]` 走；不猜檔名、不 Glob、不全庫 grep、不讀 `docs/`／`.claude/`／`scripts/`／`news/`。行號含檔案最上方 frontmatter 一起數。

## 路徑清單

| 跳 | 頁面 | 怎麼來的 |
|---|---|---|
| 1 | `wiki/index.md` | 入口 |
| 2 | `wiki/topics/code-quality-decline.md` | index:29「我沒升版，但它好像變慢了、帳單也變多」 |
| 3 | `wiki/entities/pricing.md` | index:30；退步頁 :44、:56、:182 也都指這裡 |
| 4 | `wiki/entities/claude-code.md` | index:28「壞掉的東西見…現在會咬到你的」 |
| 5 | `wiki/entities/opus-5.md` | 退步頁 :57、:127 指過去 |

---

## Q1「越用越笨」是真是假、官方認過什麼

✅ 拿到（2 跳）。`code-quality-decline.md:40` 摘要一句拆三條線；:55 給官方原文〈An update on recent Claude Code quality reports〉2026-04-23、issue #41930 於 04-24 關閉、:61 列出三個成因與各自修法版本號。:62 還特地說「中文媒體寫成『工程疏失』，那是轉述，不是官方說的話」——這句幫到我，它讓我知道作者分得清官方講過什麼。

## Q2 現在還有沒有進行中的退步？該釘版本嗎

⚠️ 半拿到（2 跳）。「還在」的兩條很清楚（:56 token 異常 #65687 仍開、:57 Opus 5 觀感 #77136／#83510 兩則皆開）。
釘版本這題答得比問題好：`:42`「**你釘不住你選的模型**」＋ #27892 以 not planned 關閉，:156–159 四種機制。問題在**它不在我被送到的那張表裡**——index 那一格點名的是「三條線現在到哪」，那張表四欄沒有一欄回答「該不該釘」；答案在表上方 :42 和頁面中段 :152。我是往回捲才撞到的。

## Q3 token 暴增解了沒？帳單變多是不是這個原因

✅ 拿到（3 跳）。`:56` 未解、無官方專文；`:121` 十則方向一致但沒有一則帶測試方法；`:44`＋`:182` 明講「帳單變多不等於模型變差」，把我推到 pricing。`pricing.md:36` 09-14 週配額變 83%、`:37` $100 credit 09-17 作廢、`:217–224` 六件事故表。這是本次最乾淨的一條動線。

## Q4 靜默降級／被換模型，怎麼查自己有沒有中

⚠️ 半拿到（2 跳）。`:65–71`「怎麼自己量一次」三步：量 `~/.claude/projects/` 的 JSONL、跟同 repo 同類任務的舊 session 比、用 CC-Canary 判 HOLDING／SUSPECTED／CONFIRMED。前兩步今天就能做；第三步是**唯一的判定線，卻掛在一個作者自己在 :75 說「本庫沒有把它列為推薦工具」的工具上**。等於「多少算異常」這一問沒有站內答案。
「被換掉」那半：`:157` #46221（picker 狀態不保持、session 中途降級）、`:158` 商業面回退官方已改成會通知——這半是好的。

## Q5 三頁分得出差別嗎

✅ 拿到（4–5 跳）。`code-quality-decline.md:199–206`「相關實體」一段就是分工說明書：本頁＝哪條線官方回應了＋怎麼自己量；`claude-code`「現在會咬到你的」＝逐個 issue 現在什麼壞了；`opus-5`「這些數字是誰量的」＝那個模型的官方基準與逐則觀感。實際翻過三頁，內容確實不重疊。唯一會混的是退步頁的「Opus 5 上線後品質觀感」節與 opus-5 頁——見下方特別提問 2。

---

## 特別提問 1：入口首屏

首屏（index 前 45 行）**有**路，而且是為我這題寫的：`index:29`「我沒升版，但它好像變慢了、帳單也變多（是模型退步、計費、還是我的用法）」→ 退步頁「三條線現在到哪」。括號裡三個候選解釋剛好是我腦裡的三個問號，這句是全表最像人話的一格。
看不看得見：勉強。它是 22 列大表的第 6 列，前 5 列全是「接手 repo 該設定什麼」「我卡住了」這類 how-to，掃到第 6 列前我已經在考慮改用搜尋。
**那格把我送到一張答不了「該不該釘版本」的表**：是。目標「三條線現在到哪」四欄是線／官方說法／現在還在嗎／你能先做什麼，沒有釘版本這一欄；真答案在同頁 :42 與 :152。首屏承諾三選一的分診，落點卻只答得出其中兩個。

## 特別提問 2：幾頁在講退步、哪裡打架

碰到 3 頁：退步頁、`opus-5`、`claude-code`（外加 `pricing` 講帳單那一面）。

分不出差別的兩頁：**退步頁的「Opus 5 上線後品質觀感」節（:125–148）與 `opus-5.md`「這些數字是誰量的」（:84–104）**。兩節都在數同一批貼文、都下「證據不夠」的結論，差別要讀到退步頁 :127 最後一句才說得出來。

同一事件兩處寫得不一樣：

| 事 | A | B | 差在哪 |
|---|---|---|---|
| 09-03 全模型錯誤率事件 | `code-quality-decline.md:63`「同日 16:16 UTC 標記解決」 | `opus-5.md:36`「官方是否已標記解除，本站截至 2026-09-07 未見更新」 | 一頁說收了、一頁說不知道，同一天、同一件事 |
| Opus 5 訊號則數 | `code-quality-decline.md:57`「十五則裡十二則」 | 同頁 `:127`「下表五種主張收 14 則…十四則裡十二則」 | 同一頁自己 15／14 打架 |
| 社群證據性質 | `opus-5.md:44`「社群累積的**全是**單一使用者觀感」 | `code-quality-decline.md:138` 列一則 GitHub issue #80988 已查證屬實；`opus-5.md:101` 自己引 #77136（106 則留言） | 「全是」被自己的下文推翻 |

各頁頂部看不看得出「現在」：**看不出**。每頁標頭同時有「最後更新」與「最後新聞更新」兩個日期（退步頁 :30／:31 是 09-07 與 09-03），沒有一句話說哪個代表「這頁的資訊新到哪」。callout 自己帶第三個日期（:33 的 2026-09-03）。三個日期、零解釋。

## 特別提問 3：每頁總評（百科／雷達）

| 頁 | 判 | 理由 |
|---|---|---|
| `code-quality-decline` | **雷達（偏重）** | 三條線表＋「你能先做什麼」欄＋目前結論每條帶「你可以：」，都是行動導向；但下半（技術彙整、時序 90 行）是百科尾巴 |
| `pricing` | **雷達** | 09-14 有倒數、事故表每列有「你該做的事」、:48 直接列選項含「什麼都不做」 |
| `claude-code`「現在會咬到你的」 | **雷達** | 八列全帶「誰會遇到」，我三秒就知道七列與我無關 |
| `opus-5` | **半百科** | 「你現在拿到的是什麼」是雷達；「這些數字是誰量的」是誠實的百科，答不出「所以我該不該用它」 |

## 特別提問 4：內部用語外洩（原句＋行號）

1. `code-quality-decline.md:129`「一種主張一列，同一批貼文不會在兩列各算一次」——這是編表規則，不是我要知道的事。
2. 同上 `:98`「證據硬度五值（由來源決定，不由結論多強決定）」——「由結論多強決定」是寫給自己的告誡。
3. 同上 `:127`「**收料**起點是 Opus 5 的上線日」。
4. 同上 `:148`「本頁**不重判**那場爭論」。
5. 同上 `:163`「本頁只**採計**現象，不採計其量化宣稱」。
6. 同上 `:183`「**本站於 2026-09-07 停止追蹤這一條**」——讀起來像編輯部公告。
7. 同上 `:199`「本頁答的是…相鄰的頁各答一題，不重複」——分工說明有用，但措辭是內部的。
8. 同上 `:75`「**本庫**沒有把 CC-Canary 列為推薦工具（社群工具目錄對這個症狀還沒有**首選**）」。
9. `pricing.md:211`「本表**上限 6 列**，滿載時較新的活躍事故會暫時列在下方事件流（標「未列入總表」），一併掃一次才完整」——要我配合表格容量上限自己補掃。
10. `claude-code.md:87`「**上限 8 列**；完整清單見下方『已知問題』分組」。
（另：退步頁 :51、:77、:107、:130 與 pricing :213、:215 是 `%%…%%` 包起來的維運備忘，在 Obsidian／網站上不顯示，未計入上面十條。）

## 特別提問 5：「這是在騙我／這數字哪來的」

- `code-quality-decline.md:161`「一名開發者連續 36 天記錄使用數據，量出不同模型間高達 **11.5 倍**的效率差距」——全頁最大的一個數字，**沒有連結、沒有日期、沒說是誰**。旁邊每一則都附原文連結，唯獨它沒有。
- 同上 `:183`「Boris Cherny…承諾的 50+ 修復，社群逐項驗證未見完成結果；本站停止追蹤」卻標 **✅**，而 :185 圖例說 ✅＝已有結果。內容是「查不到結果所以不查了」，符號說「已有結果」——這是全頁唯一讓我覺得被含糊帶過的地方。
- 同上 `:57`「十五則裡十二則是單一使用者觀感」與 `:127`「十四則裡十二則」——分母兩個答案，讓我開始懷疑其他數字。
- 「已結案／仍開」附依據與日期：✅ 做得好。:55 已結案附 issue 編號＋關閉日；:56、:57 仍開附 issue 編號＋最後更新日；表頭 :50 標「資料截至 2026-09-07」並說明「看官方說明與 issue 開關狀態，不看討論熱度」。
- 「官方未回應」：`:56`「無官方專文」、`:57`「無官方回應」，靠表頭的資料截至日撐時效，可接受，但沒寫「查到哪一天為止、查了哪裡」。
- 「單一使用者觀感」用得很誠實（:57、:134–136 逐列標互動數，甚至標「無互動數」）——這是本頁最值得保留的東西。
- 「統計顯著」只出現在 `:307`（2026-05 MarginLab 每日跑 SWE-bench-Pro，對象 Opus 4.7），有講明對象不是 Opus 5，未誤用。
- 「怎麼自己量一次」三步：第 1、2 步今天就能做；**第 3 步做不到**——判定線掛在 CC-Canary，而 :75 自己說本站沒推薦它，我不知道該不該信那三個判定值。

## 特別提問 6：我最想改的三件事

1. **把「你釘不住你選的模型」搬進三條線那張表的正上方一行**，並讓 index:29 的落點包含它。我是帶著「要不要釘版本」來的，那是全頁最硬的答案，卻放在第 42 行的散文和第 152 行的區塊裡。
2. **修 09-03 事件與 15／14 則數兩處打架**，並把 `opus-5.md:44`「全是單一使用者觀感」改成「除一則 GitHub issue 外」。同一件事兩個答案，會讓我對整頁的數字都打折。
3. **給「多少算異常」一個站內答案**。要嘛把 CC-Canary 的判定線寫成頁面自己的判準（附條件與限制），要嘛直說「本站給不出門檻，你只能看自己的相對變化」。現在的寫法是推薦了一個工具又收回，我兩邊都用不上。

## 特別提問 7：這頁在答幾個問題？該不該拆

退步頁在答 **5 個**問題：①「變笨」是真的嗎、官方認過什麼 ②token 燒得快是怎麼回事 ③Opus 5 上線後品質觀感成不成線 ④我釘不釘得住模型 ⑤我怎麼自己量。

①②③是同一個故事的三條線，共用同一張表、同一個結論，不該拆。⑤是①②③的收尾動作，也該留。
**④是另一個故事**：它自己的起點是 2026-02（比本頁 2026-03 還早）、自己的四個機制節點、自己的結論（⛔ 官方 not planned），而且是我這種讀者會單獨問出口的問題（「Claude Code 能不能鎖版本」）。`:165` 還說它會改到 [[topics/model-comparison]] 的成本換算——會被別頁單獨引用。

建議：拆成一個子頁「你釘不釘得住你選的模型」，母頁在三條線表正上方留兩行結論（釘不住＋issue #27892 not planned）＋連結。母頁維持一個故事：「變笨這件事，哪條線有答案、你怎麼自己量」。
