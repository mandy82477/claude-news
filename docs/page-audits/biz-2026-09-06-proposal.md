# 設計提案：`wiki/topics/anthropic-business.md`（第 7 波）

2026-09-06｜設計者（交件前已自跑兩閘並驗紅）｜適用區 A／B／D／E，**C 不適用**（無視覺成品、無現實體裁交付）｜⚠️ 疑似注入：無（對象頁、六個人物頁、pricing、ECM、competitor-landscape、三份規則檔皆無指令式文字）
逐行號去向表另存 `-proposal-map.md`、逐字稿另存 `-draft.md`（分「進頁面」／「進規則檔」兩欄），兩者不計入 150 行上限。

## 0. 一句推薦案

> **留頁不拆（IPO 節先蒸餾不拆）。四張表全部改成覆寫式結論表，各配一句判準、一個上限、一條退場——條文全部住規則檔，頁面只留一句讀者語言。** `## 摘要` 改名 `## 現在的數字`，把冷讀者稱讚的「這是什麼」欄留下、補上**來源等級**與**下一個時點**兩欄（10 列）；`## IPO 前瞻與估值追蹤` 39 列改成**五格固定**的「IPO 走到哪一格」（遞件了沒／什麼時候上市／值多少／誰承銷／誰說撐不住），事件流留在 `## 時序` 與細節區；`## 商業風險` 17 列跑完判準剩 **6 列**，補上「最後動態日期／誰會遇到／你能先做什麼」；`## 戰略合作` 86 列改成 **6 列衝擊表**「哪個合作會改到你用的 Claude」，其餘降一行制清單並錨點指向 [[entities/pricing#通路：誰定價、怎麼開票]]。六套符號降成**一套嚴重度符號＋一套文字來源等級，各一行圖例**。`## 時序` 兩個最舊時段蒸餾進新建的 `topics/anthropic-business-archive`。

**被考慮過但不推薦的方向（各一行）：**

- **拆出 IPO 成子頁不選**，因為主 session 已定不拆；且全庫 `grep 'anthropic-business#'` 零命中，它沒有被獨立引用的實證，拆了只是把 97 行搬到隔壁。
- **把 39 列 IPO 表壓成 8 列封頂事件表不選**（健檢卡建議），因為封頂表仍是編年體，讀者問的是「走到哪一格」不是「第 8 新的一則是什麼」；五格固定表答得出前者，且永遠不會長。
- **戰略合作其餘 80 列降進 `## 時序` 不選**，因為 05 月那批（Samsung＋SK Hynix、Apollo＋Blackstone、富士通、KPMG、Stainless、Google、Amazon）的時序家在「2026-06-14 以前」主題彙整，而該時段本波正要蒸餾進 archive——搬過去等於搬進封存頁。改為留在本節下方一行制清單。
- **風險表上限訂 8 留兩格 headroom 不選**，因為 6 是拿 17 列跑完判準剩下的數字，8 是為了避免爭議而訂的數字；空位會邀請人往裡塞，這張表就是這樣長到 17 列的。
- **保留「定價透明度危機」「競品分流」兩列並補日期不選**，因為兩列填不出可查證的最後動態日期不是巧合——它們的事實家分別在 [[entities/pricing]]「事故現在還在發生嗎」與 [[topics/competitor-landscape]]「對手雷達」，那兩張表有日期也有退場。本表只留一行指路。
- **為「同一數字住幾個家」加一支比對腳本不選**，因為數字沒有穩定鍵可配；改用「一個指標一列、覆寫不新增」這種結構性去重（§5）。

## 1. 可行性前提（一行）

**本案不需要新進料。** 四張表的每一格都取自頁面既有文字或 `-verified.md` 已查證的來源；`src/news_aggregator/sources/topic_watch.json` **沒有本頁的 topic**（三個 active 為 ai-talent-flow 3／ai-agent-safety 3／competitor-landscape 2，`_global_max` 8 已用滿），本頁靠既有 12 個來源的關鍵字命中，**申請不到額度、本案也不需要**。維護節奏維持每日（`.claude/rules/wiki-ingest-commercial.md` 負責頁面表已列本頁），本案新增的唯一週期性動作是退場、補位與蒸餾複查，而**本頁現在沒有 lint 層條文、`/wiki-lint` 也沒有指向本頁的步驟**——這是裁決點 3。

## 2. 骨架（每節標服務哪幾題，Q 編號同健檢卡）

| 新節 | 取代 | 服務 | 型態 |
|---|---|---|---|
| `## 現在的數字`（10 列＋來源等級圖例） | `## 摘要` L40–71 | Q1、Q2 | 覆寫式結論表 |
| `## IPO 走到哪一格`（五格固定） | `## IPO 前瞻與估值追蹤` L73–125 表 | Q1 | 覆寫式結論表 |
| `## 商業模式`／`## 產品市場契合度（PMF）觀察` | 原樣 | Q1、Q2 | 論述，不動 |
| `## 這些數字是誰說的`（19 列＋來源等級欄） | `## 財務狀況（公開資訊）` L211 | Q1、Q3 | 證據層 |
| `### 上面提到的人是誰`（≤8 人） | 新增 | — | 名字解碼 |
| `## 現在還打得到你的商業風險`（6 列） | `## 商業風險` L255–276 | Q3 | 覆寫式結論表 |
| `## 哪個合作會改到你用的 Claude`（6 列＋一行制清單） | `## 戰略合作` L295–386 | Q4 | 覆寫式結論表 |
| `## 時序`（2026-07 起） | L459–928 | Q1、Q3 | 證據層，蒸餾兩時段 |

**節名改動零風險**：`wiki_graph explain --section` 對八個節**全部**回「錨點邊（無）」，全庫 `grep -rn "anthropic-business#"` 零命中。但**九處跨頁散文直接寫著舊節名**（非 wikilink，建置抓不到），逐處逐字見 draft §K。

## 3. Q1 結論表「現在的數字」（draft §B）

十列各帶「這是什麼／現在的數字（資料日期）／來源等級／下一個時點」。**三個估值一次講清楚**：最後定價 $9,650 億（官方一手，05-28）／IPO 投資人預期 $2 兆（08-13，公司未設此目標）／$3,500 億是 04-24 Google 輪的**歷史值**（一句在細節區，並指 [[entities/google-investment]]）。**採用率改 43.5% vs 39.7%**（Ramp 8 月指數，7 月資料，08-12 發布），05-15 的 34.4% vs 32.3% 改寫成「首度超越那一次的歷史數字」。**補貼倍數改「社群估算 13–44 倍」**，三個方法論（token-xray 17／modelplane.ai 13／Quesma 40）逐筆列在細節區。

**兩行圖例**（draft §B、§D）：來源等級五值由硬到軟；嚴重度三值 ⚠️ 高／⚠️ 中／ℹ️ 低。**判準、上限、退場全部不在正文**，在 draft §J 第 1 條。

## 4. Q4「哪個合作會改到你用的 Claude」（draft §E）

六列：Amazon Bedrock（拿不到 50% Batch 折扣、beta 功能不可用）／Claude Platform on AWS（固定 Start 層、月上限 $500）／Google Cloud Vertex（費率由 Google 自訂）／EFS 三雲（資料改存客戶自有雲、免費、秋季分階段）／AWS 模型上線（Fable 5.1）／Microsoft Foundry（CCU 後付）。**錨點兩條**：[[entities/pricing#通路：誰定價、怎麼開票]]（現行 L167 h3，冷讀者判為「唯一真正回答我的一段」）與 [[entities/pricing#乘數：會互相疊乘]]。**訂閱席次的補充是本案對冷讀者最後一個未答問題的回答**：那張通路表講的是 API，席次不走那五條通路，雲端合作不改席次價格或可用性，席次內容見 [[entities/pricing#我的方案現在有什麼]]。

**Microsoft 擇一為家**：Foundry 通路事實進本表（有官方文件）；「以自研模型取代」是傳聞，留在**風險表**第 6 列。同一家公司兩張表但兩件事，各自寫明是哪一件。

## 5. Q3 風險表與同一事實多家（draft §D）

17 列跑完判準剩 6，每列補最後動態日期與來源等級。**三列的家在別頁**，改為表下三行指路（計費事故 → pricing、競品分流 → competitor-landscape、浮水印 → gov-policy）；**五則評論類**（Nvidia-Lambda 循環融資、黃仁勳後悔、Chamath、Steve Eisman、頂尖模型停售）移出表留細節區；田納西那列併入 UTRF。

**砍前保命條款（實測，不是宣稱）：** 逾期懸置 4 筆＝4 個事實，位置 L69（⟨Q-05⟩，細節區，本波依 verified **結案**）、L142（2028 營收預測，細節區，改「多家轉述、Reuters 原文未取得」並延複查日）、L419（⟨Q-02⟩ Volta，戰略合作細節區）、L577（SemiAnalysis，時序 2026-08）。**四筆全部住細節區或時序，沒有一筆的唯一家是被砍的表列**；表列短標記 L56（⟨Q-05⟩）、L222（⟨Q-01⟩）、L330（⟨Q-02⟩）與細節區成對，搬表時成對搬。**另外兩處是真的會斷**：IPO 39 列中 **L109（Axios 07-28 市值超越 Starbucks／McDonald's）與 L122（Yahoo Finance 07-09 早期投資人意外之財）在 `## 時序` 沒有家**——`## 時序` 2026-07 的 `####` 分組從 07-29 直接跳到 07-25，沒有 07-28。實作者刪表前逐列核對，這兩列先 prepend 一行再刪。

## 6. 符號系統與內部用語 10 條

正文只留**一套符號**（嚴重度 ⚠️／ℹ️，一行圖例）＋**一套文字等級**（來源等級，一行圖例）。「僅標題可用」73 次改成來源等級值「傳聞（僅標題）」或散文「（僅標題）」；「（推論）」149 次維持語法但**不再出現在表格儲存格**（四張表的「意涵」欄整欄消失）；「雙重／三重來源確認」改寫成來源等級。

| 冷讀者外洩條 | 處置 |
|---|---|
| 1、3「標／查／複」與 `❓`／`🔎` 差異 | **語法全站不改**（懸置契約）；位置改：只住細節區與時序，不進結論表儲存格。表格正文裸露 **5 → 0** |
| 2 `⟨Q-05⟩`／`⟨Q-01⟩`／`⟨Q-02⟩` 無圖例 | 表下一句：「`⟨Q-nn⟩` 是這一格還沒查實的地方，完整說明在表下細節區同編號那一條。」 |
| 4 嚴重度六種值兩種符號無圖例 | 降三值＋一行圖例（draft §D） |
| 5、6、9 `%%` 內的主編／lint／記者／轉知／訊號日 | **不動**（`%%` 是維運備忘的家，`build_web.py` 建置剝除，讀者看不到）；冷讀者是讀 raw markdown 才看到的 |
| 7、8、10 pricing L213／L424、tool-tracker L137 | **他頁**：pricing 屬第 3 波定稿頁面，**記回訪清單**（L424 未包在 `%%` 內是真外洩）；tool-tracker 非本波射程 |

**這幾條讀者語言閘抓不到**（26 個禁詞裡沒有 `標`／`查`／`複`／`僅標題可用`／`🔎`／`⟨Q-`）。本案靠結構解，**無機械看守，靠人工步驟**：健檢卡六問 Q2 的目視掃描。

## 7. 蒸餾（draft §H）

**2 個時段，正好用滿上限。** 月份算術：本月序 2026-09；09−06＝3 ≥3 ✓（`### 2026-06`，L769–858）；`### 2026-06-14 以前（主題彙整）`（L859–928）涵蓋 2026-05-29～06-14，最新日 06-14，同為 3 ✓。`### 2026-07` 09−07＝2 <3 ✗，不動。**archive 反向指針先查：全庫零 archive 頁指向本頁**（`grep -rn "anthropic-business" wiki/*archive*.md` 零命中），無 gov-policy 那種先修指針的前置。

**三條例外逐條核過：** ① 錨點邊 0（`explain --section "時序"` 回「錨點邊（無）」）；② 唯一細節出處——**這一條本來會擋下 2026-06-14 以前那個時段**：S-1 機密遞交（L879）是全頁唯一寫著這件事的地方，而它正是新結論表「遞件了沒」那一格的依據。**執行順序不可顛倒：先寫好 `## IPO 走到哪一格` 五格表（把 06-02 遞件寫進去），才搬那個時段。** ③ 非延燒／重燃討論。

**新建 `topics/anthropic-business-archive`**（全站第二個 archive 新建案，前例 `entities/pricing-archive`）：標頭領域 💼、`**上層：** [[topics/anthropic-business]]`、狀態 `resolved（封存頁）`、一句 callout、**不補 index 列**，跑 `gen_wiki_frontmatter.py` 投影進母頁列的「↳ 子故事：」。

## 8. 入口（draft §I，index 由主編執行，記者不得改 `wiki/index.md`）

- **`## 💻 開發實務入口` 表末加一列**，句子用冷讀者的原話：「這家公司撐不撐得住、會不會漲價、該不該簽兩年約」。現在首屏唯一帶錢的路（L30）把他送到自己的訂閱帳單頁——照它走，10 分鐘全花在錯的頁上。
- **L113 鉤子句改寫**：現句五項全是 05–07 月事實、**完全沒有 IPO**，而 IPO 是本頁最大的一節；且把兩個最軟的數字（34.4%、17 倍）當事實寫。新句只給文字，且明寫「補貼倍數只有社群估算，沒有官方數字」。
- **L36「誰強」那條誤導路不堵**：那是 competitor-landscape 的鉤子，規則檔 L131–137 已明文把「具名企業採用」排除出該頁，問題在本頁沒有入口而不是那條路寫錯。新增的商業路一上，Q2 讀者就有正確的門。

## 9. 數字自洽與日期口徑

| 處 | 定哪個值 | 理由 |
|---|---|---|
| ARR（L46 的 470 億 vs L52 的 650 億） | **650 億**（2026-07 數字，08-18 四媒體） | L46「整體態勢」整段刪除，470 億降為 archive 封存總結裡的 ARR 軌跡 |
| 採用率 | **43.5% vs 39.7%**（Ramp 08-12） | verified 一手；34.4% 標為「首度超越那一次」的歷史值 |
| 補貼倍數 | **社群估算 13–44 倍** | 三個方法論並陳；17 倍單獨當事實是 verified 點名的最軟卻曝光最高處 |
| EFS 公告日（L44／L302／L390／L472 四處） | **09-01**，並補「EFS 免費」 | 官方原文；H-f62a57 可銷 |
| S-1 機密遞交日 | **2026-06-02** | 本頁記 06-02 且五家同日報導；verified 的轉述站寫 06-01，兩者皆為二手（機密遞交本無官方公告）。**這是我最沒把握的一處，見 §12** |

## 10. 自己過閘的輸出（含驗紅）

暫存檔 `wiki/topics/__bizdraft.md`（只放要進 wiki 的文字，剔除本檔與 draft 的段落標題與轉知逐字），量完即移除；`git status --short wiki/` 確認未動其他 session 的檔。

```
--- 首跑（未修） ---
FAIL: 讀者語言閘 — 1 筆新增的內部用語外洩　topics/__bizdraft.md:45 [二手]
FAIL: 字元上限機械閘 — 2 筆新增　:32 [細節區條列（>200），實測 258]／:33 [同，實測 262]
--- 修後 ---
OK: 讀者語言閘 — 無新增命中
OK: 字元上限機械閘 — 無新增超限
--- 驗紅（同稿注入「ingest／派工／記者／覆寫」一行＋134 字元儲存格＋217 字元條列）---
FAIL: 讀者語言閘 — 5 筆新增的內部用語外洩　:160 [ingest][派工][記者][覆寫]
FAIL: 字元上限機械閘 — 2 筆新增　:160 [表格儲存格（>120），實測 134]／:162 [細節區條列（>200），實測 217]
```

首跑那三筆不是演練是真的命中：「JPMorgan 為**二手**轉述」被閘擋下（禁詞 `二手`，替代詞「媒體轉述」），兩條細節區條列超限。**兩支閘確實在量這份稿。**

| 宣稱 | 真的有嗎 | 出處 |
|---|---|---|
| 儲存格 >120／條列 >200 被擋 | ✅ 已驗紅 | `check_cell_limits.py` `TABLE_LIMIT` L50、`LIST_LIMIT` L51；基線分流 L222–226 只擋新增 |
| 內部用語被擋 | ⚠️ **只擋 26 個禁詞**，不擋懸置 metadata、`⟨Q-nn⟩`、`🔎`、「僅標題可用」 | `check_reader_language.py --list` 全跑過。本頁 5 筆表格裸露 metadata 全部通過該閘。**無機械看守**，靠人工步驟：健檢卡六問 Q2 目視掃描 |
| 改節名不會斷錨點 | ✅ | `explain --section` 對八節全回「錨點邊（無）」；全庫 `grep "anthropic-business#"` 零命中。**但九處跨頁散文寫著舊節名，機器抓不到**（draft §K） |
| 四張表有淘汰機制 | ❌ 現在沒有，本案補 | `table_census.py` 現回四張表「機制 無」。判定邏輯 `_mechanism()` L39–50：剝掉節名全形括號後，要求規則檔**同一行**同時出現節名與 `淘汰\|移除\|保留\|封存\|覆寫\|蒸餾\|汰除\|到期\|清理`。draft §J 四條刻意把新節名與「覆寫」「移除」寫在同一行，**改完重跑才算數，未重跑前不得宣稱有看守** |
| 風險表「一直在長」 | ⚠️ `table_census` 判它 static 是**量測假象** | `_growth()` L53–68 需半數列帶日期才判成長型，本表無日期欄。**交主 session**（機械層待辦 1） |
| 懸置逾期會被擋 | ❌ 只有 WARN | `check_pending_markers.py` 對本頁 4 筆逾期皆印 `⚠️`，總結行仍是通過 |
| 「同一數字住幾個家」會被抓 | ❌ **無機械看守**；本案改用「一個指標一列、覆寫不新增」結構性去重 | 全庫無此檢查 |

## 11. 裁決點（3 個）

1. **併入四個人物頁 `entities/bernanke`（67 行）／`entities/jensen-huang`（55）／`entities/robert-mahari`（58）／`entities/amir-salek`（61）。** 四頁供料 0–2、停滯 10–58 天、存在必要三題全否，唯一事實都已完整寫在本頁。落點是新的 `### 上面提到的人是誰` 一行一人；四頁各留 redirect 殼（`wiki-ingest-format.md`「對稱規則：併回」明訂，否則週報與外部連結永久斷）。**殼的規格照第 6 波配套**：領域改 💼（母頁領域）、`**上層：** [[topics/anthropic-business]]`、識別字「已併回」、index 列刪、`gen_wiki_frontmatter.py` 的「↳ 子故事」投影已排除 redirect。map J 表照「併入」寫，並附「若裁留」的替代（每頁補一句「他這個動作對讀者意味什麼」，否則仍是名片頁）。**併頁屬總帳規定必須停下來問的事項，設計者不代判。**
2. **`## 戰略合作` 86 列砍成 6 列衝擊表。** 這是砍整節等級的改動（86 → 6，其餘降一行制清單留在原節）。事實一筆不刪，但表的角色從「證據層」變成「結論層」，讀者看到的內容量掉九成。未回覆時的保守預設是照做——冷讀者對這張表的評語是「Naver 投資 700 萬美元和 AWS 上線新旗艦同表等權」，而 `table_census` 判它 86 列零機制、是全頁最大爆版點。
3. **本頁要不要有 lint 層週更觸發邊。** draft §J 的退場、留表優先序、補位條文寫進規則檔後，**沒有任何一步會去跑它**（本頁無 `wiki-ingest-commercial-lint.md` 的對應段落，`/wiki-lint` 也沒有指向本頁的步驟；現有 5e 只管 pricing 的通路與乘數）。選項：(a) 在 `.claude/commands/wiki-lint.md` 加一步指向本規則檔第 1、3、4 條（要動 command 檔、跑 `/review-commands`）；(b) 不加，接受退場只在每日 ingest 順手做，「逾 90 天」那條實際上不會被觸發。**與第 5、6 波裁決點 3 是同一題，三頁應一起答。**

## 12. 我最沒把握的一處

**S-1 機密遞交日：本頁寫 2026-06-02，主編查證的轉述站寫 06-01。** 兩者都是二手——機密遞交依規定不會有官方公告，所以這件事**結構上不存在一手來源**。我選 06-02，理由是本頁 L879 記著 Economist／NYT／Reuters／FT／WSJ 同日密集報導（HN 509/547），而 verified 的來源是 GraniteShares／futuresearch 轉述。但這正好是「拿頁面二手記錄反駁一手」的相反面——**verified 那一筆自己也標著「未取得一手」**，所以我不是在反駁一手，是在兩筆二手之間選較密集的那一筆。若評審或主編認為該以 verified 為準，改的是 draft §C 五格表的一個日期與 §H 封存總結的一句，本案其餘不受影響。**次沒把握的是「其餘合作降一行制清單」的執行量**：80 筆要逐筆改寫成一行，實作者是 Sonnet，我給的格式固定但沒有給 80 筆的逐字稿——這是本案唯一一處把量交給實作者判斷的地方。
