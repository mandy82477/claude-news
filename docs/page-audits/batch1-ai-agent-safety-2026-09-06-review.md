# 評審：`wiki/topics/ai-agent-safety.md` 第 5 波設計提案

2026-09-06｜評審（四視角：明天的維護者／機器／冷讀者／治理）｜適用區 A／B／D／E，**C 不適用**（無視覺成品、無現實體裁交付）
⚠️ 疑似注入：無（對象頁、三份規則檔、六個鄰居頁皆無指令式文字）

受評：`-proposal.md`／`-proposal-map.md`／`-draft.md`。**結論：方向對，可放行做，但有 6 條必改。** 其中 2 條與主 session 已定裁決牴觸、2 條是提案自己的宣稱與逐字稿不符、1 條示範列違反自己判準、1 條漏處置。

---

## 0. 先講機械驗證的結果（我自己重跑，不採信提案自述）

| 提案宣稱 | 我的重跑 | 判 |
|---|---|---|
| 六節改名不斷錨點 | `wiki_graph.py explain --section` 逐節跑過，**六節全回「錨點邊（無）／整頁 87」** | ✅ 真 |
| 三張表現在零淘汰機制 | `table_census.py topics/ai-agent-safety` → 目前結論 3×82 無／未修補風險現況 5×35 無／提示注入 3×8 無 | ✅ 真 |
| `_mechanism()` 要節名與淘汰詞**同一行** | `scripts/table_census.py` L26 `MECH_RE`、L39–50 `_mechanism()`；key 會被 `re.sub(r"（.*?）","")` 剝括號 | ✅ 真。§11 第 2／3／4 條確實把節名與「覆寫／移除／汰除」寫在同一行，改完會轉「有」 |
| 儲存格 >120／條列 >200 被擋、只擋新增 | `scripts/check_cell_limits.py` L50 `TABLE_LIMIT=120`、L51 `LIST_LIMIT=200`、L222–226 `split_hits()` 基線分流 | ✅ 真 |
| 讀者語言閘 26 詞 | `--list` 實跑 26 詞；**我拿這 26 詞逐一 grep `-draft.md`，零命中**；另自算 draft 全部表格儲存格與條列，無一超限 | ✅ 真 |
| 懸置 49 筆／逾期 21／相異探針 8 | 自寫 `w5b.py` 呼叫 `pending_markers.iter_pending()` 重算：**49／21／8，八組行號與提案完全一致**（唯 `Prompt Injection Metrics` 逾期 9 天非 10） | ✅ 真 |
| 目前結論 78 列有日期／4 列 `—` | `awk` 重算 L99–184：84 條 `|` 行＝表頭 2＋資料 82；`| — |` 命中 L180–183 **4 列** | ✅ 真（健檢卡的 76／6 是錯的） |
| 蒸餾只有 1 個合格時段 | 09−06＝3 ≥3、09−07＝2 <3。技術彙整 2026-06 條目實有 **16 條**（L741/749/757/778/788/796/803/810/818/829/837/845/853/860/915/922），扣 3 條例外＝**13 條可蒸** | ✅ 真 |
| 訊號表 8 則／7 個相異日／11 天 | 表列日期 08-23、08-26、08-27、08-28、08-29、08-30、09-02、09-02 → 8 列、7 個相異日、跨 11 天 | ✅ 真 |
| 本頁斷鏈 WARN 兩筆 | `build_web.py`：全庫 15 筆，本頁 2 筆（`news/2026-08-06`、`news/2026-07-27`） | ✅ 真（「本頁唯二」成立） |
| topic_watch 本頁 1 條 query／max 3／global 已滿 | 實讀 JSON：ai-talent-flow 3＋ai-agent-safety 3＋competitor-landscape 2＝8＝`_global_max` | ✅ 真 |
| 懸置逾期只有 WARN | `check_pending_markers.py` 檔頭第 9 項；今日全庫實跑狀態 ✅ | ✅ 真 |

**基線**：`python scripts/run_tests.py` 今日**全綠**（check_rules 五檢查、check_pending_markers、check_tools_page、check_hierarchy、兩支閘皆 OK）。任何一項在實作後轉紅都是本波造成的。

**去向表抽驗 7 段**（契約要求 ≥5）：L54 表上說明、L56–57 表頭五欄、L62 Auto Mode、L88／L89／L90 三列、L253 EFS、L818／L829／L860 三條例外、L869／L941／L1001／L1166 節界——**全部對得上原檔行號**。map 開頭「`explain` 行號少 24」我用 `entities/fable-5`（檔案 L154 ↔ explain L130）驗過，成立。

---

## 🔴-1 第 1 列狀態 ⛔ 與主 session 已定裁決牴觸

主 session 就裁決點 4 已代判：**Auto 模式那列判 🔴**，官方「非安全邊界」立場寫進「官方擋到哪」格，⛔ 嚴守「官方明示拒修」。提案 §14 自己也把這條列為最沒把握處，顧慮已被裁決收掉；draft §D 仍寫 ⛔。

**改 `-draft.md` §D 表 Auto 模式那列第 3 欄**：`⛔` → `🔴`
表上引言的五值定義不動（⛔ 仍由 symlink 那列使用）。

## 🔴-2 「10 列全收」算術不成立——合格的是 11 列

主 session 就裁決點 2 代判「**10 列全收**」，依據是提案 §13-2 的「改 10 列即可全收」。但提案 §3 與 map C 自己算出的是 **8 列在表＋第 9–11 名下沉＝11 列合格**（我把 map C 的 35 列逐列加總核過：入表 9 原始列→8 表列、下沉 3、併 2、退場 2、判準刷掉 20，合計 35，無遺漏）。**10 列不是全收，是讓 1 列。**

處置：照 10 列做，明寫讓位者是灰市轉售那列，並在 catch-all 點名它，保住 `entities/pricing` 的反向入邊。

**改 `-draft.md` §D 表，在 infostealer 那列之後補兩列（逐字）：**

```
| Claude for Chrome 兩項權限缺陷：合成點擊可觸發預設工作流、`skipPermissions` 可無同意提示啟用特權模式 | 裝了 Claude for Chrome、且瀏覽器裡另有具 claude.ai 權限擴充功能的人 | 🔴 | 2026-05-21 通報、2026-07-19 公開，未見修補版本號 | 不要同時裝其他會拿 claude.ai 權限的擴充功能；不用時把它停用 |
| Gmail 整合可未經詢問直接寄信，不再需要人工確認草稿 | 已啟用 Gmail 整合的人 | 🔴 | 無獨立官方安全說明（2026-08-19 媒體報導） | 把信箱權限收成唯讀搜尋，不給代寄與刪信 |
```

**改 §D 表上引言最後一句**：`上限 8 列，依「誰會遇到」的範圍由寬到窄。` → `上限 10 列，依「誰會遇到」的範圍由寬到窄；寫「所有人」的一律在最上。`

**改 §D catch-all 整句（逐字）：**
```
**沒列進上表的**：灰市轉售的折扣帳號，營運者可以讀走你送出的每一段 prompt——它還在發生，但只影響走灰市管道買帳號的人，細節見下方事件記錄與 [[entities/pricing]]。
```

**連帶**：`-proposal.md` §3／§11 第 2 條／§13-2 的「上限 8 列」全部改 10，排序句補「寫『所有人』的一律在最上」；map C 的 L80、L66 由「下沉」改「入表第 9／第 10 列」，只有 L75 維持下沉。

## 🔴-3 示範列違反自己寫的排序判準

§3 明寫「**我丟掉了一條自己先寫的排序規則……只留寬窄一把尺**」。但 draft §D 第 1 列是「開 Auto 模式、且會讓它讀網頁或外部檔案的人」，第 2 列是「**所有**在本機跑 Claude Code 的人」——第 2 列明顯比第 1 列寬，示範列第一眼就打自己的判準（skill 檢查清單點名的「示範列違反自己判準」）。

**改 `-draft.md` §D**：把 SSH 私鑰那列**上移為第 1 列**，Auto 模式列降為第 2 列，其餘不動。搭配 🔴-2 的排序句補丁後，判準與示範列一致。

## 🔴-4 摘要說「上表」，但摘要在表之上

§2 骨架是 callout → `## 摘要` → `## 現在會打到你的`。draft §C 第 1 段寫「**上表**列出現在還打得到你的八個面」——方向反了，「八個」在 🔴-2 之後也不對。

**改 `-draft.md` §C 第 1 段第 3 句（逐字）：**
```
下方「現在會打到你的」列出十個面，以及各自能先做的一件事。
```

## 🔴-5 EFS 移位會弄丟兩條出邊，與提案 §5 的宣稱相反

提案 §5 與 map F 都寫「原條目兩條出邊（`topics/anthropic-business`、`entities/pricing`）隨新位置保留，本頁出邊數不減」。但 draft §D「官方現在擋到哪」第 2 點**逐字稿裡一個 wikilink 都沒有**。我 grep 過：`entities/pricing` 在本頁**只出現在 L258 這一行**，刪掉即歸零。

**改 `-draft.md` §D「官方現在擋到哪」第 2 點，句末補一句（逐字）：**
```
企業採用與資料主權面見 [[topics/anthropic-business]]，它同時是 [[entities/pricing]] 兩則企業資料保留傳聞的官方版本。
```

## 🔴-6 infostealer 的懸置標記只清掉一半

`-verified.md` 第二節已把 infostealer 結案（官方 08-30 起強制登出、移除已存付款方式、退款；平台未被入侵）。map C 只寫「L63……懸置標記移除」。但該探針組實際掛在 **L63 與 L275 兩處**（我重算：`'infostealer、session 劫持' -> [63, 275]`），L275 在技術彙整、是它的「家」。L63 隨表改寫消失後，**L275 的 `❓ 待查證（標 2026-08-31｜查 infostealer、session 劫持）` 會原封不動留著**，讀者在證據層讀到一個主編已經查實的問號。這正是「事實更正必回掃」要治的病。

**改 `wiki/topics/ai-agent-safety.md` L275 整行（逐字）：**
```
- ✅ **2026-08-30 官方處置**：Anthropic 主動通知受影響用戶、強制登出、移除已存付款方式並退款未授權扣款；惡意軟體家族含 Vidar、LummaC2、StealC、RedLine、Acreed、AMOS，平台本身未被入侵。
```
**並在 map I 表末加一列**：`infostealer、session 劫持｜L63／L275｜結案移除（非定家）｜兩處皆改，不留標記`。

---

## 🟡（7 條，附修法）

**🟡-1 懸置去重只做了逾期那 8 組，全頁其實有 14 組重複。** 我重算全部 17 組探針：**14 組掛在 2–5 處**（`Ars Technica…` 5 處、`.git config…` 4 處、`self-replicating…` 4 處、`AISLE…`／`MCP RCE…`／`蜜罐遙測…`／`Gym API…` 各 3 處）。模擬砍掉 L52–186 兩張表後，**仍有 26 筆標記、10 組重複**（技術彙整＋時序各一份）。它們今天未逾期，下一輪就會逾期，21 的帳單會回彈。
**修法**：把 §7 的定家規則從「處理逾期 8 組」擴為「**本頁所有探針組一次定家**」，map I 表補上其餘六組（家分別為 L236／L251／L315／L372／L324／L333／L350，時序那份改「（待查證，詳見事件記錄）」）。`Ars Technica` 在時序有**兩份**（L1034、L1040），兩份都要降級。

**🟡-2 蒸餾產物放哪裡、空掉的分組小標怎麼辦，都沒寫。** 13 條 2026-06 條目散在四處（開頭編年段、`### (0)`、`### (1)`、`### (5)`）；`### (5) 官方政策收緊` 底下**只有 L915／L922 兩條，全數蒸走後該小標會空掉**。上一輪（2026-05）的做法是「一個 `### 2026-05 條目封存總結（技術彙整）` 塊放在 (1) 尾端＋各分組留一行 `> …見 archive` 指針」。
**修法**：map F 補一句——`### 2026-06 條目封存總結（技術彙整）` 緊接在既有 L869 之前插入；`### (5) 官方政策收緊` 底下改留一行 `> 2026-06 的官方政策收緊條目（Persona 年齡驗證、Bedrock 推論資料共享）見 [[topics/ai-agent-safety-archive#2026-06]]。`

**🟡-3 改名留下一個頁內死引用。** L357 散文寫「與本頁「**防護機制建議**」既有社群整理（沙盒隔離、操作確認節點等）性質相近」。節改名 `## 拿什麼擋` 後這句指向不存在的節。
**修法**：把該句的「防護機制建議」改為「拿什麼擋」，其餘不動。

**🟡-4 給功能記者的第二筆轉知是 no-op。** map H 寫「`entities/claude-code` L88『狀態沿用本頁**既有**五值』→ 直接列出五值」。我讀了該行：**它已經逐字列出五值**（🔴 未修復／✅ 已修復／⛔ 官方拒修／❓ 待查證／🔎 查無官方）。冷讀者不滿的是「沿用本頁既有」六個字，不是缺定義；照現在的交辦寫法，功能記者會回「已符合，不適用」。
**修法**：轉知 note 改為逐字——`把 entities/claude-code L88 的「狀態沿用本頁既有五值：」改成「狀態五值：」，其餘不動。`

**🟡-5 pricing 反向入邊的行號錯了。** 提案 §3 與 map C 兩處寫「`entities/pricing` L214–215 反向指本頁」。實際在 **L239（灰市結論句）與 L240（🔎 查無官方標記，探針含 `[[topics/ai-agent-safety]]`）**；L211 才是冷讀者第 7 條講的填表規則書。實質結論不受影響，但實作者照錯行號開檔會找不到。
**修法**：兩處行號改 L239–240。

**🟡-6 退場條文只會縮，不會補。** 依「距最後動態逾 90 天即移除」跑一遍現行八列：SSH 私鑰最後動態 2026-06-20 → **2026-09-18 到期**；Agentjacking 2026-06-27 → **2026-09-25 到期**。三週內表會從 10 掉到 8，而條文裡**沒有任何一句說下沉的列可以回來**——這張表只有出口沒有入口，幾個月後會退化成「最近事件表」。
**修法**：§11 規則條文第 2 條的退場句末補一句（逐字）——`移除後表未滿載時，從下方事件記錄中挑仍符合三條判準、最後動態最新者補位，並移除其條目末的「（未列入現況表）」註記。`

**🟡-7 治理：契約第 5 條的「整頁去向」小節缺席。** 健檢卡 §7 把本頁判為「**留，並延伸**」，而派工契約寫「節點去留欄若判延伸／併／砍或缺一頁，去向表末尾加『整頁去向』小節」。map 沒有這一節。我逐頁核過六個鄰居的節點三問（健檢卡 §7 全部答了、去留欄齊），也確認**沒有任何頁被順手併掉或砍掉**——這是形式缺項不是偷渡。
**修法**：map 末加一節 `## J. 整頁去向`，三行：本頁＝留＋延伸（延伸物是 `## 現在會打到你的` 與 `## 拿什麼擋` 兩節，不開新頁）；六個鄰居＝全留，無併無砍；`topics/ai-agent-safety-archive`＝留，且本波開始真的用它。

---

## 四視角一句話

- **明天的維護者**：§11 六條規則是本波最有價值的產出——舊條文指名不存在的 `## 事件記錄`，記者每天照著空氣做事。但退場只有出口沒入口（🟡-6），上限數字要跟表對齊（🔴-2）。
- **機器**：改完必須重跑 `table_census.py` 才可以宣稱三張表「有機制」。`check_pending_markers.py` 的 ⟨Q-nn⟩ 對帳不會被本波觸發（本頁 8 組全是 `qid=None`，我確認過），刪重複標記不會 FAIL。
- **冷讀者**：draft §D 的五欄骨架與「你能先做什麼」欄，正面回答了冷讀者 Q2 的原話「`.git`、`llms.txt`、MCP 這三個字在這一節裡一次都沒出現」；§E 每條指名擋哪一列是本波最好的一處。摘要的「上表」（🔴-4）是唯一會讓冷讀者當場迷路的地方。
- **治理**：裁決點 1 我採**預設案**——砍 78 列、留 L180–183 四列、節改名 `## 跨事件的四條結論`。理由：那 4 列是本頁唯一的跨事件結論，砍掉節殼等於把它們埋進摘要，而摘要每段 ≤3 句吃不下四條。

---

## 照順序執行清單（＝實作單）

| # | 改什麼 | 預期輸出／驗證 |
|---|---|---|
| 1 | 依 🔴-1～🔴-5 改 `-draft.md`：狀態 ⛔→🔴、補兩列、SSH 列上移、上限句、摘要「下方…十個面」、EFS 補兩個 wikilink、catch-all 改寫 | draft §D 表 10 列、第 1 列為 SSH、Auto 模式列狀態 🔴 |
| 2 | 依 🔴-2／🟡-1／🟡-2／🟡-5／🟡-6／🟡-7 改 `-proposal.md` 與 `-proposal-map.md` | 兩份文件內部一致：「合格 11 列／上限 10／讓位 1」三個數字對得起來 |
| 3 | 動 `wiki/topics/ai-agent-safety.md`：標頭 L29／L31、callout、摘要、`## 現在會打到你的`（含表下三點與 catch-all）、`## 拿什麼擋`、訊號表三處數字、EFS 移位、參考來源瘦身 | — |
| 4 | 同一次改：L275 infostealer 改 ✅（🔴-6）、L357 節名死引用（🟡-3）、14 組懸置一次定家（🟡-1）、砍 `## 目前結論` L102–179 並改名 `## 跨事件的四條結論` | `python scripts/check_pending_markers.py` → 狀態 ✅；本頁逾期由 21 降至 0–2 |
| 5 | 蒸餾 13 條 2026-06 技術彙整條目 → `topics/ai-agent-safety-archive` 既有 `## 2026-06`（原文一字不刪 append）；主頁換 draft §G ≤15 行總結，位置與 (5) 指針依 🟡-2 | `python scripts/build_web.py` → 錨點 WARN 仍 0；本頁斷鏈 WARN 由 2 降至 0 |
| 6 | 改 `.claude/rules/wiki-ingest-safety-policy.md` L40–43 為 §11 六條（含 🔴-2 的上限 10、🟡-6 的補位句） | `python scripts/table_census.py topics/ai-agent-safety` → 三張表「機制」由 **無 ×3** 轉 **有**；未轉「有」不得宣稱有看守 |
| 7 | 加週更觸發邊（裁決點 3 已代判「要加」）：`.claude/commands/wiki-lint.md` 的 `### 5h.` 之後插入 `### 5i. ai-agent-safety 三表退場複查（主編親做）`，內文指向本規則檔第 2、3、4 條；步驟 8 的 log 範本補一行 `- 安全三表退場（5i）：（複查 N 列，移除 a／補位 b／不動 c）` | `python scripts/run_tests.py` 全綠（含 `check_rules.py` 的 anchors／sync_pairs）；再跑 `/review-commands` 至零錯誤 |
| 8 | 跨頁三筆走帳本：`python scripts/pending_handoffs.py open --from 安全政策 --to 功能 --page entities/claude-code --note "…"`（draft §I 一句＋🟡-4 的六字）、`--to 商業 --page entities/pricing`（L211 規則書進 `%%`）、`--to 商業 --page topics/anthropic-business`（L88／L90 兩句改寫） | `pending_handoffs.py list` 出現 3 筆新 H-id，寫進 `wiki/log.md` |
| 9 | 主編自己做（不登帳）：`wiki/index.md` L107 鉤子句改寫＋`## 💻 開發實務入口` 表末加 draft §H 一列 | `python scripts/gen_wiki_frontmatter.py` → `python scripts/run_tests.py`（含 `check_hierarchy` 的 index 投影）全綠 |
| 10 | 收尾 | `run_tests.py` 全綠、`build_web.py` 錨點 WARN 0，主 session 指名路徑 commit（不得 `git add -A`） |

**驗紅（不只看綠）**：第 6 步改完規則檔後，先故意把「移除」二字從第 2 條刪掉重跑 `table_census.py`，應回「無」；補回應回「有」——確認轉「有」是本波條文造成的，不是別處蹭到的關鍵詞。

---

## 設計者第二輪必須親改的項目

**無。** 六條 🔴 與七條 🟡 都附了可直接貼上的逐字修訂，實作者照實作單執行即可。唯一需要主 session 知情的是：**代判的「10 列全收」在算術上不成立（合格 11 列）**，本評審按「上限 10、灰市讓位並在 catch-all 點名」執行；若主 session 要真正全收，把上限改成 11 並刪掉 catch-all 的灰市句即可，其餘不動。
