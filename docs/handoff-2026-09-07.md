# Session 交接 2026-09-07（全站頁面 review 第 8–12 波；暫停）

## 狀態一句

第 1–12 波共 **17 頁定稿**（含 code-quality-decline），全部 push（最後 commit `db662d80`）。使用者裁決「跑完第 12 波就停」，全站 review **暫停**；廣度優先第 1 層 37 頁做到第 8 頁，佇列見 `scratchpad/bfsq.py` 的輸出或 `docs/page-audits/ledger.md`「三條路徑」。

## 這次 session 改了什麼機制

- **skill 改單檔**：`.claude/skills/page-audit-review/SKILL.md` 4,333 字（原則／角色／派工／邊界），dispatch-prefix 與六個模板已刪；每波教訓只進 ledger 校準表，加一條原則必先併一條。
- **拆頁判準改版**（`wiki-ingest-format.md`「頁面拆分原則」09-07 版、skill 原則 3）：拆子頁看「這頁回答的問題是否太多」（考題分群、每群一子頁、母頁留索引＋重要結論），不按行數／月份／層切；主 session 提過的「證據簿」切法被否決。
- 新閘：`check_pending_markers.py` 標記數基線（只擋減少——回訪待辦：也該擋／列出增加，count 改唯一 key）；讀者語言閘補 8 詞＋退場／蒸餾 collocation 白名單、archive 頁豁免；index 鉤子禁 ⟨Q-nn⟩ 短標記（`wiki/CLAUDE.md`）。
- 規則檔新增：community「四張結論表」「三張結論表（discussions）」「code-quality-decline 三張表」；models「模型頁兩代對照表」泛化＋「同一數字多頁以最保守方為準」；lint 5k／5l／5m。

## 待使用者裁決（未回，皆照保守預設）

- opus-4-8「Opus 5 傳聞」12 行歸檔（不動）。
- **code-quality-decline「模型釘選／靜默降級」拆子頁：三比二**（健檢卡、兩位冷讀者判拆；設計者、評審判不拆）——照不拆；09-14 回訪重判（若 10-03 前無新事件，不拆的推論成立）。
- bernanke／jensen-huang／robert-mahari／amir-salek 併入 anthropic-business（第 7 波起，不併）。

## 接續時怎麼跑

1. `git pull --rebase` 前確認工作區乾淨（本 session 結束時 0 未提交檔）；雲端 pipeline 每日 12／17／22 UTC 會 commit。
2. 下一頁：`topics/community-tech-tools`（入邊 30）；之後 enterprise-tool-tracker、official-community-gap、boris-cherny、coding-workflow-guide、ai-talent-flow…
3. **CP 建議**（使用者問過）：樞紐頁維持全流程（5 新 agent＋4 續用，約 100 萬 token／頁）；中層與葉子改「主編官方查證＋冷讀者＋Sonnet 直修」約五分之一。值錢的是官方查證與冷讀者，貴的是設計者／評審來回（🔴 多為機械層）。
4. 每波開工先做官方查證再派健檢卡（第 12 波教訓：健檢卡把過期但寫得篤定的段落評為最好）。
5. 派健檢卡的句子要寫死「跳數＝開了幾頁、index 算第一跳」與「行號＝檔案原始行號含 frontmatter」。

## 回訪（09-13／09-14）重點

見 ledger 各波「回訪要查什麼」。跨波共通：首屏「我卡住了」列第三次命中（改列序）；patterns／discussions 版型同構冷讀者分不出；帳本接手（社群→功能／模型／商業多筆）；13 頁 frontmatter 漂移已 chore 收。
