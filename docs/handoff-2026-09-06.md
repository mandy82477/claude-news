# 交接文件 — 2026-09-06（桌面版 session → 終端機 CLI）

給在終端機 `claude` 裡接手的自己。**先讀本檔，再讀 `.claude/skills/page-audit-review/SKILL.md` 與 `docs/page-audits/ledger.md`。** 上一份交接 `docs/handoff-2026-09-05.md` 的內容已全部消化，不必再讀。

---

## 0. 三十秒版

- 工作區乾淨、已 push（HEAD 見 `git log -1`），`python scripts/run_tests.py` 綠、`python scripts/build_web.py` exit 0。
- 全站頁面 review 已跑 **2 波 3 頁**（＋試點 1 頁）：`model-comparison`／`model-task-leaderboard`（P1）、`managed-agents`（使用者點名）。總帳與分層在 `docs/page-audits/ledger.md`。
- **為什麼換到 CLI**：桌面版 Code tab 沒有子 agent 續用（`SendMessage`），每多一棒就全量重讀；CLI ＋ `~/.claude/settings.json` 的 `env.CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` 有（使用者 2026-09-06 驗證）。**開工第一句先 `ToolSearch select:SendMessage` 驗一次**，有才走續用軌。
- 下一波：使用者未指定。候選 P3「成本與衝擊」（`entities/pricing`，入邊 135，全庫最高，且是使用者標記的網站重點領域）或 P2「該不該升版」（`feature-radar`／`entities/claude-code`／`code-quality-decline`）。**先問使用者要哪條**，不要自己選。

## 1. 派工契約（雙軌，開工前選軌）

`docs/page-audits/dispatch-prefix.md`：共用前綴逐字＋各角色接續段＋交棒數預算。

| 有 SendMessage（CLI） | 沒有（桌面版） |
|---|---|
| 健檢卡、冷讀者（新 agent）→ 設計者 → 評審 → **SendMessage 續用原設計者**修到無阻擋 → 主 session 實作 → 複驗（新 agent） | 同左，但**不回派設計者**：評審修法即實作單 |

不管哪軌：冷讀者與複驗**一律新 agent、獨立 context、禁止讀 `docs/page-audits/`**；設計者只派一次且交件前自己跑 `check_reader_language.py` 與 `check_cell_limits.py`；記者禁 git stash／checkout／reset（角色檔會帶到）。

## 2. 開放迴路（依急迫）

| 事項 | 何時 | 誰 |
|---|---|---|
| **09-05 日報**：截至 09-06 UTC 16:32 `news/2026-09-05.md` 仍未上 remote；抓料 `src/gathered_archive/2026-09-05.json` 已在。雲端 17／22 UTC 班次會重試；**若開機時仍無，本機跑 `/news-pipeline` 補** | 開機第一件事查 | 主編 |
| 使用者裁決：**熱度上限式判準要不要成法**（ledger 末節，兩選項＋影響面已寫） | 下次對話 | 使用者 |
| 轉知帳本 **5 筆未結案**：H-7dbf7a／H-b432d2（→功能）、H-27721d／H-ab0af9（→安全政策）、H-402a37（→商業）、H-abb47e（→社群）、H-4d498f（→商業，複核 pricing 計費條）、H-b4a266（社群→功能，負向對照首例）——`python scripts/pending_handoffs.py list` 為準 | 下次 ingest／lint 派工附上 | 主編 |
| 第 1 波回訪 09-12：MC 三節有沒有長回、LB 邊界句、凍結節名、字元閘誤擋、`本週註記` 口吻（未修項）| lint | 主編 |
| 第 2 波回訪 09-13：零件表週更有沒有人做、負向對照回流第二例、🔥🔥 有沒有被加回、「訂閱配額 vs token 兩種貨幣怎麼換算」資料缺口 | lint | 主編 |
| 競品頁 7 筆懸置複查 09-12／13；market-signals 三列結算 09-18／19（本機執行，雲端 egress 封鎖） | lint 5c／5h | 主編 |
| `wiki/log.md` 待辦：模型記者下次 ingest 補 `entities/mythos`（06-10 條件式降級 vs 36氪傳聞一句）與 `entities/sonnet-5`（MarkTechPost 三模型分數）| 下次 ingest | 模型記者 |
| 工作區五個未追蹤目錄／檔（`Haiku`／`Opus`／`Reddit`／`Sonnet`／`最新動態（2026-08-28）`）不是本 session 產生的，兩個 session 都沒動；問使用者要不要清 | 順口問 | 使用者 |

## 3. 這兩波學到、下次別再踩

- **設計者不要 N 案**：第 1 波「2–3 個有差異的提案」製造 85 萬 token 注定被否決品；契約已改一個推薦案＋被否決者各一行。
- **主編官方查證是唯一能抓到「記者看不到」的層**：第 2 波兩處事實更正（Managed Agents 仍 beta、計費 $0.08/hr）健檢卡與設計者都沒抓到——他們無 web 工具。樞紐頁健檢**必排一步主編 WebFetch**，記錄寫 `<slug>-<date>-verified.md`。
- **新閘會擋主編自己**：pricing 那條 561 字元被 `check_cell_limits.py` 擋下。寫任何條列先想 200 字元。
- **讀者語言閘只擋清單詞**，抓不到「整段寫給自己看」的結構性洩漏（第 2 波複驗抓到的 L60）——這類仍靠冷讀者，維運備忘一律 `%% %%`。
- **`git add` 指名到檔案**，不要 `git add wiki/`（第 1 波我掃過一次太寬，事後驗證沒掃進半成品，但那是運氣）。
- `pending_handoffs.py` 拒絕 `--from 主編` 與 from==to：跨記者才走帳本，同記者待辦寫 log。
- Windows：python 寫 scratchpad `.py` 再跑，`PYTHONIOENCODING=utf-8`，不要 heredoc inline python。

## 4. 檔案索引

- 總帳／契約：`docs/page-audits/ledger.md`、`docs/page-audits/dispatch-prefix.md`
- 第 1 波：`docs/page-audits/model-comparison-2026-09-05{,-proposals,-review,-final}.md`、`wave1-cold-reader{,-recheck}-2026-09-05.md`
- 第 2 波：`docs/page-audits/managed-agents-2026-09-05{,-final,-review,-verified}.md`、`wave2-cold-reader{,-recheck}-2026-09-05.md`
- 機械閘：`scripts/check_cell_limits.py`（＋`data/cell-limit-baseline.json`）、`scripts/build_web.py` 錨點致命化
- log：`wiki/log.md` 2026-09-05／09-06 各條
