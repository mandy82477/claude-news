# 2026-09-12 待你動作清單（repo 分割與雲端設定）

本機 `CLAUDE_NEWS/` 已經是獨立 git repo（1,841 筆歷史，remote 指向 `https://github.com/mandy82477/claude-news.git`），接點全改完、四支驗證全綠，只差 GitHub 那一端還沒建。以下按順序做，做完哪步告訴 Claude，它接手後面的。

## 第一段：讓新 repo 存在（最急，今晚前）

- [ ] **1. 建 repo**：GitHub → New repository → Owner `mandy82477`、名稱 `claude-news`、**Public**。README／.gitignore／license 三個都**不要**勾。
- [ ] **2. 推歷史**：在你的終端機跑（Git 會跳瀏覽器登入，用你的帳號）：

  ```bash
  cd C:/Users/Mandy/CLAUDE_OBSIDIAN/ObsidianLab/CLAUDE_NEWS && git push -u origin master
  ```

  成功會看到 `master -> master`。本機現在有 4 個 commit 等著一起推（改版乙、看門狗 ③b、web 重建、另一個 session 的 market 拆頁）。

> 沒在今晚前做完的話：今晚的雲端 pipeline 與 GitHub Actions 仍會跑在舊 repo `ObsidianLab`，多長出來的 commit 之後要搬。來不及就跟 Claude 說「先暫停 routine」。

## 第二段：新 repo 的設定

- [ ] **3. Pages**：`claude-news` → Settings → Pages → Source「Deploy from a branch」→ Branch `master`、資料夾 `/ (root)` → Save。新網址會是 `https://mandy82477.github.io/claude-news/web_reader/`。
- [ ] **4. Actions secrets**：`claude-news` → Settings → Secrets and variables → Actions → New repository secret，設兩個（值從舊 repo `ObsidianLab` 同位置抄）：
  - `REDDIT_CLIENT_ID`
  - `REDDIT_CLIENT_SECRET`

  `GITHUB_TOKEN` 不用設，Actions 自動給。
- [ ] **5. GitHub App 授權雲端**：claude.ai → Code → 環境設定（三個 routine 共用的那個環境，ID 結尾 `988i`）→ GitHub 連結 → 把 `mandy82477/claude-news` 加進授權 repo 清單。沒做這步，routine 改指新 repo 後會在 clone 那步失敗。

## 第三段（順手，不急）：雲端網路開 Custom

- [ ] **6. egress**：同一個環境設定 → Network access → 從 Trusted 改 **Custom**，加入下列網域。改了之後每週六的雲端 lint 會自己做完 5b／5c／5m 三步，不再留待辦給本機 `/weekly`；不改也不會壞（規則已改成探測式）。

  ```
  platform.claude.com
  support.claude.com
  docs.claude.com
  www.anthropic.com
  api.github.com
  raw.githubusercontent.com
  www.swebench.com
  aider.chat
  lmarena.ai
  arena.ai
  artificialanalysis.ai
  www.tbench.ai
  llm-stats.com
  huggingface.co
  eqbench.com
  metr.org
  github.com
  openrouter.ai
  ```

## 你做完第 2 步後，Claude 接手的事

1. 用 RemoteTrigger 把三個 routine 的 repo 改指 `claude-news`（新值已寫在 `docs/cloud-runbooks/triggers/*.json`）
2. 跑一次雲端探針，確認新 repo 從專案根啟動、`wiki-reporter-*` 自訂 subagent 看得到
3. 把 vault repo `ObsidianLab` 的收尾 commit `27327bcf`（停止追蹤 CLAUDE_NEWS、刪 workflows、放轉址頁）給你看，你點頭再 push
4. 停用的探針 routine 與 vault 的 `claude-news-split` 分支清掉

## 今天已完成、不用你動的

- 根目錄 CLAUDE.md 178 → 64 行；17 份記者規則搬到 `.claude/reporter-rules/`；`.claude/rules/` 三檔帶 `paths:`；`git add -A` 改由 hook 擋
- GitHub 星數榜「LLM 知識庫」類補查詢；新頁 `topics/llm-wiki-pattern`；新指令 `/wiki-query`
- 雲端 lint 四步改 egress 探測式；`scripts/cloud_egress_check.py`
- 日報改版乙：`daily/YYYY-MM-DD.md` 讀者版、Step 2b、看門狗 ③b 檢查；首份樣本 `daily/2026-09-12.md`
- 雲端探針證實：routine 從 repo 根啟動，子目錄的自訂 subagent 載不到，這就是分割 repo 的理由

## 已知待看（不急）

- `pipeline-change-check` 的 `sentimentRatio` 改版前後都是 0.0，既有問題
- `web_reader/data/weekly/` 七份 JSON 今天曾被某次 build 清空，重建後復原，來源未查明
- 本機跑 `daily_health_check.py` 在 remote 建好前會卡住（它會查 remote）
