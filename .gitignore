# Secrets
.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/

# Logs (keep locally, don't commit)
logs/

# 例外：task_scheduler.log 是 pipeline 的執行紀錄，雲端 routine 每次執行與中止都
# 必須把它 commit + push——容器銷毀後「保留在本機」沒有意義，那是唯一能事後追查
# 「那天到底跑了沒、為什麼中止」的證據。
#
# 上面的 `logs/` 沒有前導斜線，會比對任意深度的 logs 目錄，因此也擋住了
# src/logs/。git 規定「父目錄被排除時無法單獨 re-include 子檔案」，
# 所以要先把該目錄放行、再排除目錄內容、最後單獨放行這一個檔。
# （2026-07-25 雲端執行被此規則擋下，當次改用 git add -f 繞過後回報登記。）
!src/logs/
src/logs/*
!src/logs/task_scheduler.log

# OS
Thumbs.db
desktop.ini
.DS_Store

# IDE
.vscode/
.idea/

# Claude Code worktrees
.claude/worktrees/

# Claude Code 個人設定（權限允許清單，含本機絕對路徑）——每台機器自己的，不進 repo（2026-09-04 公開前掃描）
**/.claude/settings.local.json

# 規則檢查記號檔（dirty-period 提醒 / Stop hook 共用，見 .claude/hooks/）
.claude/.last-rules-check
.claude/.rules-reminder-sent
.claude/.last-tests-ok

docs/architecture-evolution-react.bak.html

# 本機專用雜檔（session 交接筆記等）——不進 repo
docs/local/
.claude/tree-claims/
